# Taption Plan 앱 기획 대화 원문

> 기록 범위: iPhone·iPad 앱 전환 아이디어가 처음 나온 시점부터 앱 기획을 별도 문서로 분리해 달라는 요청 직전까지
>
> 보존 원칙: 사용자와 Codex의 발언을 순서대로 옮겼으며 문장, 오탈자, 링크, 목록을 원문 그대로 유지했습니다. 시스템 지시, 도구 실행 내역, 검색 도구의 내부 결과는 제외했습니다.

---

## 최신 구현 확정 — 실시간 활동 기록·미세먼지·목표 상세 (2026-08-01)

### 걷기·달리기 기록

- 평상시 저장 목표 간격은 설정의 `배터리 최소화 15분 · 균형 5분 · 정확도 최적화 1분`을 따른다. iOS 백그라운드에서 일반 타이머의 정확한 실행 시각을 보장한다고 표현하지 않는다.
- iPhone에서 중간 이상 신뢰도의 걷기·달리기가 10초 지속되면 자동 운동 세션으로 승격한다. 사용자는 iPhone과 Watch에서 `걷기 시작 · 달리기 시작 · 종료`를 직접 실행할 수도 있다.
- 활성 세션은 최고 위치 정확도, 5m 거리 필터, 수평 정확도 50m 이내 좌표를 우선 사용한다. 자동 세션은 2분 정지 후 종료한다.
- 판정 우선순위는 `Watch 운동 유형 → iPhone 동작 분류 → 걸음·보폭·페이스 → GPS 속도`다.
- Watch 운동 중 가속도·자이로·중력·사용자 가속도·회전·자세, 심박수·걸음·거리·활동 에너지, 상대고도·기압·층계, Watch GPS를 수집한다. 30초 단위 요약과 경로 청크를 iPhone에 신뢰성 전송한다.

### 저장·지도 갱신

- 기존 월별 `taption-raw-YYYY-MM.jsonl.zlib`은 계속 읽고 쓴다.
- 활성 세션 원본은 추가로 `연월/세션 UUID/000001.jsonl.zlib` 형식의 30초 독립 청크로 보존한다. 각 샘플은 세션 ID, 기기, 순번, 원본 시각, 정확도, 센서 가용 상태를 포함한다.
- 새 GPS 점은 원본 저장과 `LiveRouteState` 갱신을 먼저 수행한다. 화면의 Apple 지도 위치·폴리라인은 새 점마다 증분 갱신한다.
- 장소·층·최종 이동수단 분석은 세션 종료 즉시 또는 마지막 샘플 2분 후 실행한다. GPS 한 점마다 하루 전체를 다시 분석하지 않는다.
- 사진 위치는 실제 GPS가 비어 있는 시간의 위치 앵커로만 사용하며 연속 이동 경로로 잇지 않는다.

### 날씨·미세먼지

- 위치의 날씨와 함께 PM10·PM2.5 농도, 각각의 등급, 더 나쁜 통합 등급, 관측시각, 측정소, 제공원, 대체값 여부를 저장한다.
- 국내는 에어코리아 WGS84 측정소 중 가장 가까운 곳을 사용한다. 키가 없거나 호출에 실패하거나 국외이면 Open-Meteo Air Quality를 대체값으로 쓰고 UI에 표시한다.
- 국내 등급은 PM10 `0–30 / 31–80 / 81–150 / 151 이상`, PM2.5 `0–15 / 16–35 / 36–75 / 76 이상`을 쓴다.
- 첫 유효 GPS, 3km 이상 이동, 30분 경과, 새 장소 확정 때만 갱신한다. GPS 샘플마다 API를 호출하지 않는다.
- 일간 상단은 `미세 좋음`처럼 통합 등급을 표시하고, 지도 상세에는 PM10·PM2.5 수치, 측정소·제공원과 대체값 여부를 표시한다.

### 검증 완료 기준

- 단위 테스트와 iPhone·Watch 포함 빌드를 각각 통과해야 한다.
- 실기기에서는 권한, 잠금 화면에서 걷기·달리기 5분 이상, 자동/수동 시작, 양 기기의 세션 ID·샘플 시각, 이동 중 지도 선 증가, 종료 후 재읽기, 현재·저장 위치의 미세먼지, 반복 목표 계획·실제 비교를 각각 확인한다.
- 빌드 성공, 설치 성공, 앱 실행, 잠금 상태 센서 기록, 저장 파일 재읽기, 실제 지도 표시를 서로 다른 증거로 남긴다.

---

## 사용자

이거 아이폰. 아이패드 앱으로 만들면 차별화 포인트가 있지 않을까?

---

## Codex (진행 응답)

있습니다. 다만 “웹 WBS를 앱으로 감싼 것”만으로는 약하고, 아이패드는 정밀 편집 도구, 아이폰은 빠른 확인·수정 도구로 역할을 나누는 게 핵심입니다. 현재 Apple 플랫폼 기능을 기준으로 차별화가 될 만한 지점을 확인해보겠습니다.

---

## Codex (진행 응답)

현재 WBS의 강점은 이미 충분합니다. 차별화는 기능을 더 쌓는 것보다, iPad에서는 “회의 중 정밀 편집”, iPhone에서는 “오늘 일정 확인·빠른 수정”으로 역할을 분리하는 데서 나옵니다.

---

## Codex

있습니다. 다만 “HTML을 앱으로 감싼 WBS”는 차별화가 약하고, **iPad는 정밀 편집기, iPhone은 일정 확인·빠른 수정 도구**로 역할을 나누면 꽤 명확한 제품이 됩니다.

가장 강한 차별화 포인트는 다음입니다.

- **iPad 회의 편집 모드**
  - Apple Pencil로 액션아이템 이동·길이 조절·연결선 작성
  - 펜을 대기만 해도 연결점과 조정 핸들 표시
  - 손글씨로 항목명이나 이벤트 바로 입력
  - 지원 기기에서는 Apple Pencil hover로 터치 전 위치 미리보기가 가능합니다. 현재 빨간 연결 핸들과 특히 잘 맞습니다. [Apple Pencil hover](https://developer.apple.com/documentation/UIKit/adopting-hover-support-for-apple-pencil)

- **iPhone 오늘·이번 주 화면**
  - 전체 1년 WBS를 억지로 편집하지 않고 오늘, 지연, 이번 주 일정만 표시
  - 날짜 앞뒤 이동, 메모 추가, 이벤트 생성 정도만 빠르게 처리
  - 홈·잠금화면 위젯에서 오늘 일정 확인과 간단한 조작도 가능합니다. [Interactive Widgets](https://developer.apple.com/documentation/widgetkit/adding-interactivity-to-widgets-and-live-activities)

- **한 개의 프로젝트 파일**
  - 지금처럼 서버와 계정 없이 오프라인 사용
  - iCloud Drive/파일 앱에서 열기·저장·복제·버전 관리
  - Windows에서는 동일 파일을 브라우저로 열 수 있게 유지
  - Apple의 문서형 앱 구조는 파일 열기·저장, 다중 창, 드래그 앤 드롭을 지원합니다. [SwiftUI DocumentGroup](https://developer.apple.com/documentation/swiftui/building-a-document-based-app-with-swiftui/)

- **회의가 끝나면 바로 납품**
  - 화면과 동일한 Excel/PPTX/PNG 출력
  - 메일, AirDrop, Teams 등으로 즉시 공유
  - “모바일 일정 앱”보다 **문서 결과물이 정확한 WBS 도구**라는 차별점이 생깁니다.

제가 잡을 제품 문구는 이렇습니다.

> 회의 중 iPad에서 바로 고치고, iPhone에서 오늘 일정을 확인하며, Windows에서도 같은 파일을 여는 정확한 WBS.

개발은 처음부터 전부 Swift로 다시 만들 필요가 없습니다.

1. SwiftUI로 파일 관리·오늘 일정·위젯·알림을 구현합니다.
2. 현재 검증된 WBS 편집기는 우선 WKWebView로 재사용합니다.
3. iPad에 Pencil·키보드·트랙패드 기능을 결합합니다.
4. 사용성이 확인되면 캔버스만 네이티브로 단계적으로 전환합니다.

단순 웹 포장 앱은 App Store의 최소 기능 기준에 걸릴 수 있으므로, 파일 앱 연동·위젯·Pencil·알림·네이티브 오늘 화면까지 포함해야 합니다. [App Review Guideline 4.2](https://developer.apple.com/app-store/review/guidelines/)

결론적으로 **iPad 중심 + iPhone 보조 앱**이면 차별화가 있습니다. 핵심은 AI나 협업 기능을 많이 넣는 것이 아니라, **회의 중 수정 속도, 일정의 정확성, 한 파일의 이동성, 출력 충실도**입니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:127-133|note=[used existing WBS interaction connector export and single file product constraints]
MEMORY.md:121-123|note=[used prior WBS browser validation and compact layout requirements]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

이런 앱이 있어?

---

## Codex (진행 응답)

유사한 간트/WBS 앱은 있지만, 우리가 말한 조합이 실제로 겹치는지 나눠서 보겠습니다. 특히 iPad 정밀 편집, iPhone 보조 화면, 오프라인 단일 파일, Excel·PPT 출력까지 비교해볼게요.

---

## Codex

있습니다. 솔직히 말하면 **가장 가까운 앱은 QuickPlanX**입니다. 우리가 생각한 방향과 상당히 겹칩니다.

| 앱 | 겹치는 부분 | 다른 부분 |
|---|---|---|
| **QuickPlanX** | iPhone·iPad·Mac, WBS, 간트, 드래그 재정렬, 작업 연결, 위젯, 오프라인, iCloud 동기화·백업 | PPT 직접 출력 없음, 일정 표현과 연결선 자유도가 제한적 |
| **OmniPlan** | iPhone·iPad·Mac, 간트, 네트워크 다이어그램, 의존관계, iCloud, Apple Pencil 입력 | 자원·비용·임계경로 중심의 전문 PM 도구라 복잡함 |
| **Project Plan 365** | iPhone·iPad, Windows 호환, MPP, Excel·PowerPoint 출력 | 사실상 모바일용 Microsoft Project에 가까워 UI가 무거움 |

### 가장 가까운 경쟁자

**QuickPlanX**

WBS 구조, 간트 막대, 작업 연결, 드래그 재배치, 홈 화면 위젯, CloudKit 동기화, 오프라인 작업과 자동 백업까지 제공합니다. 즉 **“iPad/iPhone + 단순한 간트 + 오프라인”만으로는 차별화가 어렵습니다.** [QuickPlanX App Store](https://apps.apple.com/ca/app/quickplanx-project-gantt-plan/id6502736030)

**OmniPlan**

Apple 쪽의 대표적인 전문 프로젝트 관리 앱입니다. 간트와 네트워크 다이어그램, 자동 일정 계산, MPP 호환, iCloud 동기화, Apple Pencil Scribble을 지원합니다. 다만 우리가 원하는 간결한 일정표보다 훨씬 무겁습니다. [OmniPlan 기능](https://www.omnigroup.com/omniplan/features/)

**Project Plan 365**

Microsoft Project 파일을 iPhone·iPad에서 열고 편집할 수 있으며 Excel, 이미지, PowerPoint 출력도 제공합니다. 하지만 공식 설명만으로는 화면 그대로의 **편집 가능한 PPT 도형 출력**인지 확인되지 않습니다. [Project Plan 365](https://apps.apple.com/us/app/project-plan-365/id742239541), [출력 기능](https://www.projectplan365.com/onlinehelp/import-export/)

### 그래도 우리 쪽에 남아 있는 차별점

경쟁 제품 공식 자료에서 다음 조합은 찾지 못했습니다.

- 액션아이템 위에 이벤트를 자유롭게 겹쳐서 배치
- PPT처럼 자유로운 연결선, 교차 건널목, 조정 핸들 및 자동 회피 배선
- 화면과 동일한 결과를 **편집 가능한 PPT 표·도형**으로 출력
- Excel에서도 일정표 전체 모양을 그대로 재현
- 한 장에 6개월~1년이 들어가는 보고용 일정표
- 한국 공휴일과 워킹데이 자동 계산
- 동일한 단일 HTML 파일을 Windows와 Apple 기기에서 모두 사용
- 자원·원가·진척관리 없이 일정만 간단하고 정확하게 관리

따라서 **“간트 프로젝트 관리 앱”으로 출시하면 QuickPlanX·OmniPlan과 정면충돌**합니다. 대신 이렇게 잡는 게 좋습니다.

> 복잡한 프로젝트 관리가 아니라, 회의에서 바로 수정하고 Excel·PowerPoint로 그대로 제출하는 한국형 일정표.

특히 **화면 그대로 편집 가능한 PPT로 출력되는 기능**과 **자유 연결선·이벤트 오버레이**가 실질적인 차별화 포인트입니다. Apple Pencil은 좋은 사용 경험이지만, 그것만으로는 경쟁사가 쉽게 따라올 수 있으므로 핵심 차별점으로 삼기에는 약합니다.

개발 전에 iPad에서 **QuickPlanX와 OmniPlan을 직접 사용해보는 경쟁제품 검증**은 반드시 해보는 게 좋겠습니다. 그 두 앱을 써보고 불편한 지점이 지금 WBS의 제품 방향을 더 정확하게 만들어 줄 겁니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:127-133|note=[used existing WBS connector export and single file requirements for competitor comparison]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

너무 무겁지는 않게. 다른 일장관리랑 다른 간트차트 기반으로 일반인이 사용하는 시나리오면 주별계획. 일병. 월별. 년도별 계왹 이런식으로 나눠서 작성할 수 있으면 좋겠네?

---

## Codex (진행 응답)

그 방향이 더 좋습니다. “프로젝트 관리 앱”이 아니라 **시간 단위별로 계획을 확대·축소하는 생활형 간트 일정표**로 잡으면 훨씬 가볍고 일반인에게 설명하기 쉽습니다. 화면 구조와 데이터 흐름이 복잡해지지 않는 선에서 정리해보겠습니다.

---

## Codex

그 방향이면 훨씬 차별화됩니다. **“프로젝트 관리용 WBS”가 아니라 “일반인이 쓰는 간트 기반 계획표”**가 되는 겁니다.

핵심은 일별·주별·월별·연도별 계획을 각각 따로 저장하는 게 아니라, **하나의 계획을 시간 배율만 바꿔서 보는 구조**로 만드는 것입니다.

### 기본 화면

상단에 네 가지 보기만 둡니다.

`일별 | 주별 | 월별 | 연도별`

- **일별**: 시간 단위로 오늘 할 일 배치
- **주별**: 월~일 사이에 이번 주 계획 배치
- **월별**: 한 달의 주요 일정과 기간 확인
- **연도별**: 여행, 시험, 이사, 프로젝트 같은 큰 계획 확인

계획 막대를 이동하면 모든 화면에 즉시 반영됩니다. 예를 들어 주별 화면에서 일정을 이틀 미루면 월별·연도별 화면도 같이 바뀌어야 합니다.

### 일반인 사용 시나리오

1. 연도별 화면에 `자격증 취득`을 3~6월로 등록
2. 월별 화면에서 `강의 수강`, `복습`, `시험 접수`로 나눔
3. 주별 화면에서 `1~3강 듣기`를 월~수요일에 배치
4. 일별 화면에서 오늘 저녁 8시 일정으로 지정
5. iPhone 위젯에는 오늘 해야 할 계획만 표시

이렇게 하면 단순한 할 일 앱과 달리 **오늘 하는 일이 올해 계획 어디에 포함되는지** 항상 보입니다.

### 무겁지 않게 만드는 기준

입력창의 기본 항목은 이것만 있으면 됩니다.

- 계획명
- 시작
- 종료
- 색상
- 메모

담당자, 비용, 자원, 임계경로, 진척률, 복잡한 의존관계는 기본 화면에서 제외합니다. 현재 연결선 기능은 없애기보다 **‘관계선 보기’ 옵션을 켰을 때만 나타나는 고급 기능**으로 두는 게 좋습니다.

명칭도 일반인에게 맞추는 편이 좋습니다.

- 액션아이템 → `계획`
- 이벤트 → `중요 일정`
- 항목 → `분류`
- WBS → 화면에서는 사용하지 않음

분류는 `업무·개인·건강·학습·가족`처럼 시작하고 사용자가 추가하게 합니다.

### 기기별 역할

**iPhone**

- 기본 시작 화면은 오늘
- 일별·주별 편집 중심
- 월별·연도별은 요약 확인
- 빠른 계획 추가와 날짜 이동
- 위젯에는 오늘 일정 3~5개만 표시

Apple도 위젯은 복잡한 앱 화면보다 핵심 정보와 단순한 동작에 집중하도록 권장합니다. [Apple 위젯 디자인 지침](https://developer.apple.com/design/human-interface-guidelines/widgets)

**iPad**

- 주별·월별 편집 중심
- 연도 전체 계획 표시
- Apple Pencil과 드래그로 막대 이동·길이 조절
- 필요할 때만 관계선과 세부 설정 표시
- 가로 화면에서는 왼쪽 분류와 오른쪽 간트 화면 구성

iPhone에서는 탭, iPad에서는 사이드바로 자동 전환할 수 있어 같은 앱을 무리 없이 구성할 수 있습니다. [Apple 사이드바 지침](https://developer.apple.com/design/human-interface-guidelines/sidebars)

이 제품의 핵심 문장은 이렇게 잡을 수 있습니다.

> 오늘 할 일부터 올해 계획까지, 하나의 시간표에서 이어서 관리합니다.

첫 버전은 **일·주·월·연 보기, 드래그 일정 배치, 중요 일정, 분류, iCloud 저장, 오늘 위젯**까지만 만드는 게 적당합니다. PPT·Excel 출력과 자유 연결선은 iPad용 고급 기능으로 뒤에 붙이면 무겁지 않으면서 현재 WBS의 장점도 살릴 수 있습니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:129-134|note=[used current WBS editing export and compact timeline requirements to define a lighter consumer product]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

이게 어짜피 모수를 늘릴려면 일반인 대상이어야 하고 일반인이면 년도별로 계획하고 월별 주별 일별 세부 계획을 만들어서 나중에 1년을 돌이켜 보면 전체 나의 삶이 나오는 거면?

---

## Codex (진행 응답)

맞습니다. 그 지점부터는 단순 일정 앱이 아니라 **“계획이 자동으로 인생 기록이 되는 앱”**이 됩니다. 다만 계획표가 실제 삶의 기록으로 남으려면 ‘계획한 것’과 ‘실제로 한 것’을 부담 없이 구분하는 설계가 핵심입니다. 비슷한 제품이 어디까지 와 있는지도 함께 확인해보겠습니다.

---

## Codex

맞습니다. 이 방향이면 제품의 정체성이 확실해집니다.

> **미래를 계획하는 간트차트와, 과거를 돌아보는 인생 기록을 하나로 연결하는 앱**

일반 일정 앱은 오늘과 이번 주만 보여주고, 일기 앱은 이미 지나간 일을 사용자가 직접 기록해야 합니다. 이 앱은 **계획한 일정이 시간이 지나면서 자동으로 삶의 타임라인이 되는 것**이 핵심입니다.

### 하나의 계획, 네 가지 거리

계획을 네 벌 만드는 것이 아니라 하나의 데이터를 확대·축소합니다.

- **연도**: 취업, 이사, 여행, 자격증, 건강 같은 큰 흐름
- **월별**: 큰 계획을 단계별로 나눈 일정
- **주별**: 이번 주에 실제로 실행할 계획
- **일별**: 시간대별 행동과 약속

연도 화면의 `자격증 취득`을 누르면 월별 준비 과정이 나오고, 다시 누르면 주별 공부 계획, 일별 공부 시간이 나오는 식입니다. 지도처럼 확대하면 세부 계획이 나타나는 구조가 적합합니다.

### 기록으로 만들기 위한 최소 확인

계획이 있었다고 실제로 한 것은 아니므로, 하루가 끝날 때 복잡한 진척률 대신 세 가지만 묻습니다.

- `했어요`
- `다음으로 이동`
- `건너뜀`

여기에 원할 때만 사진이나 한 줄 메모를 붙입니다. Apple의 Journaling Suggestions를 이용하면 사용자가 선택한 사진·방문 장소·운동 같은 순간을 기록 후보로 가져올 수도 있습니다. [Apple Journaling Suggestions](https://developer.apple.com/documentation/JournalingSuggestions)

### 1년 뒤에 보이는 화면

연말의 `나의 2026년` 화면에는 다음이 나타납니다.

- 업무·가족·건강·학습·여행별 연간 타임라인
- 계획했던 시점과 실제 실행한 시점
- 중요 일정과 기억에 남긴 사진
- 가장 바빴던 달과 여유로웠던 달
- 계속 이어진 계획과 다음 해로 넘긴 계획
- 월별 한 줄 회고

이것을 한 장의 이미지나 PDF로 저장하면 단순한 통계가 아니라 **“올해 내가 어떻게 살았는지”가 보이는 인생 간트차트**가 됩니다.

### 기존 앱과의 차이

유사 요소는 이미 있습니다.

- Timestripe는 일·주·월·연도·인생 단위 목표를 연결합니다. [Timestripe Horizons](https://timestripe.com/magazine/learning-center/features/)
- Structured는 일·주·월 타임라인으로 일정을 관리합니다. [Structured](https://help.structured.app/en/articles/380546)
- Apple Journal과 Day One은 지나간 삶을 기록하고 찾아보게 합니다. [Apple Journal](https://apps.apple.com/us/app/journal/id6447391597), [Day One](https://dayoneapp.com/guides/day-one-ios/journal-views-in-day-one-for-ios/)

하지만 확인한 공식 기능 기준으로는 **간트 기반 장기 계획이 실행 기록으로 전환되고, 연말에 한 사람의 생활 타임라인으로 완성되는 조합**은 뚜렷하지 않습니다.

### 무거워지지 않게 할 기준

첫 버전에는 이것만 두는 게 좋습니다.

- 일·주·월·연 보기
- 계획 막대 이동과 길이 조절
- 업무·개인·건강·학습·가족 분류
- 했어요·이동·건너뜀
- 한 줄 메모와 사진 한 장
- 월간·연간 돌아보기
- iCloud 저장과 개인정보 잠금

연결선, Excel·PPT 출력, 복잡한 글꼴 설정은 iPad의 고급 보기로 숨깁니다. AI, 협업, 비용, 담당자, 진척률은 넣지 않는 편이 맞습니다.

그리고 중요한 건 **연말 보고서가 앱을 쓰는 이유가 아니라 보상**이라는 점입니다. 매일 앱을 여는 이유는 오늘 계획이 간단해서이고, 계속 사용한 결과로 연말에 내 삶이 완성되어야 합니다.

이 방향이라면 기존 WBS 엔진은 내부 기반으로 활용하되, 일반 사용자에게는 `WBS`나 `액션아이템`이라는 말조차 보여주지 않는 것이 좋습니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:119-130|note=[used existing date interaction version history and event data concepts to shape plan to life record behavior]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

근데 이게 기본 달력과 연동이 된다면?

---

## Codex (진행 응답)

그렇게 되면 완성도가 크게 올라갑니다. 달력의 약속은 ‘고정된 현실’, 앱의 계획은 ‘움직일 수 있는 목표’로 구분해서 같은 시간축에 겹쳐 보여주면 됩니다. 연동 범위와 중복 방지 방식이 핵심이라 현재 Apple 캘린더 접근 방식을 확인해보겠습니다.

---

## Codex

그렇게 되면 이 앱의 일상 사용 이유가 생깁니다. 단, 기본 달력을 대체하면 안 되고 **기본 달력 위에 계획을 얹는 앱**이어야 합니다.

> 캘린더는 이미 확정된 현실, 간트차트는 내가 만들고 싶은 미래.

### 같은 화면에서 구분

- **캘린더 일정**: 회색 또는 줄무늬, 고정 일정
  - 회의, 병원, 약속, 항공편, 기념일
- **나의 계획**: 파스텔 색상의 이동 가능한 막대
  - 운동, 공부, 여행 준비, 개인 프로젝트
- **중요 기록**: 사진·메모가 붙은 마일스톤

예를 들어 Apple 캘린더의 `14:00 병원`은 고정되어 있고, 사용자는 빈 시간에 `운동 1시간` 막대를 끌어다 놓습니다. 이게 일반 달력과 다른 핵심 경험입니다.

### 시간 단위별 표현

- **일별**: 캘린더 약속과 개인 계획을 시간 단위로 모두 표시
- **주별**: 약속 사이의 빈 공간에 계획 배치
- **월별**: 중요한 약속과 장기 계획만 표시
- **연도별**: 모든 회의를 보여주지 않고 여행·휴가·시험·프로젝트 등 큰 흐름만 집계

월·연도 화면에 모든 달력 일정을 표시하면 바로 무거워집니다. 화면을 축소할수록 사소한 일정은 자동으로 묶거나 숨겨야 합니다.

### 연동 방식

처음부터 완전한 양방향 동기화를 적용하기보다 단계적으로 가는 편이 안전합니다.

1. 사용자가 연결할 캘린더만 선택
2. 캘린더 일정을 앱에 읽어와 배경에 표시
3. 앱의 계획은 기본적으로 앱 안에만 저장
4. 필요한 계획만 `캘린더에 추가`
5. 사용자가 명시적으로 연결한 일정만 양방향 동기화

Apple EventKit은 일정 읽기·수정을 위한 전체 접근과 일정 생성만 가능한 쓰기 접근을 구분합니다. 시스템 일정 편집창을 이용하면 캘린더 전체 접근 권한 없이도 사용자가 직접 일정을 추가할 수 있습니다. [Apple EventKit 접근 방식](https://developer.apple.com/documentation/eventkit/accessing-the-event-store), [EventKitUI](https://developer.apple.com/documentation/eventkitui)

### 1년 회고에서의 역할

캘린더 연동 덕분에 사용자가 일기를 매일 쓰지 않아도 한 해의 기본 뼈대가 생깁니다.

- 언제 여행했는지
- 병원이나 운동 일정이 어떻게 이어졌는지
- 어떤 달이 약속으로 바빴는지
- 계획한 공부나 개인 프로젝트가 실제 생활 속에서 어떻게 진행됐는지
- 사진과 메모를 남긴 중요한 순간

다만 캘린더에 있다고 실제로 실행했다는 뜻은 아니므로 `완료`로 자동 처리하지 않고 **‘달력에 있었던 기록’**으로만 남기는 게 정확합니다.

### 가장 강한 사용 시나리오

> 이번 주 약속을 먼저 불러오고, 남은 시간에 내가 하고 싶은 일을 간트 막대로 배치한다. 시간이 지나면 그 계획과 약속이 모여 나의 한 달과 한 해가 된다.

이렇게 되면 일반 캘린더는 `약속 관리`, 할 일 앱은 `목록 관리`, 이 앱은 **`삶의 방향과 시간 배치 관리`**가 됩니다.

첫 버전에는 `선택한 Apple 캘린더 읽기 + 계획을 캘린더로 보내기`까지만 넣고, 기존 달력 일정을 앱에서 직접 수정하는 완전한 양방향 연동은 다음 단계로 두는 것이 적절합니다. 반복 일정, 시간대, 외부 삭제, 중복 생성 문제가 있어 초기에 넣으면 앱이 무거워질 가능성이 큽니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:119-130|note=[used existing event date preservation calendar and version behavior to define safe calendar integration]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

혹은 구글달력과 연동 된다면?

---

## Codex (진행 응답)

구글 캘린더까지 직접 연동하면 대상 사용자가 Apple 생태계 밖으로 크게 넓어집니다. 다만 iPhone에 등록된 Google 캘린더를 시스템 달력으로 읽는 방식과, Google 계정에 직접 로그인해 연동하는 방식은 제품 범위가 꽤 다릅니다. 두 경로를 비교해보겠습니다.

---

## Codex

구글 캘린더와 연동되면 **일반인 대상 확장성은 Apple 캘린더 단독보다 더 커집니다.** Windows·Android·웹 사용자까지 같은 일정을 활용할 수 있기 때문입니다.

다만 연동 방법은 두 가지입니다.

| 방식 | 장점 | 한계 |
|---|---|---|
| iPhone 시스템 달력을 통해 읽기 | 구현이 단순하고 iCloud·Google·Exchange를 한꺼번에 표시 | iPhone에 Google 계정이 미리 등록되어 있어야 하고 Windows 웹과 직접 연결되지 않음 |
| Google 계정 직접 연결 | iPhone·iPad·Windows·웹에서 동일하게 동기화 | Google 로그인, OAuth 검증, 동기화 서버 등 개발 범위 증가 |

iPhone 기본 달력은 Google 계정을 등록할 수 있으므로, EventKit으로 시스템 달력을 읽으면 등록된 Google 일정도 함께 가져올 수 있습니다. [Apple의 Google 캘린더 계정 지원](https://support.apple.com/en-ie/guide/iphone/iphc37be2016/ios)

### 사용자에게 보이는 경험

구글 일정은 고정된 약속으로 표시합니다.

- `10:00 팀 회의` — Google 캘린더, 고정
- `운동 1시간` — 앱의 유동 계획
- `자격증 공부` — 장기 계획에서 내려온 세부 계획

사용자는 Google 일정 사이의 빈 공간에 계획 막대를 배치합니다. 앱이 단순히 Google 캘린더를 다시 보여주는 것이 아니라, **이미 잡힌 약속 사이에 인생 계획을 배치해주는 역할**을 하는 겁니다.

### 동기화 원칙

- Google 일정은 기본적으로 읽기 전용
- 사용자가 선택한 계획만 `Google 캘린더에 추가`
- 연결된 일정만 양방향 수정
- 앱 계획과 Google 일정을 색상·아이콘으로 명확히 구분
- 시스템 달력과 Google 직접 연동을 동시에 사용하면 하나만 선택하도록 하여 중복 방지
- 반복 회의는 연도 화면에서 모두 표시하지 않고 월별 활동량으로 묶기

Google Calendar API는 최초 전체 동기화 후 `syncToken`을 이용해 변경된 일정만 가져올 수 있습니다. [Google Calendar 증분 동기화](https://developers.google.com/workspace/calendar/api/guides/sync)

### 직접 Google 연동이 무거워지는 지점

앱이 실행 중이 아닐 때도 변경 사항을 즉시 받으려면 Google이 알림을 보낼 HTTPS 서버가 필요합니다. [Google Calendar Push Notifications](https://developers.google.com/workspace/calendar/api/guides/push)

또한 공개 앱에서 캘린더 내용을 읽거나 수정하려면 Google OAuth 권한을 요청해야 하고, 필요한 범위에 따라 앱 검증 절차가 필요할 수 있습니다. Google도 최소한의 권한만 요청하도록 요구합니다. [Google Calendar 권한 범위](https://developers.google.com/workspace/calendar/api/auth)

### 권장 개발 순서

1. **1차:** iPhone 시스템 달력 연동  
   등록된 iCloud·Google·Exchange 일정을 함께 표시합니다.

2. **2차:** Google 계정 직접 연결  
   Windows 웹에서도 같은 계획을 볼 수 있게 합니다.

3. **3차:** 실시간 양방향 동기화  
   사용자가 명시적으로 연결한 계획에만 적용합니다.

초기에는 서버 없이 시스템 달력을 이용하면 가볍게 시작할 수 있습니다. 다만 장기적으로 Windows·Android까지 확장할 생각이라면 데이터 구조에는 처음부터 `외부 캘린더 종류·캘린더 ID·이벤트 ID`를 저장할 자리를 만들어야 합니다.

중요한 점은 **Google 연동 자체가 차별화는 아니라는 것**입니다. Google 캘린더는 약속을 보여주고, 우리 앱은 그 약속 사이에 장기 계획을 배치하고 나중에 1년의 삶으로 정리합니다.

> Google 캘린더는 내가 어디에 있었는지를 보여주고, 이 앱은 내가 무엇을 향해 살았는지를 보여준다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:119-130|note=[used existing date preservation calendar and version data rules to define external calendar synchronization]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

위젯을 만들어서 간트처트가 시간의 흐림에 따라 움직이는 거지. 그건 어때

---

## Codex (진행 응답)

아이디어는 좋습니다. 특히 앱을 열지 않아도 “지금 내 하루의 어디쯤인지” 보이기 때문에 이 제품의 대표 기능이 될 수 있습니다. 다만 일반 위젯은 초·분 단위로 자유롭게 계속 움직일 수 없어서, 홈 위젯과 Live Activity를 나눠 설계해야 합니다. 현재 Apple 제한을 확인해보겠습니다.

---

## Codex

좋습니다. 이건 단순 부가기능이 아니라 **앱을 대표하는 시각적 기능**이 될 수 있습니다.

> 일정표를 보는 것이 아니라, 시간 속에서 내 계획이 흘러가는 것을 본다.

### 가장 좋은 움직임 방식

빨간 현재 시점선은 위젯 가운데에 고정하고, 간트차트가 시간에 따라 왼쪽으로 흘러가게 합니다.

```text
지난 일정  ←   │ 지금 │   →  다음 일정
───────────────🔴──────────────
   회의        │ 운동 │    공부
```

이 방식은 현재 위치를 매번 찾을 필요가 없습니다.

- 지난 일정은 점점 흐려짐
- 현재 진행 중인 계획은 강조
- 다음 일정은 선명하게 표시
- 일정이 없는 구간은 `1시간 30분 여유`로 표시
- Google·Apple 캘린더 일정은 회색
- 사용자가 만든 계획은 파스텔 색상

### 위젯 구성

**작은 위젯**

- 현재 계획
- 남은 시간
- 다음 계획

**중간 위젯 — 대표 기능**

- 현재를 중심으로 전후 3~4시간 간트차트
- 캘린더 약속과 계획을 함께 표시
- 빨간 현재 시점선
- `했어요`, `미루기` 버튼

**큰 위젯**

- 오늘 하루 전체 간트
- 현재 시점선
- 중요 일정과 빈 시간 표시

**iPad 대형 위젯**

- 이번 주 간트
- 오늘 날짜 세로선
- 주간 장기 계획과 캘린더 일정

별도로 `나의 1년` 위젯을 만들 수도 있습니다. 1월부터 12월까지의 긴 막대 위에서 오늘 선이 하루씩 움직이고, 여행·시험·프로젝트 같은 큰 계획만 표시합니다.

### 실제 Apple 위젯의 제한

일반 위젯은 앱처럼 계속 실행되지는 않습니다. Apple은 자주 보는 위젯도 일반적으로 하루 약 40~70회, 대략 15~60분 간격의 갱신 예산을 부여하며 정확한 갱신 시각도 보장하지 않습니다. [WidgetKit 갱신 지침](https://developer.apple.com/documentation/widgetkit/keeping-a-widget-up-to-date)

따라서 간트 전체가 매분 부드럽게 움직이게 할 수는 없지만 다음처럼 구현할 수 있습니다.

- 차트 위치는 약 15~30분 단위로 이동
- 현재 계획의 남은 시간은 실시간 타이머로 표시
- 진행 막대는 `ProgressView(timerInterval:)`로 계속 움직임
- 앱을 열면 간트차트가 실제 시간에 맞춰 부드럽게 이동

WidgetKit은 위젯이 실행 중이지 않아도 타이머와 시간 기반 진행 막대를 화면에서 계속 갱신할 수 있습니다. [동적 시간 표시](https://developer.apple.com/documentation/widgetkit/displaying-dynamic-dates), [시간 기반 ProgressView](https://developer.apple.com/documentation/swiftui/progressview)

### Live Activity와 조합

사용자가 `오늘 계획 시작`을 누르면 Live Activity를 띄우는 것도 좋습니다.

- 잠금화면: 지금 하는 일과 남은 시간
- Dynamic Island: 현재 계획 진행 막대
- 완료·미루기 버튼
- 종료되면 다음 계획 안내

다만 Live Activity는 하루나 1년 전체가 아니라, 시작과 종료가 명확한 수시간짜리 활동에 적합하며 Apple도 8시간 이하 활동을 권장합니다. [Live Activities 지침](https://developer.apple.com/design/human-interface-guidelines/live-activities)

결론적으로 세 가지로 나누면 됩니다.

- **앱 내부**: 부드럽게 계속 흐르는 전체 간트
- **홈 위젯**: 15~30분 단위로 이동하는 오늘·이번 주 간트
- **Live Activity**: 현재 실행 중인 계획의 실시간 진행

이 기능은 “시간이 지나가는 느낌”을 직접 보여주기 때문에 일반 달력과 확실히 구분됩니다. 특히 **고정된 현재 선을 중심으로 내 계획과 실제 약속이 흘러가는 모습**은 앱의 로고나 이름보다 먼저 기억될 만한 차별점입니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:128-129|note=[used existing calendar and timeline item semantics to distinguish fixed events from moving plans]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

## 사용자

그러면 약간 간트차트 + 실시간 시간표 + 1년 계획 느낌이네

---

## Codex

맞습니다. 정확히는 이 세 가지가 연결된 형태입니다.

> **1년 간트계획 + 오늘의 실시간 시간표 + 지나간 삶의 기록**

흐름은 이렇게 됩니다.

`연간 목표 → 월간 단계 → 주간 계획 → 오늘 시간표 → 연간 회고`

핵심은 이것들이 별도 기능이 아니라 **하나의 계획을 시간 배율에 따라 다르게 보여주는 것**입니다.

- 연도 보기: 내 삶의 큰 방향
- 월·주 보기: 실행 계획 배치
- 일 보기: 캘린더 약속과 함께 실시간으로 흐르는 시간표
- 위젯: 지금 하는 일과 다음 일정
- 지난 시간: 완료·이동·건너뜀과 사진·메모가 기록으로 축적
- 연말: 실제로 살아온 1년의 타임라인

일반 사용자에게는 `간트차트`라는 용어를 전면에 내세우지 않는 게 좋습니다. 내부 방식은 간트지만, 제품 설명은 이렇게 가는 편이 자연스럽습니다.

> 올해 하고 싶은 일을 계획하면, 오늘의 시간표로 이어지고, 지나간 계획은 나의 1년이 됩니다.

화면도 복잡하게 나누기보다 `일 · 주 · 월 · 년` 버튼과 확대·축소만으로 이동하고, 모든 화면에 동일한 **빨간 현재 시점선**을 사용하면 제품 정체성이 생깁니다. 달력은 약속을 관리하지만, 이 앱은 **목표가 실제 시간 속에서 어떻게 흘러갔는지를 보여주는 도구**가 되는 겁니다.

---

## 사용자

비슷한 앱이 있어?

---

## Codex (진행 응답)

이번에는 기능을 따로 비교하지 않고, **간트 + 실시간 하루 시간표 + 연·월·주·일 계획 + 1년 회고 + 움직이는 위젯**이 한 제품에 같이 있는지를 기준으로 찾아보겠습니다.

---

## Codex

비슷한 구성요소를 가진 앱은 있지만, **말씀하신 조합을 하나로 묶은 앱은 현재 공식 기능 기준으로 찾지 못했습니다.** 가장 가까운 것은 `Timestripe`입니다.

| 앱 | 닮은 부분 | 빠진 부분 |
|---|---|---|
| **Timestripe** | 일·주·월·연·인생 계획, 시간의 흐름 표시, Google·Apple 캘린더 연동 | 간트차트와 실시간 하루 시간표가 아님 |
| **Structured** | 실시간 하루 타임라인, 주·월 보기, 캘린더, 위젯, Live Activity | 연간·인생 계획과 간트 구조가 없음 |
| **QuickPlanX** | 간트차트, WBS, 작업 연결, 현재 작업 위젯 | 일반인용 하루 시간표·인생 기록이 아님 |
| **AJournal** | 일·주·월 계획, 캘린더, 사진·손글씨 회고 | 간트와 실시간으로 흐르는 계획이 없음 |
| **Life Widget** | 일·주·월·연·인생의 시간 진행을 위젯으로 표시 | 일정과 계획을 배치할 수 없음 |

### 가장 가까운 앱: Timestripe

Timestripe는 `Day · Week · Month · Year · Life`로 목표를 연결하고, `Clock` 기능으로 시간의 흐름도 보여줍니다. Google·Apple 캘린더 연결도 지원합니다. [Timestripe 기능](https://timestripe.com/magazine/learning-center/features/)

하지만 현재 공식 FAQ상 Google 캘린더는 양방향 동기화가 아니며, 계획을 캘린더의 종일 일정으로 보내는 수준입니다. 간트 막대가 현재 시간에 따라 흐르는 구조도 아닙니다. [Timestripe 캘린더 FAQ](https://timestripe.com/magazine/learning-center/faq/)

### 하루 사용 경험이 가장 가까운 앱: Structured

Structured는 하루를 시간축으로 보여주고, 캘린더 일정과 할 일을 함께 배치하며, 위젯과 Live Activity도 지원합니다. 하지만 중심이 일상 루틴과 오늘의 할 일이라 연간 목표에서 오늘 계획으로 내려오는 구조는 없습니다. [Structured App Store](https://apps.apple.com/us/app/structured-daily-planner-todo/id1499198946)

### 간트 부분이 가장 가까운 앱: QuickPlanX

QuickPlanX는 iPhone·iPad·Mac에서 간트, WBS, 작업 연결과 위젯을 제공합니다. 그러나 전문 프로젝트 관리 도구에 가까우며 일별 실시간 시간표나 1년의 삶을 회고하는 구조는 아닙니다. [QuickPlanX App Store](https://apps.apple.com/ca/app/quickplanx-project-gantt-plan/id6502736030)

### 결론

각 시장은 이미 존재합니다.

- Timestripe: 장기 목표
- Structured: 오늘의 실행
- QuickPlanX: 간트 일정
- Journal/AJournal: 삶의 기록
- Life Widget: 시간의 흐름

우리가 생각한 것은 이들을 연결한 형태입니다.

> **연간 목표를 간트로 계획하고, 오늘의 실시간 시간표로 실행하며, 위젯에서 시간의 흐름을 보고, 지나간 계획은 나의 1년으로 남기는 앱**

따라서 완전히 새로운 개별 기능은 아니지만, **기능들의 연결 방식은 충분히 차별화될 수 있습니다.** 특히 `연간 계획 → 오늘 시간표 → 자동 회고`와 `시간에 따라 흐르는 간트 위젯`이 핵심입니다. 경쟁제품을 직접 비교한다면 우선 **Timestripe, Structured, QuickPlanX** 세 개를 사용해보는 것이 가장 정확합니다.

---

## 사용자

그러면 1년 계획. 분기별 계획. 월별. 주별. 하루 계획을 만들고 이게 취합되는 구조. 즉 하루가 모여서 1년을 만들고 1년 동안 내가 얼마나 잤는지. 운동을 얼마나 했는지 이런거 보면 좋겠네?

---

## Codex (진행 응답)

맞습니다. 이 구조의 핵심은 **계획은 위에서 아래로 내려오고, 실제 기록은 하루에서 1년으로 올라가는 것**입니다. 수면·운동처럼 자동으로 가져올 수 있는 항목과 사용자가 확인해야 하는 항목을 구분하면 부담 없이 만들 수 있습니다. Apple 건강 데이터 연동 범위도 확인해보겠습니다.

---

## Codex

맞습니다. 그러면 구조가 완전히 선명해집니다.

> **계획은 연도에서 하루로 내려오고, 실제 삶의 기록은 하루에서 연도로 올라간다.**

```text
연간 계획 → 분기 계획 → 월간 계획 → 주간 계획 → 하루 시간표
연간 회고 ← 분기 집계 ← 월간 집계 ← 주간 집계 ← 실제 기록
```

### 예시: 건강 계획

- **연간**: 건강한 생활 만들기
- **1분기**: 수면시간 안정화
- **1월**: 평균 7시간 수면
- **이번 주**: 12시 전에 잠들기
- **오늘**: 23:30 취침 계획

실제 수면시간은 사용자가 매일 입력하는 대신 Apple 건강 앱에서 가져옵니다. HealthKit은 수면 분석 데이터와 운동 데이터를 사용자 동의하에 읽을 수 있습니다. [Apple HealthKit](https://developer.apple.com/documentation/healthkit), [수면 데이터](https://developer.apple.com/documentation/healthkit/hkcategorytypeidentifier/sleepanalysis)

### 자동으로 취합할 수 있는 기록

**Apple 건강에서 가져오는 항목**

- 총 수면시간과 일평균
- 수면 시작·기상 시간
- 운동 횟수와 총 운동시간
- 걷기·달리기·자전거 등 운동 종류
- 걸음 수와 활동시간

**캘린더에서 가져오는 항목**

- 업무·회의 시간
- 병원·약속·여행일
- 가족·친구와 보낸 일정
- 휴가와 출장 기간

**앱 계획에서 집계하는 항목**

- 공부한 시간
- 개인 프로젝트에 사용한 시간
- 독서·취미·휴식
- 계획을 다음으로 옮긴 횟수
- 사진과 메모를 남긴 중요 순간

캘린더 일정은 실제 실행 여부를 보장하지 않으므로 `기록된 약속`, HealthKit 데이터는 `측정된 활동`, 사용자가 확인한 계획은 `실행한 계획`으로 구분해야 정확합니다.

### 1년 뒤에 보이는 결과

`나의 2026년` 화면에는 이런 내용이 나옵니다.

- 총 수면시간과 하루 평균 수면
- 월별 수면시간 변화
- 총 운동시간·운동일수·운동 종류
- 공부·업무·개인·가족·여행에 사용한 시간
- 가장 바빴던 달과 여유로웠던 달
- 연초에 세운 계획과 실제 생활의 차이
- 월별 중요 일정과 사진
- 다음 해로 이어갈 계획

단순히 `목표 달성률 72%`처럼 점수를 매기기보다, **내 시간이 실제로 어디에 사용됐는지** 보여주는 편이 좋습니다.

### 입력 부담을 줄이는 방식

사용자가 매일 해야 할 일은 최소화합니다.

- 캘린더·수면·운동은 자동 수집
- 앱 계획은 시간이 지나면 `했어요 / 이동 / 건너뜀`
- 중요한 날에만 사진이나 한 줄 메모
- 일요일에는 주간 요약
- 월말에는 월간 타임라인 확인

건강 데이터는 민감하므로 사용자가 `수면 기록 연결`, `운동 기록 연결`을 눌렀을 때 해당 데이터만 개별적으로 권한을 요청해야 합니다. Apple도 HealthKit 접근은 필요한 시점에 최소 범위로 요청하도록 권장합니다. [HealthKit 권한 지침](https://developer.apple.com/documentation/healthkit/authorizing-access-to-health-data), [개인정보 보호](https://developer.apple.com/documentation/healthkit/protecting-user-privacy)

이렇게 되면 앱의 핵심 가치는 단순한 일정 관리가 아닙니다.

> **올해 어떻게 살고 싶은지 계획하고, 실제로 어떻게 살았는지를 자동으로 보여주는 앱**

간트차트는 계획을 보여주는 뼈대이고, 캘린더와 건강 데이터는 실제 삶을 채우는 증거가 됩니다.

---

## 사용자

그러면 위젯에서 바로 수정되면 좋겠는걸

---

## Codex (진행 응답)

가능하고, 오히려 위젯의 핵심 가치가 됩니다. 다만 위젯 안에서 막대를 직접 드래그하거나 텍스트를 타이핑하는 수준은 제한되므로, 자주 쓰는 수정은 버튼으로 즉시 처리하고 정밀 편집만 앱의 해당 위치로 연결하는 구성이 적합합니다. 현재 WidgetKit의 가능한 동작 범위를 확인해보겠습니다.

---

## Codex

좋습니다. 그러면 위젯이 단순 표시판이 아니라 **오늘 계획의 리모컨**이 됩니다.

Apple 위젯은 App Intents를 통해 앱을 열지 않고도 버튼과 토글로 데이터를 수정할 수 있습니다. 수정 후 위젯 타임라인 갱신도 보장됩니다. [Apple 대화형 위젯](https://developer.apple.com/documentation/widgetkit/adding-interactivity-to-widgets-and-live-activities)

### 위젯에서 바로 가능한 수정

```text
09시          12시          15시
[ 회의 ] [ 운동    ] [ 영어 공부 ]
              ▲ 지금

[✓ 했어요]  [+30분]  [다음 빈 시간]
```

- `했어요`: 현재 계획 완료
- `+15분 / +30분`: 종료시간 연장
- `30분 미루기`: 시작·종료시간 함께 이동
- `다음 빈 시간`: 캘린더의 다음 빈 공간으로 자동 이동
- `건너뜀`: 오늘 기록에는 남기고 일정 종료
- `시작`: 계획보다 일찍 활동 시작
- `잠시 멈춤`: 현재 활동 중지
- 수면·운동 기록 연결 켜기/끄기

버튼을 누르면 간트 막대 위치와 연·분기·월·주 집계까지 바로 갱신됩니다.

### 위젯에서 어려운 수정

일반 위젯은 다음 동작을 지원하지 않습니다.

- 간트 막대 직접 드래그
- 막대 길이를 손가락으로 조정
- 자유로운 스크롤
- 텍스트 키보드 입력
- 복잡한 팝업 편집창

Apple도 위젯은 스크롤 목록이나 텍스트 입력 같은 복잡한 상호작용을 일반적으로 지원하지 않는다고 명시합니다. [WidgetKit 제한](https://developer.apple.com/documentation/widgetkit/creating-a-widget-extension)

따라서 막대를 누르면 앱이 열리면서 **해당 날짜·해당 계획의 편집 위치로 곧바로 이동**하게 해야 합니다. 앱 홈부터 다시 탐색하게 하면 안 됩니다.

### 추천 위젯별 기능

- **작은 위젯**: 현재 계획, 남은 시간, 완료 버튼
- **중간 위젯**: 전후 일정 간트, 완료·미루기·연장
- **큰 위젯**: 하루 전체 간트, 계획별 완료 버튼
- **iPad 대형 위젯**: 이번 주 간트와 오늘 계획 수정
- **Live Activity**: 현재 활동의 완료·중지·연장

위젯의 버튼이 너무 많으면 간트가 보이지 않으므로 사용자가 자주 쓰는 버튼 2~3개를 선택하게 하면 좋습니다. Apple 역시 위젯에서는 단순하고 관련성 높은 동작만 제공하도록 권장합니다. [Apple 위젯 디자인 지침](https://developer.apple.com/design/human-interface-guidelines/widgets)

가장 적절한 기본 조합은 다음입니다.

`했어요 | 30분 미루기 | 다음 빈 시간`

Google 캘린더와 연결된 일정을 수정할 때는 먼저 위젯에서 막대를 이동시키고, 동기화가 실패하면 원래 상태로 되돌리며 `동기화 실패`를 표시해야 합니다. 앱 내부 계획은 즉시 수정하고 외부 캘린더 일정은 동기화 결과까지 확인하는 구조입니다.

결론적으로 **위젯에서 빠른 일정 수정은 반드시 넣는 게 좋습니다.** 다만 전체 편집기를 위젯에 넣는 것이 아니라, 하루에 반복적으로 쓰는 세 가지 행동만 극도로 빠르게 만드는 게 핵심입니다.

---

## 사용자

그리고 데이터는 로컬이 아니라 아이클라우드에 모으는 거야

---

## Codex (진행 응답)

그렇게 하면 기기별 복사본이 아니라 **사용자의 iCloud 개인 영역이 원본이고, iPhone·iPad·Mac은 동기화된 화면**이 됩니다. 다만 계획·캘린더 연결정보와 HealthKit에서 가져온 수면·운동 원본은 Apple 정책상 저장 범위를 다르게 잡아야 하므로 그 경계를 확인하겠습니다.

---

## Codex

좋습니다. 정확히는 **iCloud가 원본이고 로컬은 임시 캐시만 사용하는 구조**가 적합합니다.

앱과 위젯이 인터넷이 없을 때도 작동하려면 로컬 캐시 자체는 필요하지만, 기기를 잃거나 앱을 재설치해도 iCloud에서 전부 복원되어야 합니다.

| 데이터 | 저장 위치 |
|---|---|
| 연·분기·월·주·일 계획 | iCloud CloudKit 개인 데이터베이스 |
| 계획 간 연결과 분류 | iCloud |
| 완료·이동·건너뜀 기록 | iCloud |
| 메모·회고·사진 | iCloud |
| 버전 이력 | iCloud |
| Apple·Google 캘린더 원본 | 각 캘린더 서비스 |
| 캘린더와 계획의 연결 ID | iCloud |
| Google 로그인 토큰 | 기기 Keychain |
| 위젯에 표시할 현재·다음 계획 | 로컬 임시 캐시 |
| 수면·운동 측정값 | HealthKit |

### 기본 동기화 구조

```text
                 iCloud CloudKit
                       │
       ┌───────────────┼───────────────┐
       │               │               │
    iPhone           iPad            Mac
       │               │
     위젯          대형 편집 화면
```

한 기기에서 수정하면 CloudKit을 통해 다른 기기로 전달됩니다. SwiftData는 CloudKit과 연결해 사용자 기기 간 데이터를 자동으로 동기화할 수 있습니다. [Apple SwiftData·CloudKit 동기화](https://developer.apple.com/documentation/swiftdata/syncing-model-data-across-a-persons-devices)

CloudKit 개인 데이터베이스는 사용자의 iCloud 계정에 속하며 계정 기반 암호화를 지원합니다. [CloudKit 데이터 암호화](https://developer.apple.com/documentation/cloudkit/encrypting-user-data)

### 위젯에서 수정할 때

```text
위젯에서 ‘30분 미루기’
        ↓
위젯 화면 즉시 변경
        ↓
iCloud에 변경 저장
        ↓
iPad·Mac에도 반영
        ↓
연결된 Google·Apple 캘린더 갱신
```

인터넷이 없으면 로컬 동기화 대기열에 잠시 보관했다가 연결되는 순간 iCloud로 올립니다. 따라서 로컬 데이터는 원본이 아니라 **아직 전송되지 않은 변경사항과 화면 표시용 캐시**입니다.

### HealthKit 데이터는 예외

여기에는 중요한 Apple 정책 제한이 있습니다. Apple은 HealthKit에서 얻은 개인 건강정보를 iCloud에 별도로 저장하는 것을 금지합니다. [App Review Guidelines 5.1.3](https://developer.apple.com/app-store/review/guidelines/)

따라서 다음처럼 처리해야 합니다.

- `매일 운동하기`라는 계획 → iCloud 저장
- 사용자가 누른 `운동 완료` → 일반 계획 실행 기록으로 iCloud 저장
- Apple Watch가 측정한 운동시간·심박수 → HealthKit에만 유지
- 실제 수면 단계와 수면시간 원본 → HealthKit에만 유지
- 연간 화면을 열 때 HealthKit에서 데이터를 읽어 기기에서 계산
- HealthKit 수치와 이를 기반으로 만든 건강 통계는 안전하게 별도 검토

즉, 앱의 계획과 회고는 iCloud에 모이고, 건강 측정 원본은 Apple 건강 앱이 관리합니다.

### 권장 기술 구성

첫 버전은 다음 구성이 가장 단순합니다.

- `SwiftData + CloudKit Private Database`
- 위젯과 앱은 `App Group` 캐시 공유
- 사진은 CloudKit Asset
- 캘린더 일정은 원본을 복사하지 않고 연결 ID만 저장
- HealthKit 데이터는 조회만 하고 CloudKit에 복제하지 않음
- 기기별 수정 충돌을 대비해 변경 시각과 버전 이력 저장

이 구조라면 별도의 회원가입이나 자체 서버 없이도 사용할 수 있고, 사용자의 데이터는 개인 iCloud에 남습니다.

다만 장기적으로 Android까지 지원하려면 iCloud 전용 구조가 제약이 됩니다. **iPhone·iPad·Mac을 대상으로 시작한다면 iCloud 중심이 가장 적절하고**, 내부 데이터 계층만 나중에 다른 동기화 서비스로 교체할 수 있도록 분리해두면 됩니다.

---

## 사용자

그러면 이거 여행 계획 앱으로도 가능하겠는데

---

## Codex (진행 응답)

맞습니다. 여행은 이 구조가 가장 자연스럽게 이해되는 대표 시나리오입니다. 다만 여행 전용 앱으로 방향을 좁히기보다, 같은 계획 엔진 위에 **‘여행 템플릿’**을 제공하는 편이 전체 모수와 차별화를 함께 살릴 수 있습니다. 기존 여행 앱과 겹치는 범위도 확인해보겠습니다.

---

## Codex

맞습니다. 여행은 이 앱의 가치를 일반인이 가장 쉽게 이해할 수 있는 대표 시나리오입니다.

다만 앱 전체를 여행 전용으로 좁히기보다는, **‘인생 타임라인 앱 + 여행 템플릿’**으로 가는 편이 좋습니다.

예를 들면:

- 연간 계획: 8월 일본 여행
- 분기 계획: 항공권·숙소 예약, 여권 갱신
- 월간 계획: 관광지·식당 예약, 예산, 준비물
- 주간 계획: 환전, 짐 싸기, 체크인
- 일간 계획: 비행 → 이동 → 관광 → 식사 → 숙소
- 여행 중 위젯: 지금 일정, 다음 장소, 지연 시 일정 밀기
- 여행 후: 이동 경로·사진·걸음 수·기록을 자동으로 모아 회고 생성

기존 앱 중 Wanderlog는 지도·예약·경로 최적화·공동 편집에 강하고, Polarsteps는 이동 경로 추적과 여행 기록·포토북에 강합니다. [Wanderlog](https://wanderlog.com/en), [Polarsteps](https://www.polarsteps.com/)

우리가 차별화할 지점은 지도나 항공권 예약이 아니라 다음입니다.

> 여행을 별도로 관리하는 것이 아니라, 여행을 준비한 과정부터 실제 여행과 추억까지 내 1년의 타임라인 안에 남긴다.

첫 버전에는 이 정도가 적당합니다.

- 여행 템플릿
- 일정과 캘린더 연동
- 국가 간 시간대 자동 처리
- 항공권·숙소·입장권 첨부
- 오프라인 일정 확인
- 현재·다음 일정을 보여주는 위젯
- 일정이 늦어졌을 때 뒤 일정을 한꺼번에 미는 기능
- 여행 종료 후 사진과 실제 이동 기록으로 여행 회고 생성

특히 지금 WBS에서 만든 **뒤 일정 함께 밀기, 연결선, 시간 플레이헤드, 연·월·주·일 확대/축소**가 여행 일정에서 상당히 강력하게 작동합니다. 여행 템플릿은 이 앱의 주 기능이면서 광고에서 보여줄 대표 화면으로도 좋겠습니다.

---

## 사용자

그러면 뭔가 템플릿이 있으면 좋겠네

---

## Codex

맞습니다. 처음부터 빈 화면을 보여주면 일반인은 간트차트에서 무엇을 써야 할지 막막합니다. **템플릿을 고르면 기본 뼈대가 생성되고, 사용자는 날짜와 이름만 바꾸는 방식**이 좋습니다.

초기에는 템플릿을 너무 늘리지 말고 다음 정도가 적당합니다.

- 여행 계획
- 연간 목표
- 시험·공부 계획
- 운동·건강 계획
- 이사·결혼 같은 큰 일정
- 빈 계획

예를 들어 `여행 계획`을 선택하면 자동으로 다음 항목이 생깁니다.

1. 예약: 항공권, 숙소, 입장권
2. 준비: 여권, 환전, 보험, 준비물
3. 이동: 출발, 비행, 현지 교통
4. 일정: 관광, 식사, 휴식
5. 여행 후: 사진 정리, 비용 정산, 회고

템플릿에는 단순히 항목명만 넣는 것이 아니라 다음 설정까지 함께 담으면 좋습니다.

- 기본 항목과 색상
- 연·월·주·일 중 시작 화면
- 추천 일정 간격
- 캘린더 연동 여부
- 위젯 표시 방식
- 알림 시점
- 일정이 밀릴 때 뒤 일정도 같이 이동할지
- 완료 후 자동 회고 항목

사용자가 만든 계획도 **‘내 템플릿으로 저장’**할 수 있어야 합니다. 그러면 매년 반복하는 여행, 건강검진, 명절 준비, 업무 프로젝트 등을 다시 사용할 수 있습니다.

핵심 화면은 이렇게 단순하게 가면 됩니다.

> 새 계획 → 템플릿 선택 → 날짜 지정 → 바로 타임라인 생성

그리고 템플릿은 서로 분리된 기능이 아니라, 지금 만든 동일한 일정 엔진에 **초기 항목과 설정만 다르게 적용하는 구조**가 가장 가볍고 유지보수도 쉽습니다. 여행 템플릿부터 완성해서 검증한 다음 연간 목표·공부·운동으로 확대하는 순서가 좋겠습니다.

---

## 사용자

운동이나 다이어트 프로젝트 등 그러면 내 분류별로 보기 편집하기 이런거 되겠네. 일히고 생활을 분리하되 필요할 땐 한 번에 보기

---

## Codex

맞습니다. 이때 핵심 개념은 단순한 캘린더가 아니라 **‘영역별 타임라인’**입니다.

구조는 다음처럼 잡으면 깔끔합니다.

> 영역 → 프로젝트 → 항목 → 일정

예시:

- 일
  - 신제품 개발
  - 보고서 작성
- 생활
  - 가족 일정
  - 집안일
- 건강
  - 다이어트 프로젝트
  - 러닝 훈련
- 여행
  - 여름 일본 여행
- 개인 성장
  - 영어 공부
  - 자격증 준비

보기 방식은 두 가지입니다.

- 분류별 보기: 건강만, 업무만, 여행만 선택해서 집중
- 통합 보기: 선택한 모든 영역을 한 타임라인에 겹쳐서 확인

통합 보기에서는 이런 사실을 바로 발견할 수 있습니다.

- 업무 마감과 여행 준비가 겹침
- 야근이 많은 주에 운동 계획이 과도함
- 휴가 기간에 업무 일정이 잘못 들어가 있음
- 시험 직전에 가족 행사가 있음

편집도 어떤 화면에서든 가능해야 합니다. 예를 들어 `건강 보기`에서 운동 일정을 옮기면 통합 보기와 위젯에도 즉시 반영되는 구조입니다.

상단에는 복잡한 메뉴 대신 컬러 칩을 두면 편합니다.

`전체` `일` `생활` `건강` `여행` `+`

여러 개를 동시에 선택할 수도 있고, 자주 사용하는 조합은 저장할 수 있습니다.

- 업무 집중
- 퇴근 후
- 이번 주 전체
- 가족과 공유
- 휴가 준비

특히 **일과 생활은 기본적으로 분리하되, 일정 충돌을 확인할 때만 통합해서 본다**는 것이 기존 업무용 간트차트와 다른 중요한 장점입니다. 공유도 영역 전체가 아니라 프로젝트 단위로 제한하면 회사 일정과 개인 일정이 섞여 노출되는 문제를 막을 수 있습니다.

---

## 사용자

이런 앱이 있어?

---

## Codex (진행 응답)

비슷한 요소를 가진 앱과, 지금 구상처럼 모두 결합한 앱이 실제로 있는지를 나눠서 확인해보겠습니다. 특히 영역별 보기·통합 타임라인·연→일 계획·캘린더 연동을 기준으로 비교하겠습니다.

---

## Codex

비슷한 앱은 있지만, 지금 구상한 조합을 그대로 제공하는 앱은 찾기 어렵습니다. 가장 가까운 앱은 **Griply**입니다.

| 앱 | 비슷한 점 | 빠진 부분 |
|---|---|---|
| [Griply](https://griply.app/) | 생활 영역 → 목표 → 하위 목표 → 할 일·습관, 영역별 분류와 주간 통합 계획 | 직관적인 간트차트, 연간 전체 타임라인, 실시간 플레이헤드가 약함 |
| [Structured](https://structured.app/) | 캘린더와 할 일을 하루 타임라인으로 통합하고 위젯 제공 | 장기 프로젝트·연간 계획·영역별 간트 관리가 약함 |
| [Notion Calendar](https://www.notion.com/en-gb/product/calendar) | 업무·개인 캘린더와 프로젝트 일정을 한 화면에 표시 | 장기 계획을 일간 일정으로 내려보내는 구조가 아님 |
| [Tilly](https://www.trytilly.com/) | 업무·개인·가족 캘린더를 분리하거나 통합 타임라인으로 확인 | 목표·프로젝트·간트차트 구조가 없음 |
| [Wanderlog](https://wanderlog.com/en) | 여행 템플릿에 가까운 일정·예약·지도 관리 | 여행 전용이고 삶 전체의 연간 계획과 연결되지 않음 |

정리하면 기존 앱은 보통 한쪽에 치우쳐 있습니다.

- 목표 관리 앱은 장기 목표는 잘 보여주지만 시간표가 약함
- 캘린더 앱은 하루 일정은 잘 보여주지만 1년의 방향이 안 보임
- 간트차트 앱은 프로젝트 관리는 강하지만 일반인이 사용하기 무거움
- 여행 앱은 여행이 끝나면 다른 생활 계획과 단절됨

따라서 지금 구상의 차별점은 분명합니다.

> 일·생활·건강·여행을 각각 관리하면서 필요할 때 하나의 시간축으로 보고, 연간 계획부터 지금 이 시간까지 자연스럽게 확대하는 개인용 간트 플래너

특히 **가로로 흐르는 간트차트 + 현재 시간선 + 영역 필터 + 템플릿 + 일정이 밀리면 뒤 일정도 함께 이동 + 연말 회고** 조합이 핵심입니다. 개별 기능은 이미 존재하지만, 이를 일반인용으로 가볍게 결합한 것이 차별화 지점입니다.

---

## 사용자

이거 안드리오드랑 동시개발되갰내

---

## Codex (진행 응답)

가능합니다. 다만 안드로이드 동시 개발을 결정하면 앞서 정한 **‘iCloud에만 저장’**이 가장 큰 설계 충돌입니다. 화면 공유 범위와 플랫폼별 위젯·캘린더 연동까지 포함해 현실적인 개발 구조를 확인해보겠습니다.

---

## Codex

네, **iOS·Android 동시 개발이 가능합니다.** 타임라인·템플릿·분류·드래그 일정 조정 같은 핵심 기능은 하나의 코드로 공유할 수 있습니다.

가장 현실적인 구성은 다음입니다.

- 공통 앱: Flutter
  - 간트 타임라인
  - 연·분기·월·주·일 보기
  - 일·생활·건강·여행 영역 필터
  - 템플릿과 통계
  - 드래그 및 뒤 일정 함께 밀기
- iOS 전용 연결
  - Apple 캘린더, HealthKit
  - WidgetKit, Live Activity
- Android 전용 연결
  - Google·기기 캘린더
  - Health Connect
  - Android 위젯

Flutter는 iOS·Android뿐 아니라 Windows도 지원하므로, 나중에 PC 편집 화면까지 같은 기반으로 확장할 수 있습니다. 플랫폼 고유 기능은 Swift와 Kotlin을 연결해서 구현할 수 있습니다. [Flutter 지원 플랫폼](https://docs.flutter.dev/reference/supported-platforms), [플랫폼별 기능 연결](https://docs.flutter.dev/platform-integration/platform-channels)

다만 **iCloud 단독 저장은 변경해야 합니다.** CloudKit은 다른 플랫폼에서도 웹 API로 접근할 수 있지만, Android에서는 브라우저를 통한 Apple 계정 인증이 필요해 자연스러운 사용 경험이 어렵습니다. [Apple CloudKit 외부 플랫폼 접근](https://developer.apple.com/documentation/CloudKit/obtaining-an-api-token-for-an-icloud-container)

따라서 권장 구조는:

> 공용 암호화 클라우드에 원본 저장 + 기기에는 오프라인 캐시 + iCloud는 Apple 기기의 선택적 백업

이렇게 하면 아이폰에서 만든 여행 계획을 Android 가족과 공유하고, Windows에서 편집한 내용이 양쪽 위젯에 반영될 수 있습니다.

위젯은 화면 코드를 완전히 공유할 수 없어서 각각 제작해야 합니다. iOS는 WidgetKit의 버튼·토글 상호작용을, Android는 Glance 위젯을 사용합니다. [iOS 인터랙티브 위젯](https://developer.apple.com/documentation/widgetkit/adding-interactivity-to-widgets-and-live-activities), [Android Glance 위젯](https://developer.android.com/develop/ui/compose/glance)

결론적으로 **앱 본체는 약 80% 공유하고 캘린더·건강 데이터·위젯만 플랫폼별로 만드는 방향**이 좋습니다. 출시일을 억지로 완전히 맞추기보다는 같은 코드로 개발하면서 iOS 베타를 먼저 검증하고, 바로 Android 베타를 이어서 내는 편이 안정적입니다.

---

## 사용자

Ios전용으로 갈까

---

## Codex

제 추천은 **iOS 전용으로 먼저 가는 것**입니다. 정확히는 영구적인 iOS 독점보다 **iOS 우선 출시**가 좋습니다.

이 앱의 핵심 차별점이 Apple 생태계와 잘 맞습니다.

- iCloud 자동 동기화
- Apple 캘린더 연동
- HealthKit의 수면·운동·걸음 데이터
- 홈 화면·잠금 화면 위젯
- Live Activity로 현재 일정 표시
- iPhone에서 확인하고 iPad에서 넓게 편집
- 향후 Apple Watch에서 일정 완료 처리

개발 구조도 단순해집니다.

- 화면: SwiftUI
- 간트차트: SwiftUI Canvas
- 저장: SwiftData + CloudKit
- 캘린더: EventKit
- 건강 데이터: HealthKit
- 위젯: WidgetKit
- 현재 일정: ActivityKit

특히 간트차트의 드래그, 확대·축소, Apple Pencil 편집은 iPad 네이티브로 만들었을 때 품질이 훨씬 좋습니다.

첫 버전 범위는 다음이 적당합니다.

1. 일·생활·건강·여행 영역
2. 빈 계획과 기본 템플릿
3. 연·분기·월·주·일 타임라인
4. Apple 캘린더 가져오기
5. iCloud 동기화
6. 현재 일정과 다음 일정을 보여주는 위젯
7. 일정 완료·미루기
8. 연간 회고

위젯에서는 `완료`, `30분 미루기`, `다음 일정` 정도를 바로 처리하고, 정밀한 드래그 편집은 앱을 열도록 하는 것이 현실적입니다.

다만 나중에 Android를 만들 가능성에 대비해 일정 데이터 자체는 Apple 전용 형식에 묶지 않는 것이 좋습니다.

> 영역 → 프로젝트 → 항목 → 일정 → 실제 기록

이 공통 구조와 고유 ID, 시간대 규칙을 처음부터 독립적으로 설계하면 나중에 Android와 공용 서버를 추가하기 쉽습니다.

결론은 **iPhone·iPad용으로 작고 완성도 높게 시작하고, 반응이 확인되면 Android로 확장**하는 방향이 가장 안전합니다. 이 제품은 넓게 시작하는 것보다 Apple 기기에서 위젯과 타임라인 경험을 제대로 만드는 것이 먼저입니다.

---

## 사용자

애플워치까지 연동하면?

---

## Codex (진행 응답)

애플워치가 들어가면 이 앱의 구조가 더 명확해집니다. 넓은 계획은 iPad·iPhone에서 만들고, Watch는 **지금 실행하고 실제 결과를 기록하는 기기**로 역할을 분리하는 방향이 좋습니다. 가능한 상호작용 범위도 확인해보겠습니다.

---

## Codex

애플워치까지 연동하면 차별점이 훨씬 강해집니다. 앱이 단순한 계획표에서 **‘계획과 실제 생활을 연결하는 앱’**으로 바뀝니다.

역할은 이렇게 나누는 것이 좋습니다.

- iPad: 연간·월간 계획과 프로젝트 편집
- iPhone: 주간·일간 계획과 일정 조정
- Apple Watch: 지금 실행, 완료, 실제 기록

Apple Watch의 기본 화면은 간트차트가 아니라 다음 정보가 적합합니다.

- 지금 할 일과 남은 시간
- 다음 일정과 시작까지 남은 시간
- 오늘 전체 일정 진행 위치
- `시작`, `완료`, `10분 연기`, `건너뛰기`
- 일정 종료 시 다음 일정 시작 확인
- 지연된 경우 이후 일정 함께 밀기
- 손목 진동으로 시작·종료 알림

예를 들어 운동 프로젝트라면:

> 19:00 러닝 40분 → Watch에서 시작 → 심박수·거리·운동시간 기록 → 종료 → 실제 수행 결과가 연간 건강 타임라인에 반영

Apple의 WorkoutKit은 예약한 운동을 Apple Watch 운동 앱과 동기화할 수 있고, HealthKit 운동 세션은 운동 중 심박수 등의 데이터를 기록할 수 있습니다. [WorkoutKit](https://developer.apple.com/documentation/workoutkit), [HealthKit 운동 세션](https://developer.apple.com/documentation/HealthKit/running-workout-sessions)

일반 일정도 Watch에서 실제 시간을 기록할 수 있습니다.

- 계획: 영어 공부 21:00~22:00
- 실제: 21:18 시작, 21:53 종료
- 연말 통계: 총 계획 120시간 / 실제 78시간

수면·운동·걸음은 HealthKit에서 자동으로 가져오고, 공부·업무·여행 같은 활동은 Watch에서 시작·종료 버튼으로 기록하면 됩니다. 이렇게 모인 실제 기록으로 “올해 나는 무엇에 시간을 사용했는가”를 보여줄 수 있습니다.

또한 iPhone에서 실행한 현재 일정 Live Activity는 연결된 Apple Watch의 Smart Stack 상단에도 표시할 수 있습니다. 시간·장소·운동 상황에 맞춰 관련 위젯을 우선 표시하는 것도 가능합니다. [Apple Watch Smart Stack과 Live Activity](https://developer.apple.com/watchos/), [상황별 위젯 표시](https://developer.apple.com/documentation/widgetkit/widget-suggestions-in-smart-stacks)

Apple Watch 1차 버전은 가볍게 가는 것이 좋습니다.

1. 현재·다음 일정 표시
2. 시작·완료·미루기
3. 오늘 진행 위치 컴플리케이션
4. 운동 계획과 실제 HealthKit 연동
5. 하루 종료 요약

전체 간트 편집이나 복잡한 템플릿 설정은 Watch에 넣지 않습니다. 최종적으로 이 앱의 설명도 훨씬 선명해집니다.

> 계획은 큰 화면에서 만들고, 실행은 손목에서 기록한다. 하루의 실제 기록이 모여 나의 1년이 된다.

---

## 사용자

지도랑 위치 연동까지 하면 어때

---

## Codex (진행 응답)

지도·위치까지 연결하면 여행뿐 아니라 운동·생활 기록의 자동화가 가능해집니다. 다만 항상 위치를 추적하는 앱이 되면 배터리와 개인정보 부담이 커지므로, **계획한 일정 주변에서만 작동하는 방식**이 적합합니다. Apple이 허용하는 위치 실행 범위도 확인해보겠습니다.

---

## Codex

좋습니다. 지도·위치가 붙으면 이 앱은 **‘언제 무엇을 할지’뿐 아니라 ‘어디에서 실행할지’까지 계산하는 생활 타임라인**이 됩니다.

가장 강력한 기능은 단순한 지도 표시보다 **이동시간 자동 반영**입니다.

예를 들어:

- 업무 종료: 회사 18:00
- 헬스장 운동: 18:20
- 실제 이동시간: 35분
- 앱 표시: `15분 충돌 — 운동 시작시간 조정 필요`

일정 사이에 `이동 35분` 블록을 자동으로 만들고, 앞 일정이 늦어지면 이동시간과 뒤 일정까지 함께 미는 구조입니다. 여행 일정에서도 관광지 간 이동시간 때문에 불가능한 계획을 미리 발견할 수 있습니다.

지도·위치는 세 단계로 적용하는 것이 좋습니다.

1. 장소 기반 계획

- 일정에 장소 검색·등록
- 여행 장소를 지도 핀과 이동 경로로 표시
- 자동차·대중교통·도보 예상시간 계산
- Watch에서 다음 장소와 출발시간 표시
- `길찾기`를 누르면 Apple 지도 실행

MapKit으로 지도, 장소 검색, 핀, 경로와 예상 이동시간을 앱 안에 표시할 수 있습니다. [MapKit](https://developer.apple.com/documentation/mapkit/mapkit-for-swiftui)

2. 도착·출발 자동 확인

- 회사 도착 → 업무 일정 시작 제안
- 헬스장 도착 → 운동 시작 제안
- 공항 도착 → 체크인 일정 표시
- 장소에서 떠남 → 실제 종료시간 기록 제안

Core Location의 지오펜스를 사용하면 앱이 닫혀 있어도 특정 장소의 진입·이탈을 감지할 수 있습니다. 다만 동시에 감시할 수 있는 조건이 제한되므로 가까운 일정 위주로 자동 교체해야 합니다. [Apple 위치 조건 감지](https://developer.apple.com/documentation/corelocation/monitoring-the-user-s-proximity-to-geographic-regions)

자동 기록보다는 Watch에서 확인받는 것이 안전합니다.

> 헬스장에 도착했습니다. 운동을 시작할까요?  
> `[시작]` `[오늘 건너뛰기]`

3. 실제 이동 기록

여행이나 러닝처럼 사용자가 명시적으로 `기록 시작`을 누른 경우에만 실제 경로를 저장합니다.

- 여행에서 방문한 장소와 체류시간
- 실제 이동 경로
- 걸음 수와 이동 거리
- 사진 촬영 장소
- 계획한 일정과 실제 여행 비교

항상 GPS를 켜두는 방식은 피해야 합니다. Apple도 실시간 경로나 운동처럼 꼭 필요한 경우에만 백그라운드 위치를 사용하도록 안내하며, 지속적인 고정밀 위치 확인은 배터리 소비가 큽니다. [효율적인 위치 사용](https://developer.apple.com/documentation/xcode/accessing-the-device-s-location-efficiently)

추천 화면 구성은 다음과 같습니다.

- iPhone: `타임라인 | 지도` 전환
- iPad: 왼쪽 타임라인 + 오른쪽 지도
- Watch: 현재 일정, 다음 장소, 출발까지 남은 시간
- 위젯: `다음 장소까지 35분 · 17:25 출발`

개인정보 보호도 제품 특징으로 내세울 수 있습니다.

- 위치 기능은 프로젝트별 선택
- 정확한 좌표 대신 `회사·헬스장·집`과 체류시간만 저장 가능
- 여행 경로 기록은 명시적으로 시작·종료
- 데이터는 개인 iCloud 영역에 저장
- 위치 기록 전체 삭제 기능 제공

첫 버전에는 **장소 등록, 이동시간 계산, Apple 지도 길찾기 연결**까지만 넣고, 이후 도착·출발 감지와 여행 경로 기록을 추가하는 것이 좋습니다. 특히 이동시간을 일정의 일부로 취급하는 기능은 일반 캘린더와 확실히 구별되는 장점입니다.

---

## 사용자

그리고 애플워치릉 통해 간트차트에 자동 추가 기능은?

---

## Codex (진행 응답)

가능합니다. 이 기능은 오히려 Apple Watch 연동의 핵심이 될 수 있습니다. 다만 Watch가 모든 행동을 임의로 추측하는 방식보다, **한 번의 시작 동작 이후 간트 막대가 자동으로 자라는 방식**이 정확하고 가볍습니다. 자동 감지할 수 있는 범위도 구분해서 정리하겠습니다.

---

## Codex

가능합니다. 그리고 이것이 Apple Watch 연동의 핵심 기능이 될 수 있습니다. **Watch에서 활동을 시작하면 간트차트에 막대가 실시간으로 생성되고 시간이 흐르는 만큼 자동으로 길어지는 방식**입니다.

예를 들어 Watch에서:

> `[운동 시작]` → 19:07 시작 → 운동 중 막대 자동 증가 → 19:54 종료

그러면 iPhone·iPad 간트차트에는 다음처럼 기록됩니다.

- 계획: 러닝 19:00~19:40
- 실제: 러닝 19:07~19:54
- 차이: 7분 늦게 시작, 7분 초과

실시간으로 매분 데이터를 전송할 필요는 없습니다. 시작 시각만 기록하고 화면에서는 현재 시각까지 막대를 늘려서 보여주며, 종료할 때 종료 시각을 확정하면 됩니다.

Watch에서 간트차트에 추가하는 방법은 세 가지가 좋습니다.

1. 바로 시작

컴플리케이션이나 Smart Stack에서 `+`를 누른 뒤 최근 활동을 선택합니다.

- 업무
- 공부
- 운동
- 이동
- 휴식
- 직접 입력

선택 즉시 현재 시각부터 간트 막대가 생성됩니다.

2. 음성으로 추가

- “지금부터 영어 공부 시작”
- “내일 오후 7시에 러닝 40분 추가”
- “여행 준비 30분 시작”

App Intents를 만들면 Siri와 단축어에서 앱의 일정 추가·시작 기능을 실행할 수 있습니다. [Apple App Intents](https://developer.apple.com/documentation/AppIntents/app-intents)

3. 자동 감지 후 추가

- Apple Watch 운동이 저장됨 → 건강 영역에 운동 막대 생성
- 헬스장 도착·출발 → 운동 기록 후보 생성
- 회사 도착·출발 → 업무시간 후보 생성
- 여행지 체류 → 방문 장소와 체류시간 후보 생성
- 수면 기록 → 건강 타임라인에 수면 막대 생성

HealthKit에 새로운 운동이나 건강 기록이 저장되면 앱이 백그라운드에서 변경을 확인할 수 있습니다. [HealthKit 백그라운드 기록 확인](https://developer.apple.com/documentation/healthkit/executing-observer-queries)

정확성을 위해 자동 기록 규칙은 구분해야 합니다.

- 직접 `시작·종료`: 즉시 확정
- HealthKit 운동·수면: 자동 확정
- 계획된 장소와 시간의 일치: Watch에서 한 번 확인
- 단순 위치·움직임 추정: 임시 기록함에 보관

예를 들어 위치만 보고 “업무했다”고 확정하면 오류가 생길 수 있으므로 다음처럼 묻습니다.

> 회사에 09:03~18:12 머물렀습니다.  
> 업무시간으로 기록할까요? `[기록]` `[수정]` `[무시]`

계획에 이미 같은 일정이 있다면 새 막대를 중복 생성하지 않고 **기존 계획 막대 안에 실제 수행 구간을 채워서 표시**합니다. 계획이 없으면 `예정에 없던 활동`으로 자동 생성한 뒤 분류만 확인받습니다.

Watch가 인터넷에 연결되지 않아도 시작·종료 기록을 저장해두었다가 iPhone에 연결되면 순서대로 전달할 수 있습니다. Apple의 Watch Connectivity가 백그라운드 전송과 대기열 전달을 지원합니다. [Watch Connectivity](https://developer.apple.com/documentation/WatchConnectivity)

이렇게 되면 앱의 핵심 흐름이 완성됩니다.

> 큰 계획은 iPad에서 만들고 → 오늘 일정은 iPhone에서 조정하고 → 실제 생활은 Watch가 간트차트에 기록한다.

결국 간트차트가 사용자가 매번 작성하는 계획표가 아니라, **생활하면서 자동으로 완성되는 ‘내 시간 기록표’**가 됩니다.

---

## 사용자

엄청 방대하네. 있으면 좋겠고 그지?

---

## Codex

맞습니다. **있으면 정말 좋습니다.** 다만 처음부터 전부 만들면 앱이 너무 무거워지고 정체성이 흐려질 수 있습니다.

겉으로는 방대하지만 내부 구조는 의외로 단순합니다.

> 모든 데이터 = 분류 + 이름 + 시작시간 + 종료시간 + 기록 출처

기록 출처만 달라집니다.

- 직접 작성
- 캘린더
- Apple Watch
- HealthKit
- 위치
- 여행 일정

그리고 이것들을 동일한 간트차트에 표시하면 됩니다.

출시 순서는 이렇게 잡는 것이 좋겠습니다.

1. iPhone·iPad 간트 타임라인
2. 일·생활·건강·여행 분류와 템플릿
3. iCloud 및 Apple 캘린더 연동
4. Apple Watch에서 시작·종료하면 막대 자동 생성
5. HealthKit 운동·수면 자동 기록
6. 지도·이동시간·여행 경로

초기 핵심은 딱 하나입니다.

> 계획한 시간을 간트차트로 보고, 실제 사용한 시간을 Apple Watch로 기록한다.

지도, 여행, 다이어트, 연간 회고는 이 핵심 위에 선택적으로 얹는 확장 기능으로 두면 됩니다. 사용자는 필요한 기능만 켜기 때문에 앱 화면은 가볍게 유지할 수 있습니다.

기능은 방대해질 수 있지만 제품의 중심은 단순합니다.

> **내가 계획한 삶과 실제로 보낸 시간을 한눈에 비교하는 앱.**

---

## 사용자

오 좋다. 근데 베이스는 간트차트야. 그리고 계획과 실제를 체크한다.

---

## Codex

맞습니다. 제품의 중심을 정확히 이렇게 고정하면 됩니다.

> **간트차트로 계획하고, Apple Watch로 실제를 기록하고, 계획과 실제를 비교한다.**

모든 기능은 이 구조를 벗어나지 않아야 합니다.

- 계획: 사용자가 간트 막대로 작성
- 실제: Watch·HealthKit·캘린더·위치가 기록
- 비교: 동일한 간트 막대 안에서 겹쳐 표시
- 회고: 주·월·분기·연 단위로 확대해서 확인

표현도 간단하게 할 수 있습니다.

- 계획 막대: 연한 파스텔색
- 실제 막대: 내부의 진한 색
- 계획보다 늦게 시작: 실제 막대가 오른쪽에서 시작
- 계획보다 오래 수행: 실제 막대가 계획 밖으로 돌출
- 실행하지 않음: 계획 막대만 남음
- 계획에 없던 활동: 별도의 실제 막대 생성

예를 들면:

```text
계획  |──────── 운동 40분 ────────|
실제       |──────── 운동 52분 ──────────|
          7분 늦음              19분 초과
```

중요한 원칙은 **실제가 계획을 덮어쓰지 않는 것**입니다. 계획은 당시의 의도로 보존하고, 실제는 별도 기록으로 남겨야 비교가 가능합니다.

화면 구조도 간트차트가 항상 주인공이어야 합니다.

- 가로축: 시간
- 세로축: 일·생활·건강·여행 또는 프로젝트
- 계획 막대와 실제 막대
- 현재 시각선
- 필요할 때만 지도·통계·상세정보 표시

제품을 한 문장으로 설명하면 가장 명확합니다.

> **계획과 실제가 함께 움직이는 개인용 간트차트.**

이 문장을 기준으로 기능을 판단하면 됩니다. 간트차트를 강화하거나 계획·실제 비교에 도움이 되면 추가하고, 그렇지 않으면 넣지 않는 방향입니다.

---

## 사용자

지도. 사진 기록까지 연동하면 어떻게 될까

---

## Codex (진행 응답)

지도와 사진은 별도 SNS 피드로 만들기보다, **간트차트의 ‘실제 기록’을 증명하고 회상하게 하는 자료**로 붙이는 것이 맞습니다. 촬영 시각·위치로 일정과 자동 연결할 수 있는 범위와 개인정보 권한을 확인해보겠습니다.

---

## Codex

지도와 사진까지 연결하면 간트차트가 단순한 일정표를 넘어 **‘시간·장소·기억이 연결된 생활 기록’**이 됩니다. 다만 지도나 사진 피드가 주인공이 아니라, 계속 **간트차트의 실제 기록에 붙는 증거**여야 합니다.

예를 들어 실제 간트 막대가 이렇게 보입니다.

```text
10:00 ├──── 경복궁 관람 ────┤ 12:30
          📍 경복궁   📷 12
```

막대를 누르면 상세 기록이 열립니다.

- 계획시간과 실제시간
- 머문 장소와 이동 경로
- 해당 시간에 촬영한 사진
- 걸음 수와 이동거리
- 간단한 메모
- 다음 장소까지 이동시간

사진은 촬영 시각과 위치를 기준으로 일정에 연결할 수 있습니다. PhotoKit의 사진 정보에는 촬영시간과 위치가 포함될 수 있으며, 사진 보관함 접근에는 사용자 허가가 필요합니다. [Apple PhotoKit](https://developer.apple.com/documentation/photokit/fetching-assets), [사진 메타데이터](https://developer.apple.com/documentation/photokit/phasset/1624755-favorite)

예를 들어 여행에서는:

1. Watch로 `여행 시작`
2. 위치에 따라 실제 이동 경로 기록
3. 방문 장소와 체류시간을 실제 간트 막대로 생성
4. 그 시간·장소에서 찍은 사진을 자동 추천
5. 여행 종료 후 날짜별 타임라인 생성

```text
09:00 공항 이동     🚇  📷2
12:00 비행          ✈️
16:00 호텔 체크인   📍  📷5
18:00 시내 관광     🚶  📷18
```

운동이나 다이어트 프로젝트에도 적용할 수 있습니다.

- 운동 막대: 경로·거리·심박수·운동 사진
- 식단 막대: 사용자가 선택한 음식 사진
- 다이어트 프로젝트: 월별 대표사진과 몸무게 변화
- 연말 회고: 운동했던 장소와 실제 운동시간

다만 사진을 전부 자동으로 붙이면 화면이 지저분하고 사생활 문제도 생깁니다. 따라서 다음 방식이 좋습니다.

- 기본: 사용자가 선택한 사진만 첨부
- 선택 기능: 시간·위치가 일치하는 사진을 앱이 추천
- 자동 추천 후 `[모두 연결]`, `[선택]`, `[무시]`
- 간트차트에는 사진 개수와 대표사진 한 장만 표시
- 다이어트 사진 등 민감한 사진은 위젯·잠금화면에서 숨김

Apple의 PhotosPicker를 사용하면 앱이 사진 보관함 전체를 읽지 않고 사용자가 고른 사진만 받을 수도 있습니다. [PhotosPicker 개인정보 보호](https://developer.apple.com/documentation/photosui/)

확대 수준에 따라서도 표현을 달리해야 합니다.

- 일간: 사진 썸네일과 정확한 장소
- 주간: 대표사진 한 장과 방문지
- 월간: 주요 활동 사진
- 연간: 여행·운동·가족 등 프로젝트별 대표 장면

첫 버전에는 **실제 간트 막대에 장소와 사진을 직접 첨부하는 기능**만 넣고, 다음 단계에서 촬영 시각·위치 자동 추천, 여행 경로, 연간 영상·포토 회고로 확장하는 것이 좋습니다.

제품의 중심 문장은 그대로 유지됩니다.

> **간트차트로 계획하고, Watch로 실제를 기록하며, 지도와 사진으로 그 시간을 기억한다.**

---

## 사용자

그러면 저장할때 사진을 경로만 가져오면 날라가잖아. 용량 최소화해서 아이클라우드로 올리면 좋으려나?

---

## Codex (진행 응답)

맞습니다. 로컬 사진 경로나 사진 앱의 기기별 식별자만 저장하면 기기 교체·삭제·동기화 상황에서 연결이 끊길 수 있습니다. 원본 전체를 복제하지 않고도 회고에 필요한 품질을 유지하는 저장 계층을 확인해보겠습니다.

---

## Codex

맞습니다. **경로만 저장하면 안 됩니다.** 사진 삭제, 기기 변경, 앱 재설치 시 연결이 끊길 수 있습니다.

가장 좋은 방식은 **사진 참조 + 저용량 보관본**을 함께 저장하는 것입니다.

### 권장 저장 방식

사진마다 다음 두 가지를 저장합니다.

1. iCloud Photos 식별자
   - 원본 사진은 사진 앱에 그대로 유지
   - 다른 Apple 기기에서도 같은 사진을 찾을 수 있음
   - 원본을 중복 업로드하지 않아 용량 절약

2. 저용량 보관본
   - 긴 변 1,280~1,600px
   - HEIC 품질 약 70~80%
   - 대략 장당 150~500KB
   - 원본이 삭제되어도 간트 기록과 회고에는 남음

Apple의 `PHCloudIdentifier`는 iCloud Photos로 동기화된 여러 기기에서 동일한 사진을 식별하기 위한 용도입니다. [Apple PHCloudIdentifier](https://developer.apple.com/documentation/photos/phcloudidentifier)

저용량 보관본은 CloudKit의 `CKAsset`으로 사용자의 비공개 iCloud 영역에 올리면 됩니다. CloudKit은 사진 같은 파일을 별도 자산으로 저장하고, 비공개 자산은 암호화됩니다. [CloudKit CKAsset](https://developer.apple.com/documentation/cloudkit/ckasset), [CloudKit 데이터 암호화](https://developer.apple.com/documentation/cloudkit/encrypting-user-data)

### 사용자 선택 옵션

- 가볍게: 작은 썸네일만 보존
- 권장: 화면 감상용 저용량 사진 보존
- 원본 보존: 선택한 사진만 원본 업로드
- 연결만: iCloud Photos 식별자만 저장

기본값은 `권장`이 적당합니다.

예상 용량은 압축 결과에 따라 달라지지만 장당 평균 250KB라면:

- 1,000장: 약 250MB
- 5,000장: 약 1.25GB
- 10,000장: 약 2.5GB
- 하루 5장씩 1년: 약 450MB

비공개 CloudKit 데이터는 사용자의 iCloud 저장공간을 사용하므로 앱 설정에서 예상 사용량과 정리 기능을 제공해야 합니다. [CloudKit 비공개 저장공간](https://developer.apple.com/documentation/CloudKit/CKContainer/privateCloudDatabase)

더 절약하려면 일정마다:

- 대표사진 1장: 1,600px 보관
- 나머지 사진: 480px 썸네일만 보관
- 원본 보기: 사진 앱의 원본 연결

이 구조가 좋습니다. 원본 사진이 남아 있으면 고화질로 보고, 삭제되었더라도 앱에는 회고용 사진이 남습니다.

결론적으로:

> **사진 경로가 아니라 iCloud Photos 식별자를 저장하고, 간트 기록이 사라지지 않도록 압축 보관본을 CloudKit에 함께 저장한다.**

이 방식이 용량·안전성·기기 간 동기화의 균형이 가장 좋습니다.

---

## 사용자

인스타 연동하면 어때

---

## Codex (진행 응답)

인스타그램은 **기록을 가져오는 기능**보다, 간트 회고를 예쁜 카드·릴스로 만들어 **내보내는 기능**부터 붙이는 것이 현실적입니다. 개인 계정 접근과 자동 게시에 대한 현재 Instagram API 제한을 공식 문서 기준으로 확인하겠습니다.

---

## Codex

좋습니다. 다만 **인스타그램은 기록을 가져오는 저장소가 아니라, 완성된 회고를 내보내는 출구**로 사용하는 것이 좋습니다.

### 가장 매력적인 공유 형태

간트차트 기록을 자동으로 콘텐츠로 만들어줍니다.

- 오늘의 계획과 실제
- 이번 주 운동 기록
- 한 달 다이어트 과정
- 여행 일정·이동 경로·사진
- 올해의 시간 사용 회고

여행이 끝나면 다음과 같은 캐러셀이 자동 생성될 수 있습니다.

1. 여행 제목과 기간
2. 계획 대비 실제 간트차트
3. 지도 이동 경로
4. 장소별 대표사진
5. 걸음 수·이동거리·방문 장소
6. 여행 한 줄 회고

릴스는 현재 시간선이 간트차트를 지나가면서 장소와 사진이 순서대로 나타나는 형태가 좋습니다.

> 간트 막대 이동 → 지도 경로 이동 → 해당 장소 사진 표시 → 하루 요약

### 인스타그램 연동 방식

일반 사용자에게는 직접 계정 연결보다 다음 방식이 적합합니다.

> `공유` → `스토리 / 피드 / 릴스` 선택 → 공유용 이미지·영상 생성 → Instagram에서 최종 확인 후 게시

자동으로 계정의 기존 게시물을 가져오거나 바로 게시하는 공식 API는 주로 비즈니스·크리에이터 같은 프로페셔널 계정을 대상으로 하며, 일반 개인 계정 접근에는 제한이 있습니다. [Meta 공식 Instagram API 문서](https://www.postman.com/meta/instagram/documentation/6yqw8pt/instagram-api)

따라서 일반 사용자용 앱에서는 계정 비밀번호나 접근 토큰을 받지 않고, iOS 공유 기능으로 Instagram 작성 화면에 넘기는 것이 간단하고 안전합니다.

### 공유 전 개인정보 보호

간트차트에는 민감한 정보가 많으므로 공유용 복사본을 별도로 만들어야 합니다.

- 회사명·업무 일정 숨기기
- 집과 회사의 정확한 위치 숨기기
- 위치를 동·도시 단위로 단순화
- 일정 제목 가리기
- 사진 직접 선택
- 실제 시각 대신 총 소요시간만 표시
- 공유 전 전체 미리보기

예를 들어 사용자가 `여행` 영역만 선택하면 업무와 생활 일정은 공유 이미지에서 완전히 제외됩니다.

### 개발 순서

1. 9:16 스토리 카드
2. 4:5 피드 캐러셀
3. 움직이는 간트차트 릴스
4. 여행·운동·연간 회고 템플릿
5. 프로페셔널 계정용 자동 게시 연동

인스타그램은 필수 관리 기능이 아니라 **자연스러운 홍보와 공유 기능**이 됩니다. 사용자가 만든 멋진 간트 회고가 공유될 때마다 다른 사람이 “저건 무슨 앱이지?”라고 궁금해하는 구조입니다.

핵심 표현은 이게 좋습니다.

> **계획과 실제가 쌓인 간트차트를, 나의 하루·여행·1년 이야기로 공유한다.**

---

## 사용자

서버는 어짜피 메타가 돈내니까

---

## Codex

맞습니다. 인스타에 올라간 공유본의 저장·전송 비용은 Meta가 부담합니다. 하지만 **인스타를 우리 앱의 원본 저장소로 사용하면 안 됩니다.**

이유는:

- 사용자가 게시물을 삭제하거나 계정을 비공개·탈퇴할 수 있음
- Instagram이 사진과 영상을 다시 압축함
- 일반 개인 계정은 API로 게시물 전체를 자유롭게 읽을 수 없음
- API 정책이 바뀌거나 계정 연결이 끊길 수 있음
- 게시 성공 후 우리 앱이 안정적으로 원본 위치를 돌려받지 못할 수 있음

대신 서버 비용은 다음처럼 분담하면 됩니다.

- 원본 사진: 사용자의 iCloud Photos
- 간트 데이터: 사용자의 비공개 CloudKit
- 저용량 보관본: 사용자의 비공개 CloudKit
- 인스타용 카드·릴스: Instagram
- 기기 데이터: 삭제 가능한 캐시

비공개 CloudKit은 사용자의 iCloud 저장 용량을 사용하므로 사진 때문에 개발자가 별도 서버비를 부담하는 구조가 아닙니다. [Apple CloudKit 비공개 저장공간](https://developer.apple.com/documentation/CloudKit/CKContainer/privateCloudDatabase)

즉, 초기에는 자체 서버 없이도 가능합니다.

> Apple은 개인 기록을 보관하고, Meta는 사용자가 공유한 결과물을 배포한다.

인스타에는 전체 사진이 아니라 **간트차트·지도·대표사진을 조합한 공유용 이미지나 영상**만 넘기면 됩니다. 그러면 저장 안정성과 개발비, 홍보 효과를 모두 잡을 수 있습니다.

---

## 사용자

인스타 처럼 사진을 올릴 수 있고 이 앱에서 그 앱을 연동해서 사진 가져올만한 서비스는?

---

## Codex (진행 응답)

가능한 서비스는 있습니다. 다만 선택 기준을 **기존 사진을 안정적으로 가져올 수 있는가, 개인 계정 API가 열려 있는가, 촬영시간·위치 정보가 유지되는가**로 잡아야 합니다. 현재 API가 실제로 열려 있는 서비스를 비교해보겠습니다.

---

## Codex

있습니다. 목적에 따라 가장 현실적인 후보가 다릅니다.

| 서비스 | 특징 | 적합도 |
|---|---|---:|
| Apple 사진·iCloud Photos | iPhone 기본 사진 저장소, 촬영시간·위치 연동 우수 | 가장 적합 |
| Google Photos | 사용자가 기존 사진을 선택해 앱으로 가져올 수 있음 | 매우 적합 |
| Flickr | 인스타처럼 사진 공개·앨범·피드가 있고 개인 사진 API 제공 | 소셜 연동에 적합 |
| SmugMug | 원본 사진·앨범·위치정보 API가 강함 | 사진가용 |
| Google Drive·OneDrive | 사용자가 파일 선택 가능 | 보조 가져오기 |
| Immich | 개인 서버에 사진을 올리고 API 연동 가능 | 고급 사용자용 |

### 가장 추천하는 조합

앱의 사진 추가 화면을 이렇게 구성합니다.

> `사진 추가` → `Apple 사진` · `Google Photos` · `Flickr` · `파일`

기본은 Apple 사진이고, 외부 서비스는 Google Photos를 우선 지원하는 것이 좋습니다.

Google Photos Picker API는 사용자가 Google Photos 안에서 사진을 직접 골라 앱에 제공하는 공식 방식입니다. 다만 선택한 사진의 다운로드 주소는 60분 동안만 유효하므로, 선택 즉시 사진을 내려받아 압축 보관본을 CloudKit에 저장해야 합니다. [Google Photos Picker API](https://developers.google.com/photos/picker/reference/rest), [선택 사진 내려받기](https://developers.google.com/photos/picker/guides/media-items)

### 인스타와 가장 비슷한 서비스

소셜 기능까지 원한다면 **Flickr**가 가장 가깝습니다.

- 사진 업로드
- 공개·비공개 사진
- 앨범
- 촬영시간·위치
- 사용자 인증 후 자신의 사진 가져오기
- 앱에서 사진 검색 및 선택

Flickr는 OAuth로 사용자의 공개·비공개 사진을 읽을 수 있는 API를 제공합니다. 다만 상용 앱에서 Flickr API를 사용하면 별도 승인이 필요하거나 비용이 발생할 수 있습니다. [Flickr 사용자 인증](https://www.flickr.com/services/api/auth.oauth.html), [Flickr API](https://www.flickr.com/services/api/)

SmugMug도 사진 원본과 다양한 크기, 위치정보, 앨범을 API로 제공하지만 현재 SmugMug 구독자가 대상이라 일반인용 앱의 기본 연동으로는 범위가 좁습니다. [SmugMug 이미지 API](https://api.smugmug.com/api/v2/doc/reference/image.html)

### 권장 저장 구조

외부 서비스의 주소만 저장하면 안 됩니다.

- 원본 위치: Apple 사진·Google Photos·Flickr
- 서비스 식별자: 원본을 다시 찾기 위한 값
- 압축 보관본: CloudKit
- 썸네일: 기기 캐시
- 촬영시간·위치: 간트 실제 기록에 저장

외부 서비스에서 사진이 삭제되거나 계정 연결이 끊겨도 간트차트의 사진은 남습니다.

결론적으로 초기 버전은 다음 정도가 적당합니다.

1. Apple 사진
2. Google Photos
3. 파일 앱
4. 이후 Flickr 추가

Instagram은 공유용, Google Photos와 Flickr는 가져오기용, iCloud는 간트 기록의 안전한 보관용으로 역할을 나누는 것이 좋습니다.

---

## 사용자

오케이. 그러면 이걸 pc. 윈도우나 맥에서 연결해서 보고 싶다면?

---

## Codex (진행 응답)

PC에서는 별도 Windows 앱부터 만들기보다 **브라우저에서 동일한 간트차트를 열어 보는 웹 편집기**가 가장 효율적입니다. iCloud 비공개 데이터에 Windows 브라우저가 접근할 수 있는 방식과 사진 표시 범위를 확인해보겠습니다.

---

## Codex

가능합니다. 이 경우 **PC는 웹 간트 편집기**로 제공하는 것이 가장 효율적입니다.

사용 흐름은 다음과 같습니다.

> Windows·Mac 브라우저에서 사이트 접속 → Apple 계정 로그인 → iPhone과 동일한 간트차트 표시

CloudKit JS를 사용하면 웹에서도 iOS·macOS 앱과 동일한 CloudKit 비공개 데이터를 읽고 수정할 수 있습니다. [Apple CloudKit JS](https://developer.apple.com/documentation/CloudKitJS)

### 기기별 역할

- Apple Watch: 실제 활동 시작·종료 기록
- iPhone: 오늘·주간 일정 확인과 빠른 수정
- iPad: 터치·펜으로 계획 편집
- Windows·Mac: 전체 간트차트 편집·비교·출력

PC 화면에서는 넓은 공간을 활용해 다음 기능을 제공합니다.

- 연간 간트차트
- 영역별 필터와 통합 보기
- 계획·실제 동시 표시
- 마우스 드래그로 일정 변경
- 지도와 사진 상세 보기
- 여행·운동·업무 템플릿 편집
- Excel·PPT·PDF·이미지 출력

현재 만든 HTML WBS의 데스크톱 조작 방식도 웹 편집기 UI의 기반으로 활용할 수 있습니다.

### 서버 구성

별도의 사용자 데이터 서버가 꼭 필요하지 않습니다.

- 웹 화면: 일반 웹 호스팅
- 로그인: Apple 계정
- 일정 데이터: CloudKit
- 압축 사진: CloudKit Asset
- 지도: MapKit JS
- 원본 사진: iCloud Photos
- PC 임시 데이터: 브라우저 캐시

CloudKit Web Services는 웹에서 데이터 생성·조회·수정·삭제와 사진 같은 Asset 다운로드를 지원합니다. [CloudKit Web Services](https://developer.apple.com/library/archive/documentation/DataManagement/Conceptual/CloudKitWebServicesReference/)

다만 PC 브라우저는 iPhone의 PhotoKit에 직접 접근할 수 없으므로, 앞에서 이야기한 **저용량 사진 보관본을 CloudKit에 올리는 구조가 필요합니다.** 그래야 Windows에서도 사진이 확실히 보입니다.

### Mac은 선택지가 하나 더 있음

Mac은 SwiftUI 코드를 공유해 네이티브 앱도 만들 수 있습니다. Apple은 iPhone·iPad·Mac 앱을 하나의 멀티플랫폼 대상으로 구성할 수 있도록 지원합니다. [SwiftUI 멀티플랫폼 앱](https://developer.apple.com/documentation/Xcode/configuring-a-multiplatform-app-target)

추천 출시 순서는:

1. iPhone·iPad·Watch 앱
2. Windows·Mac 공용 웹 편집기
3. 필요하면 Mac 네이티브 앱
4. 웹 편집기를 Windows 설치형 PWA로 제공

이렇게 하면 Windows 사용자는 별도 설치 없이 사용할 수 있고, 자주 쓰는 사용자는 시작 메뉴에 앱처럼 설치할 수 있습니다.

전체 구조도 자연스럽습니다.

> **PC에서 계획하고, iPhone에서 조정하고, Watch에서 실제를 기록한다.**

---

## 사용자

그러면 또 기록할만한게 뭐가 있을까

---

## Codex (진행 응답)

간트차트가 베이스라면 “기록 가능한 모든 것”을 넣기보다, **시간을 차지한 활동은 막대로 기록하고 나머지는 막대의 정보로 붙이는 원칙**이 좋습니다. 자동화 가능한 Apple 데이터와 과도해질 기능을 나눠보겠습니다.

---

## Codex

간트차트가 중심이므로 **시간을 차지한 것은 막대**, 나머지는 그 막대의 부가정보로 기록하면 됩니다.

### 간트 막대로 기록할 활동

가치가 큰 순서로 보면:

1. 수면
2. 업무·공부·집중시간
3. 운동
4. 출퇴근·이동·여행
5. 약속·병원·행사
6. 식사
7. 가족·친구와 보낸 시간
8. 취미·창작
9. 집안일
10. 휴식·명상
11. 대기시간
12. 예정에 없던 업무와 방해

특히 `예정에 없던 활동`이 중요합니다. 계획과 실제가 달라진 원인을 보여주기 때문입니다.

```text
계획  09:00 ───── 보고서 작성 ───── 12:00
실제  09:15 ─ 보고서 ─ 10:20
      10:20 ─ 갑작스러운 회의 ─ 11:10
      11:10 ─── 보고서 ─── 12:35
```

### 막대에 붙일 정보

- 장소와 이동 경로
- 사진·영상
- 걸음 수·거리·심박수
- 날씨
- 기분·에너지 1~5
- 음성·텍스트 메모
- 함께한 사람
- 실제 결과
- 비용이나 영수증
- 계획이 달라진 이유

수면·운동·걸음·마음챙김 등은 HealthKit에 이미 정의된 데이터가 있어 사용자 허가 후 연결할 수 있습니다. [HealthKit 데이터 종류](https://developer.apple.com/documentation/healthkit/data-types)

### 의외로 좋은 기록

- 날씨: 비 때문에 운동을 못 했는지, 날씨와 생활 패턴의 관계
- 음악·팟캐스트: 여행이나 운동 당시의 기억
- 기분·에너지: 어느 시간대에 집중이 잘되는지
- 만남: 가족·친구와 실제로 보낸 시간
- 성취 지점: 시험 합격, 여행 출발, 목표 체중 등
- 계획 변경 이유: 지연, 피로, 날씨, 돌발업무

Apple의 Journaling Suggestions를 사용하면 사용자가 직접 선택한 최근 사진, 방문 장소, 운동, 음악, 팟캐스트, 걸음 활동 등을 기록에 가져올 수 있습니다. 앱이 모든 사생활을 읽는 방식이 아니라 시스템 선택창에서 사용자가 고른 항목만 전달됩니다. [Apple Journaling Suggestions](https://developer.apple.com/documentation/JournalingSuggestions), [제공 가능한 기록 종류](https://developer.apple.com/documentation/journalingsuggestions/presenting-the-suggestions-picker-and-processing-a-selection)

날씨는 별도 막대를 만들지 않고 당시 장소·시간의 정보로 자동 첨부하면 됩니다. WeatherKit은 과거 날씨 통계도 제공할 수 있습니다. [Apple WeatherKit](https://developer.apple.com/weatherkit/)

첫 버전에서는 다음 6개만 기본 분류로 두는 것이 좋습니다.

- 수면
- 일·공부
- 운동
- 이동
- 생활
- 휴식

여행·다이어트·시험 같은 템플릿이 이 분류를 세부적으로 확장하도록 하면 화면은 단순하게 유지됩니다.

결국 가장 가치 있는 기록은 **많은 데이터가 아니라 ‘계획과 달라진 실제 시간’과 그 이유**입니다. 이게 쌓이면 사용자는 단순히 무엇을 했는지가 아니라, 왜 계획대로 되거나 되지 않았는지를 알 수 있습니다.

---

## 사용자

날씨 좋네. 이거 예보랑 실제 날씨가 다를 수 있잖아. 자동으로 보정되나

---

## Codex (진행 응답)

자동 보정되게 설계할 수 있습니다. 핵심은 계획할 때의 예보를 덮어쓰지 않고, **예보 스냅샷과 일정 종료 후 확인한 실제 날씨를 별도로 저장**하는 것입니다. WeatherKit이 제공하는 과거 관측 범위를 확인하겠습니다.

---

## Codex

자동 보정되게 만들 수 있습니다. 다만 **예보를 실제 날씨로 덮어쓰면 안 됩니다.** 계획 당시 예보와 실제 날씨를 각각 남겨야 합니다.

예를 들면:

```text
야외 러닝 18:00~19:00
계획 당시 예보  ☀️ 24°C · 강수 10%
실제 날씨       🌧️ 18°C · 강수 8mm
실제 활동       취소
```

이렇게 해야 “왜 계획과 실제가 달라졌는가”를 알 수 있습니다.

### 자동 보정 과정

1. 일정 생성 시
   - 당시 예보를 `계획 날씨`로 저장

2. 일정 7일·1일·1시간 전
   - 최신 예보로 화면 갱신
   - 최초 예보는 비교용으로 보존

3. 활동 시작·진행·종료 시
   - 해당 장소의 현재 관측 날씨 저장
   - 이를 `실제 날씨`로 확정

4. 다음 날
   - 과거 일일 관측 요약으로 최고·최저기온, 강수량, 적설량 보완

WeatherKit의 `CurrentWeather`는 특정 장소의 현재 관측 상태를 제공하고, 일정이 끝난 뒤에는 날짜별 관측 요약을 다시 조회할 수 있습니다. 일일 온도·강수·적설 요약은 2021년 8월 이후 날짜에 대해 제공됩니다. [WeatherKit](https://developer.apple.com/documentation/weatherkit), [과거 일일 날씨 요약](https://developer.apple.com/documentation/weatherkit/weatherservice/dailysummary%28for%3Afordaysin%3Aincluding%3A%29)

주의할 점은 나중에 조회할 수 있는 과거 데이터는 일일 요약이 중심이라는 것입니다. 일정 시간대의 정확한 실제 날씨를 남기려면 **활동 시작·중간·종료 시점에 현재 날씨를 자동 저장**하는 것이 가장 정확합니다.

화면에는 간단하게 표시하면 됩니다.

- 일정 전: `예보 ☀️ 24°`
- 일정 중: `현재 🌦️ 20°`
- 일정 후: `실제 🌧️ 18°`
- 차이가 큰 경우: `예보와 다름`

그리고 계획이 실행되지 않았을 때 다음처럼 이유를 추천할 수 있습니다.

> 예상과 달리 비가 내렸습니다. 미실행 이유를 `날씨`로 기록할까요?

따라서 날씨도 간트차트 철학에 정확히 맞습니다.

> **계획할 때 본 예보와 실제 활동 당시 날씨를 함께 보존해 계획과 실제의 차이를 설명한다.**

---

## 사용자

좋네. 원래 내가 지도 기반의 일상기록앱을 만들려고ㅠ했는데 간트차트가 더 좋아보인다.?

---

## Codex

저도 **간트차트를 중심으로 하는 편이 더 강하다**고 봅니다. 지도 기반 앱은 주로 “어디에 있었는가”를 보여주지만, 간트차트는 다음을 모두 표현합니다.

- 무엇을 하려고 했는가
- 언제 하려고 했는가
- 실제로 무엇을 했는가
- 얼마나 오래 했는가
- 계획과 실제가 왜 달라졌는가
- 그때 어디에 있었는가

지도 중심의 약점은 일상이 반복될수록 드러납니다. 집·회사·헬스장을 계속 오가면 지도 모양은 비슷하지만, 그 안에서 업무·공부·휴식에 사용한 시간은 보이지 않습니다. 미래 계획을 만들거나 일정 충돌을 확인하기도 어렵습니다.

반대로 간트차트는 미래와 과거를 동시에 다룹니다.

- 미래: 계획
- 현재: 실시간 실행
- 과거: 실제 기록과 회고

지도는 버리는 것이 아니라 **간트차트의 두 번째 보기**로 넣으면 됩니다.

```text
[타임라인] [지도] [회고]
```

- 타임라인: 계획과 실제를 편집하는 기본 화면
- 지도: 선택한 날짜·여행·프로젝트의 장소와 경로
- 회고: 사진·날씨·통계·계획 대비 실제

데이터의 기준은 항상 간트 일정입니다.

> 일정 막대  
> ├ 계획시간  
> ├ 실제시간  
> ├ 장소·경로  
> ├ 사진  
> ├ 날씨  
> └ 메모

여행 프로젝트를 열면 지도 비중이 커지고, 업무·공부 프로젝트를 열면 간트차트가 중심이 됩니다. 사용 목적에 따라 표현만 달라지는 것입니다.

정리하면:

> 지도 기반 앱은 ‘장소 일기’에 가깝고, 간트차트 기반 앱은 ‘계획 가능한 인생 기록’이 됩니다.

가장 좋은 방향은 **간트차트를 본체로 만들고, 지도·사진·날씨를 실제 기록의 맥락으로 연결하는 것**입니다. 원래 생각했던 지도 앱의 장점도 그대로 살리면서 제품의 차별점은 훨씬 명확해집니다.

---

## 사용자

지듬까지 한 대화 모두 기억하지?

---

## Codex (진행 응답)

네. 현재 대화 전체와 기존 WBS 프로젝트의 핵심 맥락을 이어서 보고 있습니다. 이후 설계가 흔들리지 않도록 기준만 다시 확인하겠습니다.

---

## Codex

네, 기억하고 있습니다. 현재 기준은 다음과 같습니다.

- 제품의 본체는 **개인용 간트차트**
- 계획과 실제 기록을 분리하여 한 막대에서 비교
- iPad·iPhone에서 계획하고 Apple Watch에서 실제 시작·종료 기록
- 일·생활·건강·여행 등을 분리하거나 통합해서 보기
- 템플릿으로 여행·운동·다이어트·공부 프로젝트 생성
- Calendar·HealthKit·지도·위치·사진·날씨 연동
- 지도와 사진은 본체가 아니라 실제 기록에 붙는 맥락
- 예보와 실제 날씨도 별도 보존
- 데이터는 iCloud·CloudKit에 저장
- 사진은 iCloud Photos 식별자와 저용량 보관본을 함께 저장
- Windows·Mac에서는 웹 간트 편집기로 확인·편집
- Instagram은 회고 공유용, Google Photos·Flickr는 사진 가져오기용
- 기능은 확장하되 화면은 가볍게 유지

핵심 문장은 이것입니다.

> **간트차트로 계획하고, Watch로 실제를 기록하며, 계획과 실제의 차이를 지도·사진·날씨와 함께 확인하는 앱.**

현재 대화 안에서는 계속 이 기준을 유지할 수 있습니다. 다만 아직 이 새로운 앱 구상을 별도의 기획서나 프로젝트 파일로 확정한 것은 아닙니다. 실제 개발로 넘어가기 전에는 지금까지의 내용을 한 번에 정리한 제품 기획서를 만드는 것이 좋습니다.

<oai-mem-citation>
<citation_entries>
MEMORY.md:48-60|note=[기존 WBS 간트차트 프로젝트와 핵심 상호작용 맥락 확인]
MEMORY.md:117-133|note=[기존 WBS 편집 저장 출력 검증 원칙 확인]
</citation_entries>
<rollout_ids>
019facd8-59c5-7342-addb-a7873a709c5f
</rollout_ids>
</oai-mem-citation>

---

# 부록: 개발 기획 리서치 및 확정 사항 (2026-07-30, Claude 딥리서치)

> 조사 방식: 5개 각도(구현 레퍼런스 / 참고 앱 UX / 디자인·색상 / 아키텍처·파일 구조 / 호환성·출력)로 병렬 웹·GitHub 검색 → 26개 소스 수집 → 122개 주장 추출 → 상위 25개를 각각 3표 적대적 검증(25/25 전부 확증, 반박 0건).
>
> 표기: **✅ 검증됨** = 원문 소스 대조 3-0 확증 / **⚠️ 참고** = 소스는 확인했으나 개별 적대 검증은 생략된 항목(채택 전 재확인 권장).

## A. 확정된 개발 방침 (대화에서 확정)

1. **플랫폼 개발 순서: iPhone → Apple Watch → iPad → MacBook**
2. **목표 범위 (현재 목표는 여기까지만)**
   - **1단계**: 딱 기본 기능만 넣은 간단한 간트차트 (iPhone)
   - **2단계**: Apple Watch 데이터를 받아와 실제 기록을 겹치는 것까지
   - iPad·Mac·지도·사진·날씨·인스타·웹 편집기 등은 장기 로드맵으로만 유지
3. **전체 프레임은 추후 확장을 고려해서 설계** — 화면과 기능은 최소로 시작하되, 데이터 모델·프로젝트 구조·색상 시스템은 뒤의 확장(캘린더, 지도, 사진, 날씨, 웹 편집기, Android)이 들어와도 갈아엎지 않도록 처음부터 자리를 만들어 둔다. (아래 D-3의 "추가 전용 스키마 진화" 제약 때문에 이 원칙은 선택이 아니라 필수)
4. **애플워치 연동 방식: 워치 앱 없이 시작** — 상세는 아래 F 참조.
5. **일→주→월→년 양방향 구조 필수** — 두 방향이 모두 동작해야 한다.
   - ① **기록 상향 집계**: 하루의 실제 기록이 주·월·년 화면에서 자동으로 합쳐져 보인다 (사용자가 집계를 위해 하는 일 없음)
   - ② **목표 하향 분해**: 년 목표 → 월 단계 → 주 계획 → 일 실행으로 터치로 쪼개 내려보낸다
   - 구현: 계획 모델에 자기참조 parent 관계(상위 목표 연결)를 **1단계부터** 넣는다. CloudKit 제약상 Optional + inverse로 설계(E 참조). 집계값은 저장하지 않고 쿼리로 계산 — 추가 전용 스키마 진화(D)와 충돌하지 않는 방식.
   - **뷰 하이라키 규칙**: 상위 뷰는 항상 하위 레벨의 요약본을 함께 보여준다.
     - 년 뷰 = 년 계획 + **월·주·일의 요약**
     - 월 뷰 = 월 계획 + **주·일의 요약**
     - 주 뷰 = 주 계획 + **일의 요약**
     - 하위 원본을 그대로 다 그리지 않고 집계로 압축 표시(원문 대화의 "축소할수록 사소한 일정은 묶거나 숨김" 원칙). UI 표현은 각 뷰 하단의 요약 스트립(색 = 주력 분류, 진하기 = 활동량, 탭 = 해당 기간으로 줌인) — 목업 ②③ 참조.
   - **그룹핑 규칙**: 년·월·주의 모든 계획은 하위 아이템을 가질 수 있는 그룹이다. 어느 뷰에서든 그룹 막대를 탭하면 **그 계획만의 간트차트**가 열림(행 = 하위 계획, 축 = 그 계획의 기간, 그룹 안에서도 줌 가능, 가장 자세한 레벨 = 일일 간트). 하위의 하위도 같은 방식으로 반복 — 트리 깊이 제한 없음. 목업 ④ 참조.
6. **UX는 터치 입력 위주, 키보드 입력은 철저히 제한** — 키보드는 계획명 또는 사용자가 직접 만드는 대분류 이름을 입력할 때만 등장. 분류 선택·아이콘·색상·시작·종료·길이·완료 처리·막대 이동/조절은 전부 탭·드래그·칩 선택. '최근 계획명' 칩을 쓰면 타이핑 0회로도 계획 생성 가능.
7. **하단 5탭 내비게이션** — 깔끔한 메뉴 5개: **시간표**(일|주|월|년을 한곳에서 전환) · **목표**(트리의 뿌리, 목표 카드 + 누적) · **＋**(중앙 추가 시트) · **회고**(기록·통계) · **설정**(표시·연동·개인정보). 기존 `오늘`과 `타임라인`은 같은 시간축을 배율만 달리 보는 기능이므로 `시간표` 하나로 통합한다. 루트 화면과 그룹 간트 같은 일반 탐색 화면에는 탭 바를 유지한다. 입력·편집 시트, 키보드, 온보딩, 완료·취소가 필요한 선택·검토 작업에서는 일시적으로 숨기며 작업을 마치면 이전 탭에서 다시 나타난다. 홈 위젯·Live Activity 같은 앱 밖 시스템 화면에는 앱 탭 바를 표시하지 않는다.

## B. 구현 참고 (GitHub)

### 1단계(기본 간트차트)에 바로 쓸 것

- ✅ **Yotei** — [github.com/claustrofob/Yotei](https://github.com/claustrofob/Yotei)
  - SwiftUI API(내부는 UICollectionView로 성능 확보)의 모듈형 캘린더 컴포넌트: 일 타임라인, 주 스트립, 월 그리드, 스케줄 리스트, 날짜 피커. MIT, 약 140스타, 2026-07까지 활발히 푸시됨.
  - **롱프레스로 막대를 집어 드래그 이동**(자동 스크롤, 날짜 넘어가는 이동, 15분 단위 스냅 기본값) 구현 — 우리 앱의 "막대 드래그 이동"의 가장 가까운 오픈소스 레퍼런스.
  - 이동 결과를 델리게이트로 (이전 DateInterval, 새 DateInterval) 쌍으로 돌려줌 → SwiftData 기록 + 버전 이력 저장 패턴으로 재사용 가능.
  - 한계: **줌 없음, 드래그로 길이 조절(resize) 없음**(이동 시 길이 보존만) → 확대·축소와 길이 조절은 직접 구현 필요. iOS 16+/Swift 6.2+. 신생 репо(2026-03 생성)라 API 변동 가능성 있음.
- ✅ **SpreadsheetView** — [github.com/bannzai/SpreadsheetView](https://github.com/bannzai/SpreadsheetView)
  - README에 "간트차트 레이아웃"이 명시된 목적. 고정 행/열 헤더, 셀 병합, 메모리 효율 스크롤. Examples 폴더에 실행 가능한 **Gantt Chart 샘플** 포함. MIT, 약 3,537스타.
  - 한계: **순수 UIKit**(SwiftUI 아님), 2024-05 이후 휴면 → 직접 의존하기보다 레이아웃 구조 참고용 또는 UIViewRepresentable 래핑 폴백.
- ✅ **Backyard Birds (Apple 공식)** — [github.com/apple/sample-backyard-birds](https://github.com/apple/sample-backyard-birds)
  - **우리가 계획한 구조와 정확히 같은 멀티플랫폼 레이아웃의 1차 공식 템플릿**: Multiplatform(iOS/iPadOS/macOS) + Watch + Widgets 타깃이 로컬 Swift 패키지(BackyardBirdsData = SwiftData 모델, BackyardBirdsUI = 공용 뷰)로 코드 공유.
  - SwiftData + Observable 프로토콜 조합(우리가 쓰려는 그 아키텍처), 위젯 안에서 `Button(intent:)`로 실제 동작을 수행하는 인터랙티브 위젯 예제 포함. MIT, 약 612스타.
  - 한계: WWDC23 시절 샘플, **CloudKit 동기화는 없음** → 구조·위젯 참고용이지 동기화 레퍼런스는 아님.

### 위젯·동기화 단계에 쓸 것

- ✅ **WidgetExamples** — [github.com/pawello2222/WidgetExamples](https://github.com/pawello2222/WidgetExamples)
  - 계획한 위젯 레이어 거의 전부가 한 репо에: App Intents 기반 인터랙티브 위젯(코드 확인됨), App Group 데이터 공유, SwiftData 위젯, Live Activity, Deep Link, 잠금화면 위젯 등. MIT, 약 1,114스타.
  - 한계: 2024-05 마지막 푸시(iOS 17 타깃) → iOS 18+ 신기능(Controls 등)은 없음. 다만 AppIntent 기반 Button/Toggle 패턴 자체는 2026년 현재도 현행 API.
- ✅ **CKSyncEngine 샘플 (Apple 공식)** — [github.com/apple/sample-cloudkit-sync-engine](https://github.com/apple/sample-cloudkit-sync-engine)
  - 커스텀 CloudKit 동기화(CKSyncEngine)의 공식 레퍼런스. 충돌 해결(.serverRecordChanged), 계정 변경, 재시도 로직이 SyncedDatabase.swift에 있음. 여러 기기 간 왕복 동기화를 시뮬레이션하는 테스트 패턴(SyncTests.swift)도 포함.
  - **개발 워크플로 함정(검증됨): CloudKit 푸시 알림은 시뮬레이터가 못 받으므로 푸시 기반 동기화 테스트는 실기기(iPhone/iPad/Mac) 필수.**
  - 주의: 우리는 기본적으로 SwiftData+CloudKit 자동 미러링을 쓸 계획이라 이 샘플은 "커스텀 동기화로 갈아탈 경우"의 레퍼런스. 다만 시뮬레이터 함정과 충돌 처리 패턴은 그대로 적용됨.

## C. 참고 앱 UX

### 가장 중요한 발견: Sunsama의 계획-실제 모델 (✅ 전부 공식 문서 검증)

- 계획 시간 = 작업별 예상치, 그 합계가 **하루 워크로드 총량** → 계획 vs 실제 비교가 작업 단위뿐 아니라 **하루 단위로 집계**됨. 워크로드 카운터에 'Actual vs. Planned' 나란히 표시 모드 + 초과 시 빨간색 신호등 색상. ([공식 문서](https://help.sunsama.com/docs/usage-guides/tasks/planned-and-actual-times/))
- 실제 시간 입력 경로 3가지(마찰 최소화): 작업의 시계 아이콘 수동 입력 / 단축키 E / 재생 버튼·스페이스바 타이머. → **Watch 시작·종료가 우리 앱의 '타이머 경로'에 대응.**
- **선택형 자동 채움**: 실제 시간 기록 없이 완료하면 계획 시간을 실제 시간으로 복사(계정 설정 옵트인). → "사용자가 기록을 깜빡하면?"에 대한 검증된 답.
- **시장 타이밍 증거(검증됨)**: Sunsama조차 캘린더 위 계획-실제 오버레이를 **2025-06-06에야 출시**. 원 요청은 2021-08-04에 올라와 101명이 투표하며 약 4년간 열려 있었고, 출시 시 기본 활성화 + 과거 데이터 소급 적용. ([로드맵](https://roadmap.sunsama.com/improvements/p/setting-actual-time-on-calendar-events-or-timeboxed-tasks-should-be-reflected-in)) → **우리 앱의 핵심 컨셉(계획 막대 위 실제 막대 오버레이)은 수요가 확인됐고, 선두 유료 앱도 최근에야 도달한 차별화 지점.**
- ⚠️ 사용자들은 실제 시간이 계획 슬롯을 초과하면 뒤 일정 자동 재배치 또는 겹침 표시를 원함 → 우리의 "뒤 일정 함께 밀기"가 정확히 이 요구에 대응.

### 기타 참고 앱 (⚠️ 리뷰·블로그 기반 참고)

- **Structured**: 색상 코딩된 드래그 가능 시간 블록의 세로 하루 타임라인이 핵심. 외부 캘린더를 같은 타임라인에 인라인 표시(우리 EventKit 오버레이 방식의 선례). iCloud 동기화 + Watch 앱. 프리미엄 약 $3/월. 불만: Windows/웹 없음, 분석 기능 부족 → **분석·계획-실제 깊이가 우리 차별화 기회.**
- **Timestripe**: 'Horizons' 뷰 = 일→주→월→분기→년→10년→인생 중첩 구간(우리 줌 구조의 선례). 'Climbs' 단계별 목표 프로그램 + 템플릿 갤러리(온보딩 템플릿 설계 선례). **시간 추적(실제 기록) 기능 없음**(2025-01 리뷰 기준) → 계획-실제가 우리 차별화. 불만: 장기 계획 화면이 압도적이고 학습 곡선 있음 → **연·인생 뷰 온보딩 UX 리스크로 새겨둘 것.**
- **Sorted 3**: 'hyper-scheduling' = 할 일 목록에 자동으로 시간 배치(버퍼 시간 포함) → 계획 막대 자동 배치의 선례. **핀치 줌으로 일정 시간 조절 + Time Ruler 제스처로 여러 작업 동시 조정** → 줌 가능한 타임라인 제스처 패턴 참고. 최대 위젯은 3일 스케줄 표시. 구독→일회성 구매($14.99 iOS)로 전환한 이력.

## D. 디자인·색상

### Apple HIG 제약 (✅ 전부 원문 검증)

1. **새로운 차트 유형은 온보딩 필수** — HIG '[Charting data](https://developer.apple.com/design/human-interface-guidelines/charting-data)': 낯선 차트는 해석 방법을 가르쳐야 함. Apple Watch 페어링 시 액티비티 링을 하나씩 애니메이션으로 소개하는 것이 인용 사례. **계획-실제 이중 막대 간트는 '낯선 차트'에 해당 → 첫 실행 시 계획 막대·실제 막대·빨간 현재선을 하나씩 애니메이션으로 소개하는 온보딩을 1단계 범위에 포함할 것.**
   - 또한 ⚠️ Canvas로 직접 그린 간트는 접근성 트리가 공짜로 안 생김 → 접근성 라벨·요소 수동 제공 필요. 위젯 소형 차트와 앱 전체 차트는 유형·색·주석·레이아웃을 일치시킬 것(Health Trends 사례). 한 차트에 데이터를 몰아넣지 말고 점진적 공개 — **연→분기→월→주→일 줌 구조가 HIG 권장과 정확히 일치.**
2. **다크모드는 파스텔 반전이 아님** — HIG '[Dark Mode](https://developer.apple.com/design/human-interface-guidelines/dark-mode)': 다크 팔레트는 라이트의 단순 반전이 아니므로 **분류 색상마다 별도 튜닝된 다크 변형**이 필요. 공식 메커니즘은 Xcode 애셋 카탈로그의 Color Set(라이트/다크 변형), 하드코딩 색상 금지. 대비 기준: 모든 외관에서 최소 4.5:1, 커스텀 전경/배경 쌍은 7:1 목표(막대 위 텍스트 라벨에 직접 적용; 텍스트 아닌 막대 채움색은 WCAG상 약 3:1이면 충분).
   - ⚠️ watchOS는 다크모드 자체가 없음(단일 다크 외관) → 워치용은 이중 팔레트 불필요.
3. **Live Activity 제약 (설계 변경 필요)** — HIG '[Live Activities](https://developer.apple.com/design/human-interface-guidelines/live-activities/)' + ActivityKit 문서: **활성 8시간 하드 리밋**(시스템이 자동 종료, 잠금화면 잔류 최대 12시간). 갱신은 내용이 실제로 바뀔 때만, 푸시 갱신은 예산 스로틀링. → 원래 구상한 "하루 종일 흐르는 타임라인 Live Activity"(16시간+)는 **기술적으로 불가능**. 현재 시간 블록 또는 8시간 미만 세션 단위로 축소하고, 빨간 현재선의 연속 이동은 `Text(timerInterval:)`·`ProgressView(timerInterval:)` 같은 시스템 자동 갱신 프리미티브로 구현할 것. (iOS 17.2+ push-to-start로 새 액티비티 체이닝 가능)
   - ⚠️ Live Activity 안의 인터랙션은 필수 컨트롤 1개 수준으로 최소화 권장(운동 일시정지/재개처럼) → 시작·종료 버튼 1개는 적합, 복수의 계획 조작 버튼은 부적합.
   - ⚠️ **Dynamic Island는 항상 검은 불투명 배경 + 굵은(bold) 색 권장** → 여기서는 파스텔이 아니라 **분류색의 채도 높은 변형**을 쓸 것.
   - ⚠️ iPhone에서 Live Activity를 시작하면 **Apple Watch Smart Stack 상단에 자동 표시**됨(커스텀 watchOS 레이아웃 + 인터랙티브 요소 권장) → **워치 앱을 만들기 전에도 워치에 존재감을 만드는 저비용 경로.**

### 색상 팔레트 시작점 (⚠️ 참고)

- Gridfiti의 플래너용 파스텔 팔레트 모음(12개+, hex 공개): 대표 "Pastel" 5색 = `#BEDAE3` `#C4E9DA` `#FED5CF` `#F1B598` `#D3C7E6`, "Cotton Candy" = `#E8ADB6` `#CBADCC` `#F9D9E1` `#C9D6E5` `#A0ACC6` 등. 5색 세트가 플래너 색 구성의 일반적 단위. ([출처](https://gridfiti.com/google-calendar-color-schemes/))
- 단, 이 출처는 다크모드 변형도, 기능별(일/생활/건강) 매핑 체계도 없음 → **원료로만 쓰고, 분류색 시스템(라이트/다크 쌍 + 실제 막대용 고채도 변형)은 자체 설계 필요.** 빨간 현재선이 파스텔 막대 위에서 3:1 대비를 두 외관 모두 만족하는지 확인할 것.

## E. 아키텍처·파일 구조

### 프로젝트 구조 (Backyard Birds 방식, ✅)

```text
GanttApp/
├── Packages/                  ← 로컬 Swift 패키지 (확장 대비 핵심)
│   ├── GanttData/             ← SwiftData 모델 + 저장 로직 (플랫폼 무관)
│   └── GanttUI/               ← 공용 뷰·색상 시스템
├── Multiplatform/             ← iOS 앱 타깃 (1단계)
├── Widgets/                   ← 위젯 익스텐션 (1~2단계)
└── Watch/                     ← watchOS 타깃 (워치 앱이 필요해지는 시점에 추가)
```

- **처음부터 모델을 로컬 SPM 패키지로 분리**하는 것이 "추후 확장 고려 설계"의 실체다. iPad·Mac·워치 타깃 추가가 패키지 재사용으로 끝난다.
- ✅ App Group으로 **iOS 앱 ↔ 위젯 익스텐션의 SwiftData 컨테이너 공유는 정상 동작**(같은 컨테이너 경로 확인됨). 설정 5단계: 양쪽 타깃에 같은 App Group 추가 → 모델 파일 양쪽 타깃 포함 → 위젯 Configuration에 `modelContainer()` 적용. 공유 설정 후 첫 실행 시 SwiftData가 기존 저장소를 자동 이전. 위젯 안에서 `@Query` 사용·데이터 수정도 가능. ([HackingWithSwift](https://www.hackingwithswift.com/quick-start/swiftdata/how-to-access-a-swiftdata-container-from-widgets)) 위젯 갱신은 스로틀링되므로 중요한 변경 시 `WidgetCenter.shared.reloadAllTimelines()` 명시 호출.
- ⚠️ **반면 watchOS는 App Group 저장소 공유 불가** — 같은 App Group ID를 선언해도 워치는 별도 샌드박스의 다른 컨테이너를 봄(Apple 포럼 실측 사례, 4개 타깃 동일 설정에도 컨테이너 UUID 상이). 해결책은 저장 파일 공유가 아니라 **모델 계층을 공유 패키지로 추상화하고 CloudKit 또는 Watch Connectivity로 동기화**. ([developer.apple.com/forums/thread/739235](https://developer.apple.com/forums/thread/739235)) → 위 구조의 GanttData 패키지가 이 답이기도 함.

### SwiftData + CloudKit 스키마 규칙 (⚠️ fatbobman — 실전에서 가장 많이 인용되는 정리)

CloudKit 동기화를 켜는 순간 모델에 걸리는 제약 ([출처](https://fatbobman.com/en/snippet/rules-for-adapting-data-models-to-cloudkit/)):

1. `@Attribute(.unique)` 사용 불가 (기기 간 원자적 유일성 보장 불가) → 고유 ID는 UUID 속성으로 직접 관리
2. 모든 속성은 Optional이거나 기본값 필수 (부분 동기화 상태 대응)
3. 모든 관계는 Optional + inverse 필수, Deny 삭제 규칙 불가, Ordered 불가 — 관계가 CloudKit 모델 오류의 최다 발생 지점
4. **출시 후 스키마 마이그레이션은 추가 전용(additive only)** — 엔티티/속성 삭제·이름 변경 불가(이름 변경 = 삭제+생성 취급), 타입 변경 불가 → **계획/실제 이중 기록 + 버전 이력 모델은 처음부터 추가 전용 진화가 가능하게 설계할 것 (A-3 원칙의 근거)**

### CloudKit Dev/Production 환경 함정 (⚠️ fatbobman)

- 개발 중 잘 되던 동기화가 **App Store 출시 후 통째로 실패**할 수 있음: CloudKit은 Development/Production 두 격리 환경을 유지하며, Production은 스키마가 없으면 읽기·쓰기를 전부 거부.
- 해결은 수동 작업: CloudKit Console에서 **Schema → Deploy Schema Changes**로 스키마 승격. Xcode가 자동으로 해주지 않음. **모델을 바꾼 모든 릴리스마다 반복되는 출시 체크리스트 항목.**
- TestFlight는 기본 Production 연결(엔타이틀먼트 `com.apple.developer.icloud-container-environment`를 Development로 바꿔 베타 테스트 가능). ([출처](https://fatbobman.medium.com/fix-core-data-swiftdata-cloud-sync-issues-in-production-b35ae50f8501))

### 상태 아키텍처 (⚠️ 참고)

- MV(Model-View): 설정 없이 가장 빨리 시작, 기능 1~2개의 작은 프로젝트에 적합 / 복잡 로직 둘 곳이 없음
- MVVM: 현대 Swift에서 가장 보편적, 테스트 용이 / 레이어·파일 증가
- TCA: 테스트성 최고, 대규모 팀·기능 상시 추가 앱용 / 셋업 최복잡, 첫 빌드 느림 → **1인 개발 개인 앱에는 과함**
- **권장: SwiftData + @Observable 모델(Backyard Birds 방식)로 시작** — 1단계 규모에 맞고 Apple 공식 샘플과 같은 결이라 확장 시 참고 자료가 가장 많음.

## F. 애플워치 연동 방식 결정 — "데이터만 받아오면 워치 앱이 필요한가?"

**결론: 2단계(데이터 수신만)에는 워치 앱이 필요 없다.**

- Apple Watch가 측정한 운동·수면·심박·걸음 데이터는 **페어링된 iPhone의 HealthKit 저장소로 자동 동기화**된다. iPhone 앱이 HealthKit 읽기 권한만 받으면 워치에 아무것도 설치하지 않고 전부 조회할 수 있다.
- 사용자는 워치 기본 운동 앱/수면 추적을 그대로 쓰고, 우리 iPhone 앱은 HealthKit 쿼리(+ 백그라운드 옵저버)로 새 기록을 감지해 간트에 "실제 막대"로 그려주면 된다. **즉 2단계는 '워치 연동'이라기보다 'iPhone 앱의 HealthKit 연동' 기능이다.**
- 워치 앱이 필요해지는 시점 (3단계 이후로 미룸):
  - 손목에서 직접 `시작/종료/미루기` 버튼 (plan.md 원문 구상의 "간트 막대 실시간 생성")
  - 앱이 직접 운동 세션을 실행·기록 (HKWorkoutSession은 watchOS 앱 필요)
  - 컴플리케이션, 워치 자체 화면
- 중간 다리(참고): iPhone에서 Live Activity를 시작하면 **워치 Smart Stack에 자동 표시**되므로, 워치 앱 없이도 "지금 하는 일"을 손목에 보여주는 것은 가능 (D-3 참조).
- 나중에 워치 앱을 만들 때도 App Group 파일 공유는 불가(E 참조)이므로, 지금 GanttData 패키지 분리만 해두면 추가 비용이 없다.

## G. 호환성·출력 (추후 확장 단계 참고)

- ✅ **Google Calendar 연동은 공짜가 아님**: Google Calendar 이벤트 읽기는 공식적으로 'sensitive' OAuth 스코프 → 일반 출시 전 **Google 앱 검증 필수**(심사 최대 10일). 요구사항: 앱 홈페이지와 같은 도메인에 호스팅된 공개 개인정보처리방침 + OAuth 동의 화면 링크, Search Console 도메인 소유 증명, 스코프별 기능을 보여주는 영어 데모 영상 + 서면 정당화. 검증 전엔 '미확인 앱' 경고 + 사용자 수 상한(TestFlight 베타에도 영향). ([출처](https://developers.google.com/identity/protocols/oauth2/production-readiness/sensitive-scope-verification)) → **개인 사용 단계에서는 유예 가능, 스토어 출시 시점에 일정·비용 항목으로 잡을 것.**
- ⚠️ **ICS 내보내기**: [swift-calendar/icalendarkit](https://github.com/swift-calendar/icalendarkit) — RFC 5545 **인코더 전용**(파싱 없음 → 가져오기는 별도 파서 필요). MIT, SPM. 2021-11 이후 휴면(작아서 포크/벤더링 가능), iOS 지원 명시 없음(순수 Swift라 가능성 높지만 확인 필요).
- ⚠️ **Excel 내보내기 (Swift 네이티브)**: [TheAcharya/XLKit](https://github.com/TheAcharya/XLKit) — iOS 15+/macOS 12+, Swift 6+, MIT, SPM. 2026-07-18 v1.1.7까지 활발한 릴리스(2~7월 10회). 셀 이미지 임베드(비율 자동 유지), CSV/TSV 왕복, sync/async API. 단 README 스스로 "프로덕션 검증 부족" 경고 → 채택 전 재확인.
- ⚠️ **PPT 내보내기 (웹 경로)**: [gitbrent/PptxGenJS](https://github.com/gitbrent/PptxGenJS) — 서버 없이 **브라우저 완전 클라이언트사이드**로 .pptx 생성 가능. **HTML `<table>` → 슬라이드 원콜 변환 API** 제공 → **기존 단일 HTML WBS 도구와의 직접 호환 경로.** MIT, 런타임 의존성 0, TS 정의 포함, 약 5.9k스타, v4.0.1 활발 유지. 생성 파일은 PowerPoint/Keynote/LibreOffice/Google Slides에서 열림.
- ⚠️ **CloudKit 웹 접근 (웹 편집기용)**: 비공개 DB는 사용자 대면 인증만 가능 — CloudKit Dashboard의 API 토큰 + 사용자의 Apple ID 로그인으로 얻는 웹 인증 토큰(서버-서버 키는 공개 DB 전용). 웹 토큰은 기본 30분 만료('로그인 유지' 선택 시 2주), **요청마다 1회용이라 응답의 새 토큰으로 계속 회전 저장** 필요. REST 엔드포인트: `https://api.apple-cloudkit.com/database/[version]/[container]/[environment]/[operation]`. **주의: 근거 문서가 2016년 아카이브라 웹 편집기 설계 확정 전 현행 동작 재검증 필수.** ([출처](https://developer.apple.com/library/archive/documentation/DataManagement/Conceptual/CloudKitWebServicesReference/SettingUpWebServices.html))

## H. 리서치가 못 채운 부분 (후속 조사 필요)

이번 검증을 통과한 25개 주장 밖에서, 다음은 **아직 조사·검증되지 않았다**:

- HealthKit 수면 단계/운동 쿼리의 좋은 샘플 코드, Watch Connectivity(WCSession) 패턴 → **2단계 착수 전 조사 1순위**
- 간트 막대 **드래그로 길이 조절(resize)** 구현 사례 (Yotei는 이동만 지원)
- SwiftData+CloudKit **자동 미러링** 고유의 함정 (검증된 Apple 샘플은 커스텀 CKSyncEngine 쪽)
- Sunsama 외 참고 앱들의 앱스토어 리뷰 감성 분석 (Structured/Timestripe/Sorted 3는 블로그 리뷰 기반 ⚠️)
- 파스텔 팔레트의 다크모드 변형 실례, 빨간 현재선(now-line)의 기존 앱 선례
- ICS 왕복(가져오기 파서), CloudKit JS의 2026년 현재 유효성

## I. 리서치를 개발 단계에 매핑

| 단계 | 범위 | 이번 리서치에서 가져갈 것 |
|---|---|---|
| **1단계** | iPhone 기본 간트차트 (계획 막대 CRUD, 드래그 이동, 연/월/주/일 줌, 분류 색상, 현재선, 저장) | Yotei(드래그·스냅), SpreadsheetView Gantt 예제(레이아웃), Backyard Birds(프로젝트 구조 + @Observable), CloudKit 스키마 규칙(모델 설계 시점부터), 파스텔 팔레트 + Color Set 다크 변형, 차트 온보딩, Sorted 3 핀치 줌 제스처 |
| **2단계** | Apple Watch 데이터 수신 → 실제 막대 오버레이 | 워치 앱 불필요(F), HealthKit 쿼리 후속 조사(H), Sunsama 계획-실제 모델(하루 집계 + 자동 채움 옵션), 오버레이 시 뒤 일정 처리(C) |
| 이후 | 위젯/Live Activity | WidgetExamples, App Group 공유 5단계, 8시간 제한 재설계, Dynamic Island 고채도 색 |
| 이후 | iPad → Mac | GanttData/GanttUI 패키지 재사용, CloudKit JS 재검증(G) |
| 이후 | 외부 연동·출력 | Google OAuth 검증 일정 확보, icalendarkit/XLKit/PptxGenJS |

## J. iPhone UI 초안 (1단계 기준)

> 시각 목업: 같은 폴더의 `ui-draft-iphone.html` 참조 (브라우저로 열면 화면별 목업 확인 가능)

- **화면 구성 원칙 (v3)**: 간트가 항상 주인공. **하단 5탭** — 시간표 / 목표 / 중앙 ＋ / 회고 / 설정. 기존 `오늘`과 `타임라인`은 `시간표`로 통합한다. 루트 화면과 그룹 간트 같은 일반 탐색 화면에는 탭 바를 유지한다. 입력·편집 시트, 키보드, 온보딩, 완료·취소가 필요한 선택·검토 작업에서는 숨긴다. 홈 위젯·Live Activity는 앱 밖 시스템 화면이므로 대상이 아니다.
- **시간표 상단 바**: `일 | 주 | 월 | 년` 세그먼트를 항상 같은 위치에 둔다. 앱 시작과 하단 `시간표` 탭의 기본값은 일간이며, 설정에서 마지막으로 본 배율 또는 원하는 시작 배율을 고를 수 있다. 분기는 년 뷰의 중간 줌으로 흡수한다.
- **설정 탭**: 하단 다섯 번째 고정 메뉴. 첫 화면은 `화면과 동작`(시간표 시작 배율·달리는 고양이), `앱 연동`(사진·캘린더·건강/Apple Watch·위치/이동), `데이터와 개인정보`(위젯 노출·권한·내보내기·연결 해제·삭제) 세 묶음만 보여준다. 각 권한은 해당 기능을 처음 켤 때만 요청하고 설정에서 언제든 끌 수 있다.
- **그룹 간트 화면**: 주 뷰에서 '신제품 기획' 탭 → `◀ 신제품 기획` 헤더의 전용 간트(행 = 하위 계획, 일|주|월 줌, 하단 일 요약 스트립). 하위 계획을 가진 막대에는 `▸ n` 그룹 표시. 이 화면도 하단 5탭을 유지하며 시간표에서 들어온 경우 `시간표`, 목표에서 들어온 경우 `목표`를 선택 상태로 표시한다.
- **목표 탭**: 목표 카드 리스트 — 카드마다 색 칩 + 기간 + 하위 개수 + **아래에서 쌓인 실제 누적/계획**(진행 막대). 카드 탭 = 그룹 간트, `＋ 하위 계획 내리기`로 하향 분해. 점수(%) 대신 시간 누적이 기본.
- **본문 (간트 캔버스)**:
  - 가로축 = 시간, 세로축 = 분류(일·생활·건강·여행…) 행
  - 계획 막대 = 파스텔 분류색, (2단계부터) 실제 막대 = 같은 색의 고채도 변형을 계획 막대 안/아래에 겹침
  - **빨간 현재선**은 모든 줌 레벨에서 동일하게 표시 (제품 정체성)
  - 좌우 스와이프 = 시간 이동, 핀치 = 줌 레벨 전환, 막대 롱프레스 = 드래그 이동(15분 스냅), 막대 탭 = 상세/편집 시트
  - **각 줌 레벨 = 자기 레벨의 계획 + 하위 레벨의 요약** (A-5 뷰 하이라키 규칙): 년(월·주·일 요약) / 월(주·일 요약) / 주(일 요약). 뷰 하단의 요약 스트립으로 표현, 탭하면 해당 기간으로 줌인.
- **계획 추가 시트**: 딱 5개 필드 — 계획명 / 대분류 / 시작 / 종료 / 메모 (plan.md 원문의 "무겁지 않게" 기준 유지). 대분류는 현재 선택값 한 줄만 표시하고, 탭했을 때 기본 13개(이동·위치·사진을 별도 제공)와 `＋ 직접 추가`가 있는 선택 화면을 열어 평상시 화면을 단순하게 유지.
- **사용자 대분류 직접 추가**: 이름 / 아이콘 / 색상 3개 필드만 제공. 이름만 키보드 입력이며 아이콘·색상은 터치 선택. 생성 후 이름·아이콘·색상·순서·숨김을 편집할 수 있고, 사용 중인 대분류 삭제 시 연결된 액션 아이템은 삭제하지 않고 다른 대분류로 재지정. 아이콘은 업무·학습·목표·육아·가족·건강·운동·공연·음악·여행·장소·사진·식사·반려동물·쇼핑·자연 등 최소 24개를 터치 선택으로 제공한다.
- **사진 앱 연동과 `사진` 대분류**:
  - `사진`을 열세 번째 기본 대분류로 추가한다. 직접 붙인 사진과 사진 앱에서 자동 연결한 사진 항목은 이 대분류로 모인다.
  - 사용자가 사진 접근을 허용하면 일간 타임라인에 `사진` 행을 만들고, 각 사진의 촬영 시각을 같은 하루 시간축의 X좌표로 변환해 작은 썸네일로 표시한다.
  - 서로 가까운 시각의 사진이 여러 장이면 대표사진 한 장과 `+N`으로 묶는다. 썸네일을 탭하면 묶음을 펼쳐 전체 사진, 촬영 시각, 연결된 일정·장소를 확인한다.
  - 제한된 사진 접근을 선택하면 허용된 사진만 표시한다. 접근 권한이 없거나 그날 사진이 없으면 사진 행 자체를 숨겨 간트 영역을 넓게 유지한다.
  - 사진은 계획 막대가 아니라 촬영 순간을 나타내는 자동 기록이다. 사용자가 원하는 경우 특정 사진을 액션 아이템의 메모·대표사진으로 연결할 수 있다.
  - 주·월·년에서는 모든 썸네일을 펼치지 않고 날짜별 대표사진과 사진 수만 요약한다.
  - 민감한 사진은 숨김 처리할 수 있으며 홈 위젯·잠금 화면·Live Activity에는 기본으로 노출하지 않는다. 사진 접근 권한과 사진 표시 기능은 설정에서 언제든 끌 수 있다.
- **자동 기록에서 `위치`와 `이동` 분리**:
  - `위치`: 일정 시간 머문 장소. 예: 집, 회사 9층, 회사 10층, 식당. 저장값은 장소 ID·건물·층·도착/출발·신뢰도이며 정확한 주소와 원시 좌표는 기본 화면에 노출하지 않는다.
  - `이동`: 위치와 위치 사이의 이동 구간. 1차 지원 수단은 **걷기 · 달리기 · 자전거 · 버스 · 지하철 · 택시 · 자가용 · 기차 · 비행기 · 배** 열 가지만 제공한다.
  - 같은 건물의 `회사 9층 → 회사 10층`은 두 위치 구간을 나누고 그 사이에 `층 변화 +1` 맥락을 기록한다. 층 변화 자체를 열한 번째 이동수단으로 만들지는 않는다.
  - 일간 간트에는 `위치` 행과 `이동` 행을 별도로 표시한다. 주·월·년에서는 위치별 체류시간과 이동수단별 이동시간·거리로 각각 합산한다.
  - **하단 노선형 이동 경로**: 지도 대신 일간 화면 하단에 지하철 노선도 같은 `O────O────O` 스트립을 제공한다. 노드는 위치, 노드 사이 선은 이동수단이다. 예: `집 O─걷기─O 역삼역 ─지하철─ O 을지로입구 ─걷기─ O 회사 9F ─↑1F─ ● 회사 10F`.
  - 현재 위치는 채운 노드, 지나온 위치는 빈 원, 신뢰도가 낮은 이동은 점선으로 표시한다. 위치 노드를 탭하면 장소·층·체류시간, 이동 선을 탭하면 수단·시간·거리·추정 근거를 연다.
  - 경로가 길면 가로로 밀어 하루 전체를 확인하며, 평상시에는 한 줄로 접힌다. 정확한 지도 좌표와 도로 모양은 표시하지 않는다. 주·월·년 화면에서는 노선도를 만들지 않고 집계만 보여준다.
  - **시간축 동기화**: 노드와 선을 균등 간격으로 놓지 않고 위 일간 간트와 같은 시간축에 배치한다. 위치에 머문 시간은 위치색 선의 길이, 이동시간은 이동수단 선의 길이로 표현한다. 예를 들어 회사 9층에서 2시간 머물면 해당 위치 구간도 2시간 비율만큼 길어진다.
  - 위 간트의 빨간 현재선과 하단 경로의 빨간 현재점은 같은 시각 값 하나를 공유한다. 시간이 흐르면 둘이 함께 오른쪽으로 이동하고, 이동 중이면 이동 선 위에, 머무는 중이면 위치 구간 위에 표시한다.
  - 사용자가 위 시간축 또는 하단 경로를 좌우로 밀면 두 화면이 동일한 시간 범위로 함께 이동한다. 실시간 보기에서는 현재 시각이 화면 중앙 부근에 오도록 자동 추적하며, 사용자가 직접 과거를 탐색하면 자동 추적을 잠시 멈추고 `지금으로` 버튼을 제공한다.
- **GPS + iPhone 센서 + Apple Watch 센서 결합 추정**:
  - iPhone 기본 신호: Core Location 좌표·고도·속도·방향·방문/지오펜스, Core Motion의 걷기·달리기·자전거·자동차·정지 분류와 신뢰도, 걸음·거리·층계, 기압 기반 상대고도.
  - Apple Watch 보조 신호: 사용자가 허용한 HealthKit 운동 유형·운동 경로·걸음·활동 데이터, 또는 Watch 앱이 활성 세션에서 모은 신호. Watch는 선택 사항이며 없어도 iPhone만으로 동작해야 한다.
  - Apple의 `CMMotionActivity`가 직접 구분하는 것은 정지·걷기·달리기·자전거·자동차·알 수 없음이다. 따라서 버스·지하철·택시·자가용·기차·비행기·배는 속도·경로·역/공항/항구·GPS 품질 변화·사용자 교정 등을 더한 앱 자체 분류 결과이며 센서만으로 확정하지 않는다. [CMMotionActivity](https://developer.apple.com/documentation/coremotion/cmmotionactivity)
  - 지하철은 역 출입 지오펜스 + 보행 구간 + 철도 경로 일치 + GPS 약화/단절 + 기압 상대고도 하강을 함께 사용한다. 기압 하강 하나만으로 지하철이라고 확정하지 않는다.
  - 위치 추적은 항상 고정밀 GPS를 켜두지 않고 방문·중요 위치 변화·모션 분류를 기본으로 하며, 이동 전환이 감지될 때만 짧게 정밀도를 올린다. 백그라운드 고정밀 위치는 사용자가 명확히 켠 경우에만 사용한다. [Core Location](https://developer.apple.com/documentation/corelocation), [백그라운드 위치](https://developer.apple.com/documentation/corelocation/handling-location-updates-in-the-background)
- **층 추정 규칙과 한계**:
  - Core Location이 실내 층 정보를 제공하는 곳에서는 `CLLocation.floor`를 우선한다. 층 정보가 없는 장소에서는 값이 `nil`일 수 있다. [CLFloor](https://developer.apple.com/documentation/corelocation/clfloor), [CLLocation.floor](https://developer.apple.com/documentation/corelocation/cllocation/floor)
  - `CMAltimeter.relativeAltitude`는 측정 시작점 대비 상대 고도 변화를 미터 단위로 제공한다. 사용자가 한 번 확인한 기준층과 건물별 층고를 결합해 9층→10층을 추정한다. [CMAltimeter 상대고도](https://developer.apple.com/documentation/coremotion/cmaltitudedata/relativealtitude)
  - `CMPedometerData.floorsAscended/Descended`는 계단을 걷거나 달린 층만 근사하며 엘리베이터 이동은 포함하지 않는다. 한 층은 약 3m로 계산하지만 실제 건물 층고가 다르므로 보조 신호로만 사용한다. [CMPedometer 층계](https://developer.apple.com/documentation/coremotion/cmpedometerdata/floorsascended)
  - 정확한 층을 알 수 없으면 `회사 · 층 미확인` 또는 `10층 추정 · 신뢰도 중간`으로 표시한다. 9층→10층 자동 분리는 건물 기준층 또는 `CLFloor`가 있고, 상대고도 변화 뒤 같은 층에서 일정 시간 정지했을 때만 적용한다.
  - 모든 자동 추정에는 `높음 · 중간 · 낮음` 신뢰도와 `맞아요 / 수정`을 제공한다. 사용자의 교정은 해당 기기 안의 장소·노선 개인화에만 사용한다.
- **키보드 입력은 계획명과 사용자 대분류 이름뿐 (A-6 원칙)**: 계획명에는 자동완성 + '최근 계획명' 칩(타이핑 0회 생성) + 리턴 키 = 추가 버튼. 대분류 선택·아이콘·색상·시간·완료·이동/조절에는 키보드가 나타나지 않음.
- **시간 선택 = 슬라이더 하나의 메뉴 (속도 감응)**: 별도의 휠 피커·숫자 입력 없이 미니 타임라인 슬라이더 하나로 통합.
  - 막대 자체가 컨트롤: 가운데 드래그 = 통째 이동, 양 끝 핸들 = 시작/종료 조절
  - 그냥 슬라이드 → 분 단위 이동 / **빠르게 슬라이드 → 10분 단위 반올림**으로 빠르게 이동 / **꾹 누르면 → 확대되어 세분화**(1분 미세 조정)
  - iOS 비디오 스크러빙(속도 감응 정밀도)과 같은 계열의 제스처 문법이라 학습 비용 낮음. 햅틱으로 10분 눈금마다 틱.
- **목표 상세 대시보드 (A-5 양방향 구조)**: 목표 막대를 탭하면 메인 시간표를 복제하지 않고 목표 유형에 맞는 상세를 연다.
  - 반복 규칙이 있으면 `습관 대시보드`: 이번 주 실행 횟수, 계획·실제 평균, 다음 실행, 요일별 결과, 4주 추세, HealthKit·Apple Watch 근거, 메모·반복 설정.
  - 수동 하위 계획이 있으면 `프로젝트 로드맵`: D-day, 계획·실제 누적, 현재 단계, 하위 목표 트리, 이번 주 실행, 막힌 메모, `＋ 하위 계획 내리기`.
  - 둘 다 없으면 목표 설명과 `첫 하위 계획 만들기`를 표시한다.
  - 그룹 간트는 프로젝트 상세의 `시간표에서 보기`로만 진입하는 보조 화면이다.
  - 실제 기록 연결은 명시적 `planID`를 우선하고, 연결되지 않은 수면·운동은 같은 분류와 시간 중첩이 가장 큰 반복 계획에 한 번만 연결한다.
  - 목표 자체와 모든 하위 계획을 집계 범위에 포함하되, 겹친 계획·실제 구간은 한 번만 합산한다.
- **빈 화면(첫 실행)**: 온보딩 애니메이션(계획 막대 → 현재선 → (2단계 예고) 실제 막대 순서로 소개, D-1 HIG 요구) + 샘플 계획 1~2개
- **대표 조합 온보딩 템플릿**: 첫 화면에 모든 유형을 나열하지 않고 `회사원 + 육아`, `학생 + 수능`, `회사원 + 자격증` 세 가지 대표 조합과 `직접 조합`만 제공한다. 내부 데이터는 `역할 1개 + 현재 상황 0~2개 + 목표/프로젝트 0~2개`로 구분한다.
  - **역할**: 학생·회사원·공무원·군인·운동선수·연예/공연·프리랜서/창작자·자영업자·교대/현장직·육아/가사처럼 평소 생활의 기본 틀과 표시명을 정한다.
  - **상황**: 육아·임신/출산·가족돌봄·치료/재활·교대/야간·휴직/방학·장기출장·장거리 통근처럼 현재의 시간 제약과 개인정보 기본값을 정한다.
  - **목표/프로젝트**: 수능·자격증·취업·이직·창업·대회·여행 준비·결혼 준비·이사처럼 종료점과 하위 계획을 가진다. 완료되면 역할·상황과 별개로 끝낼 수 있다.
  - 사용자에게는 하나의 자연스러운 조합 카드로 보이되, 카드 안에 `역할 · 상황 · 목표` 배지를 표시해 서로 다른 성격을 구분한다.
  - 템플릿이 바꾸는 것: 기본으로 보이는 대분류와 직업별 표시명, 빠른 추가 항목, 권장 캘린더/건강 연결, 회고 카드, 개인정보 기본값.
  - 예: `회사원 + 육아중` → 업무·돌봄·이동·건강·휴식·수면, 빠른 추가 = 회의·집중업무·등하원·가족 일정.
  - 예: `학생 + 수능` → 학업·수험·건강·휴식·수면, 빠른 추가 = 수업·과목 공부·기출·모의고사.
  - 예: `회사원 + 자격증` → 업무·학습·이동·건강·휴식·수면, 빠른 추가 = 회의·집중업무·강의·문제풀이.
  - 수업·회의·당직·촬영·등하원은 대분류가 아니라 프로젝트 또는 액션 아이템으로 생성한다.
  - 어떤 템플릿도 권한을 자동 승인하지 않는다. 군인·연예/공연 템플릿은 위치 자동 기록과 일정 공유를 기본 `끔`으로 제안한다.
  - 온보딩 이후에도 설정에서 역할·상황·목표를 바꿀 수 있고, 변경 시 기존 기록은 삭제하지 않는다.
- **달리는 고양이 외형 선택**: 위젯·Live Activity·앱 미리보기의 런캣을 `흰색 · 삼색 · 고등어 · 검정 · 회색 · 치즈 · 젖소무늬` 7종 중에서 선택할 수 있게 한다. 달리기 동작·현재 일정 위치·속도는 동일하고 털색과 무늬만 바뀐다.
  - 고양이 선택 화면은 7개 미리보기 카드를 터치하는 방식이며 키보드를 사용하지 않는다.
  - 선택값은 사용자 설정으로 저장하고 홈 위젯·잠금 화면·앱 안 미리보기에 일관되게 반영한다.
  - 흰색·젖소무늬처럼 밝은 고양이는 밝은 배경에서도 보이도록 외곽선을 유지하고, 검정 고양이는 어두운 배경에서 눈·외곽 대비를 별도로 조정한다.
  - `동작 줄이기`가 켜져 있으면 달리기 애니메이션 대신 선택한 고양이의 정지 자세를 보여준다.
- **2단계에서 추가되는 것**: 실제 막대 오버레이, 하루 하단에 `계획 6h · 실제 4.5h` 요약 칩(Sunsama 하루 집계 패턴), HealthKit 연결 설정 화면

---

## K. KPD iPhone UI 후속 대화 원문

> 사용자가 전달한 KPD와의 대화 및 작업 기록을 순서대로 보존했습니다.

```text
일 -> 주 -> 월 -> 년 이 

1. 기록이 쌓여서 합쳐지는것
2. 상위 개념에서 목표를 내리는 것


이게 되야해

Claude Browser: computer 사용함


저장됨 메모리 1개
ux는 터치 입력 위주로. 키보드 입력은 철저히 제한적

수정됨
ui-draft-iphone.html
+44
-1
이제 plan.md에 두 요구사항을 반영합니다.

수정됨
plan.md
+5
-0
시간 선택도 슬라이드로 해서 간단하게 하나의 메뉴로. 꾹 누르면 세분화 그냥 슬라이드하면 분단위 빠르게움직이면 10분 단위 반올림되서 올라가기 같은 ux 추가

시간 슬라이더 UX까지 함께 반영하겠습니다. 먼저 메모리와 인덱스를 정리합니다.


저장됨 메모리 2개

편집됨 파일 2개
+31
-0
년도를 보면 - 월, 주, 일의 요약본이 보이고
월을 보면 - 주, 일의 요약
주를 보면 - 일일의 요약

이런식의 하이라키구조면 좋겠다


편집됨 파일 2개
+80
-5

저장됨 메모리 1개
주에서 신제품 기획을 터치하면 해당 작업의 칸트 차트가 나오면 좋겠는데? 그런식으로 그룹핑 되면 좋겠다. 제일 자세하게는 일일 간트차트로. 년, 월, 주 모두 하위 아이템을 가지고 그룹핑이 되어야 해

수정됨
ui-draft-iphone.html
+1
-1
그리고 하단에는 깔끔한 메뉴가 5개 있으면 좋겠네

수정됨
ui-draft-iphone.html
+11
-0
이거 다 반영해서 ui 다시 만들어줘

업데이트됨
ui-draft-iphone.html
+202
-150
이제 plan.md의 A절과 J절을 v2 기준으로 갱신합니다.

수정됨
plan.md
+7
-3

사용 한도 초과
이거 다 반영해서 ui 다시 만들어줘

사용 한도 초과
사용 한도에 도달했습니다. 한도가 초기화된 후 다시 시도하세요.
```

---

## L. 직업·생활 유형별 온보딩 템플릿 원문

> 사용자 요청 원문  
> 대분류에 직업별로 필요한 내용이 뭘까?  
> 학생, 회사원, 군인, 수험생, 등

대표님, 직업별 내용을 모두 대분류로 추가하면 금방 복잡해집니다. `직업/생활 유형`을 온보딩 템플릿으로 선택하고, 기존 대분류 중 필요한 것만 보여주는 방식이 좋습니다.

| 유형 | 기본으로 보여줄 영역 | 빠른 추가 항목 |
|---|---|---|
| 학생 | 학업·이동·관계·취미·운동·수면 | 수업, 과제, 시험, 발표, 동아리, 알바 |
| 회사원 | 업무·프로젝트·이동·건강·관계·휴식 | 회의, 집중업무, 보고서, 출장, 출퇴근 |
| 공무원 | 행정업무·현장·이동·교육·당직·휴식 | 문서, 결재, 민원, 현장 점검, 교육, 당직 |
| 군인 | 근무·훈련·이동·운동·생활·수면 | 당직, 교육훈련, 체력단련, 개인정비, 외출·휴가 |
| 운동선수 | 훈련·경기·건강·회복·수면·식사·여행 | 기술훈련, 웨이트, 재활, 경기, 원정 |
| 연예·공연 | 작품·촬영·공연·연습·이동·건강·휴식 | 콜타임, 분장, 리허설, 촬영, 녹음, 공연 |
| 수험생 | 학습·건강·운동·휴식·수면 | 과목 공부, 문제풀이, 오답복습, 모의고사 |
| 프리랜서·창작자 | 프로젝트·건강·생활·관계·휴식 | 작업, 미팅, 영업, 정산, 콘텐츠 제작 |
| 자영업자 | 영업·운영·건강·생활·관계·수면 | 오픈, 마감, 발주, 재고, 정산, 고객 응대 |
| 교대·현장직 | 근무·이동·건강·수면·휴식 | 주간근무, 야간근무, 인수인계, 회복 |
| 육아·가사 | 돌봄·생활·건강·관계·휴식·수면 | 등하원, 병원, 식사, 집안일, 가족 일정 |

핵심은 같은 내부 대분류의 이름만 직업에 맞춰 바꾸는 겁니다.

- 학생: `프로젝트` → `학업`
- 회사원: `프로젝트` → `업무`
- 공무원: `프로젝트` → `행정업무`
- 군인: `프로젝트` → `근무`
- 운동선수: `운동` → `훈련`
- 연예·공연: `프로젝트` → `작품·활동`
- 수험생: `학습` → `수험`
- 자영업자: `프로젝트` → `영업·운영`

수업·회의·당직·모의고사 같은 것은 대분류가 아니라 그 아래의 프로젝트나 액션 아이템으로 두는 게 좋습니다.

예를 들면 수험생은:

> 학습 → 영어 → 기출문제 2회분

군인은:

> 근무 → 교육훈련 → 장비교육 2시간

회사원은:

> 업무 → 신제품 프로젝트 → 기획안 수정

이 구조라면 온보딩에서 기본 역할 하나를 선택하고 필요하면 현재 상황을 덧붙이게 할 수 있습니다. 홈 화면은 계속 단순하게 유지합니다. 군인과 연예·공연 유형은 보안·사생활 문제 때문에 위치 자동 기록과 일정 공유를 기본으로 끄는 것이 안전합니다.

### 추가 결정 — 사용자 대분류 직접 추가

- 기본 대분류만 강제하지 않고 사용자가 대분류를 직접 추가할 수 있게 한다.
- 대분류 선택 화면 마지막에 `＋ 직접 추가`를 배치한다.
- 직접 추가 필드는 `이름 · 아이콘 · 색상` 세 개로 제한한다.
- 키보드는 사용자 대분류 이름을 입력할 때만 예외적으로 표시한다.
- 생성된 대분류도 기본 대분류와 동일하게 계획·실제 기록·주/월/년 회고 집계에 사용한다.
- 생성 후 이름·아이콘·색상·순서·숨김을 편집할 수 있다.
- 사용 중인 대분류를 삭제할 때 연결된 액션 아이템을 함께 삭제하지 않는다. 다른 대분류로 옮기도록 안내한다.
- 대분류는 항목 추가·편집 과정에서만 선택하며 오늘·주·월·년 화면에 별도 분류 필터 줄을 상시 노출하지 않는다.

---

### 추가 원문 — 직업 템플릿과 현재 생활 상황

> 사용자 요청 원문  
> 직업별로 필요한게 다를 수 있잖아. 학생, 회사원, 공무원, 군인, 운동선수, 연예인 등등 뭔가 특화된 템플릿으로 분류가 되나?

직업 자체를 대분류로 늘리지 않고 `직업·생활 패턴 템플릿`으로 분류한다. 템플릿은 다음 항목을 한 번에 준비한다.

- 기본으로 보이는 대분류와 직업별 표시명
- 시작용 프로젝트와 빠른 추가 항목
- 필요한 캘린더·건강 데이터 연결 제안
- 직업별 회고 카드
- 위치 기록·일정 공유의 개인정보 기본값

사용자는 여러 역할을 동시에 가질 수 있으므로 기본 템플릿 하나를 고르고 추가 역할 또는 현재 상황을 선택적으로 겹쳐 쓴다.

| 기본 역할 | 추천 대분류 | 빠른 추가 예시 | 특화 동작 |
|---|---|---|---|
| 학생 | 학업·이동·관계·취미·운동·수면 | 수업, 과제, 시험, 발표, 동아리, 알바 | 수업표와 시험일까지 학업 시간 집계 |
| 회사원 | 업무·프로젝트·이동·건강·관계·휴식 | 회의, 집중업무, 보고서, 출장, 출퇴근 | 회의와 집중업무 비율 회고 |
| 공무원 | 행정업무·현장·이동·교육·당직·휴식 | 문서, 결재, 민원, 현장 점검, 교육, 당직 | 당직·초과근무 별도 집계 |
| 군인 | 근무·훈련·이동·운동·생활·수면 | 당직, 교육훈련, 체력단련, 개인정비, 외출·휴가 | 위치 자동 기록·일정 공유 기본 끔 |
| 운동선수 | 훈련·경기·건강·회복·수면·식사·여행 | 기술훈련, 웨이트, 재활, 경기, 원정 | HealthKit과 훈련·회복 균형 회고 |
| 연예·공연 | 작품·촬영·공연·연습·이동·건강·휴식 | 콜타임, 분장, 리허설, 촬영, 녹음, 공연 | 비공개 일정 우선, 위치·공유 기본 끔 |
| 수험생 | 수험·건강·운동·휴식·수면 | 과목 공부, 문제풀이, 오답복습, 모의고사 | 과목별 누적과 시험일까지 진도 회고 |
| 프리랜서·창작자 | 프로젝트·건강·생활·관계·휴식 | 작업, 미팅, 영업, 정산, 콘텐츠 제작 | 제작과 영업·관리 시간 분리 |
| 자영업자 | 영업·운영·건강·생활·관계·수면 | 오픈, 마감, 발주, 재고, 정산, 고객 응대 | 영업시간과 운영업무 분리 |
| 교대·현장직 | 근무·이동·건강·수면·휴식 | 주간근무, 야간근무, 인수인계, 회복 | 야간근무 후 회복·수면 회고 |
| 육아·가사 | 돌봄·생활·건강·관계·휴식·수면 | 등하원, 병원, 식사, 집안일, 가족 일정 | 가족 반복 일정과 개인 회복시간 함께 표시 |

> 사용자 요청 원문  
> 육아도 좋네. 다 반영해줘. 더 있으려나? 인물이 처한 상황에 포커싱하면 뭐가 있겠냐

직업보다 사람의 하루를 더 크게 바꾸는 것은 현재 처한 상황일 수 있다. 이를 `현재 상황 오버레이`로 제공한다.

| 현재 상황 | 추가할 추천 영역·액션 | 중요한 기본값 |
|---|---|---|
| 육아중 | 돌봄, 등하원, 수유, 병원, 가족 일정, 개인 회복 | 가족 반복 일정과 개인 시간을 함께 보여줌 |
| 임신·출산 | 진료, 휴식, 운동, 출산 준비, 회복 | 건강 권한은 선택 연결, 민감 정보 공유 끔 |
| 가족돌봄·간병 | 병원, 약, 이동, 돌봄 교대, 휴식 | 돌봄 대상 정보는 기기 안 저장 우선 |
| 치료·재활 | 진료, 약, 재활운동, 통증 메모, 수면 | 의료 판단 기능이 아닌 개인 기록임을 명시 |
| 취업·이직 | 지원, 포트폴리오, 면접, 학습, 네트워킹 | 지원 단계와 준비시간 회고 |
| 창업준비 | 제품, 고객 인터뷰, 행정, 자금, 영업 | 본업과 병행 시 시간 충돌 표시 |
| 교대·야간 | 근무, 이동, 수면, 식사, 회복 | 날짜 경계를 넘는 근무와 수면 지원 |
| 휴직·방학 | 회복, 학습, 여행, 생활 재정비 | 빈 시간을 과도하게 자동 채우지 않음 |
| 이사·정착 | 계약, 짐, 행정, 이동, 생활 기반 | 주소와 정확한 위치 공유 끔 |
| 장기출장·여행 | 이동, 예약, 업무, 시차, 휴식 | 현지 시간대와 홈 시간대 구분 |
| 은퇴·전환기 | 건강, 관계, 취미, 봉사, 학습 | 성과보다 생활 균형 회고 우선 |
| 부업·투잡 | 본업, 부업, 이동, 정산, 휴식 | 역할별 시간과 과로 신호 분리 |

1차 상황 템플릿은 위 12개로 제한한다. 선정 기준은 `하루 시간 경계가 달라지는가`, `반복 액션이 뚜렷한가`, `권한·개인정보 기본값이 달라지는가` 세 가지다. 유학·해외생활, 장거리 통근·주말가족, 반려동물 돌봄, 만성질환·에너지 관리, 복직·전역·졸업 같은 전환기는 후속 후보로 둔다. 결혼 준비·자격증·대회·집 정리처럼 종료점이 명확한 일은 인물 상황이 아니라 별도의 `프로젝트 템플릿`으로 분류한다.

온보딩 UI는 `기본 역할 1개 + 현재 상황 최대 2개`를 권장한다. 예를 들어 `회사원 + 육아중`을 선택하면 업무·돌봄·이동·건강·휴식·수면을 제안하고, 빠른 추가에 회의·집중업무·등하원·가족 일정을 배치한다. 상황 템플릿은 대분류를 상단에 늘어놓지 않고 초기 구성과 빠른 추가만 바꾸므로 오늘 화면은 그대로 단순하게 유지된다.

---

### 최신 결정 — 대표 조합 템플릿과 확장 아이콘

> 이 절의 구조가 바로 위의 `기본 역할 + 현재 상황` 온보딩 결정을 대체한다.

> 사용자 요청 원문  
> 이 모든 걸 구분할 수 있게 해주고 대표 탬플릿 몇개만 넣자.  
> 회사원 + 육아 이런식으로 대표 인물, 상황  
> 학생 + 수능  
> 회사원 + 자격증  
> 이런식으로 뭔가 조합하자. 그리고 대분류에 더 추가할 수 있는거 아이콘 추가하자

첫 화면에서 직업과 생활 상황 전체를 나열하지 않는다. 사용자가 의미를 바로 이해할 수 있는 대표 조합 세 개와 `직접 조합`만 보여준다.

| 대표 조합 | 내부 구분 | 추천 대분류 | 빠른 추가 |
|---|---|---|---|
| 회사원 + 육아 | 역할 `회사원` + 상황 `육아` | 업무·돌봄·이동·건강·휴식·수면 | 회의, 집중업무, 등하원, 가족 일정 |
| 학생 + 수능 | 역할 `학생` + 목표 `수능` | 학업·수험·건강·휴식·수면 | 수업, 과목 공부, 기출, 모의고사 |
| 회사원 + 자격증 | 역할 `회사원` + 목표 `자격증` | 업무·학습·이동·건강·휴식·수면 | 회의, 집중업무, 강의, 문제풀이 |

같은 `A + B` 카드 모양이어도 내부 의미는 세 층으로 구분한다.

1. **역할** — 평소의 기본 시간 구조와 대분류 표시명을 정한다. 예: 학생, 회사원, 공무원, 군인, 운동선수, 연예·공연, 자영업자.
2. **상황** — 현재의 시간 제약·권한·개인정보 기본값을 덧붙인다. 예: 육아, 가족돌봄, 치료·재활, 교대·야간, 휴직, 장거리 통근.
3. **목표/프로젝트** — 종료일과 하위 계획을 가진다. 예: 수능, 자격증, 취업·이직, 창업, 대회, 여행 준비, 결혼 준비, 이사.

직접 조합 화면에서는 `역할 1개 + 상황 최대 2개 + 목표 최대 2개`를 각각 다른 색의 배지와 영역으로 표시한다. 대표 조합 카드를 선택해도 나중에 구성요소를 추가·제거할 수 있다. 상황이 끝나거나 목표가 완료되어도 역할과 기존 기록은 유지한다.

대분류 직접 추가 화면의 아이콘 선택지는 8개에서 최소 24개로 확장한다. 업무·건물·학업·졸업·목표·수상·육아·가족·보안·건강·운동·수면·공연·음악·여행·장소·집·식사·카페·반려동물·쇼핑·자연·달력·메모를 심플한 선 아이콘으로 제공한다. 아이콘은 검색 없이 한 화면에서 터치로 선택하며, 대분류 이름 입력 외에는 키보드를 열지 않는다.

---

### 최신 결정 — 위치와 이동 분리, 센서 결합 추정

> 사용자 요청 원문  
> 여기에 이동 / 위치는 둘로 나누자. 예를 들면 위치는 회사, 이동은 자동차 이런식으로. 이건 GPS하고 아이폰 센서 + 애플워치 센서 조합으로 추정하는거야.  
> 이동에는 걷기, 달리기, 자전거, 버스, 지하철, 택시, 자가용, 기차, 비행기, 배 징도만  
> 지하철 같은 경우에는 고도센서도 이용해서 지하로 내려가는거 측정,  
> 위치에는 사무실이 10층이면 그거 까지 확인해서 이동 범위 확인  
> 사무실 9층 -> 10층 이거 구분

자동 기록 모델을 `위치 구간`과 `이동 구간`으로 나눈다.

| 구분 | 의미 | 예시 | 주·월·년 집계 |
|---|---|---|---|
| 위치 | 일정 시간 머문 곳 | 집, 회사 9층, 회사 10층, 식당 | 장소·층별 체류시간 |
| 이동 | 위치 사이를 이동한 방식 | 걷기, 지하철, 자가용 | 수단별 시간·거리·횟수 |
| 층 변화 맥락 | 같은 건물 안 위치가 바뀐 근거 | 회사 9층 → 10층, 상대고도 +3.1m | 건물 내 층 이동 횟수 |

이동수단은 `걷기 · 달리기 · 자전거 · 버스 · 지하철 · 택시 · 자가용 · 기차 · 비행기 · 배` 열 종류로 제한한다. 계단·엘리베이터는 별도의 이동수단으로 늘리지 않고 위치의 층 변화 근거로 보관한다.

추정 파이프라인은 다음 순서다.

1. 저전력 위치 변화와 방문 감지로 출발·도착 후보를 만든다.
2. iPhone 모션 분류로 정지·걷기·달리기·자전거·자동차 후보를 만든다.
3. GPS 속도·방향·경로, 역·공항·항구, 대중교통 노선, 기압 상대고도, 층 정보를 합친다.
4. 허용된 경우 Apple Watch의 운동·경로·활동 데이터를 보조 신호로 사용한다.
5. 결과를 `수단/장소 + 높음·중간·낮음 신뢰도`로 임시 저장하고 사용자가 한 번 탭해 확인하거나 수정한다.

지하철은 `역 진입 + 보행 종료 + GPS 약화 + 철도 경로 일치 + 상대고도 하강`이 함께 나타날 때 신뢰도를 높인다. 기압은 날씨와 환기에도 영향을 받으므로 하강만으로 지하철을 확정하지 않는다.

회사 9층→10층은 `CLFloor`가 제공되거나, 사용자가 확인한 건물 기준층에 대해 약 한 층 높이의 상대고도 변화가 감지되고 이후 10층 위치에서 머문 경우에 분리한다. 확정 근거가 부족하면 `회사 · 10층 추정`으로 표시하고 확인을 요청한다. 이 기능은 실제 층을 항상 정확히 알아내는 기능이 아니라 센서 융합 추정이며, 층 정보가 없는 건물·엘리베이터·기압 변화에서는 오차가 날 수 있다.

Apple Watch는 정확도 향상을 위한 선택적 보조 장치다. 활성 운동 중에는 운동 유형과 경로를 활용하고, 일상에서는 사용자가 허용한 HealthKit 데이터만 읽는다. Watch 센서를 하루 종일 강제로 실시간 전송하는 것을 전제로 하지 않는다.

---

### 최신 결정 — 지도 대신 하단 노선형 경로

> 사용자 요청 원문  
> 아이폰 하단에 지도말고 움직인 경로를 O-----O------O 이런식으로 지하철 노선처럼 표시하자 어떄

일간 화면 하단에는 지도를 상시 표시하지 않고, 실제로 움직인 순서를 지하철 노선도 형태로 표시한다.

```text
집 O──걷기──O 역삼역 ━━지하철━━ O 을지로입구 ──걷기── O 회사 9F ─↑1F─ ● 회사 10F
```

- `O` 노드 = 머문 위치. 장소명과 가능한 경우 층을 함께 표시한다.
- 노드 사이 선 = 이동 구간. 선 위에 걷기·지하철·자가용 같은 수단을 표시한다.
- `●` 채운 노드 = 현재 위치.
- 점선 = 신뢰도가 낮아 확인이 필요한 이동 또는 위치.
- `↑1F` = 같은 건물 안 층 변화. 별도 이동수단으로 만들지 않는다.
- 노드 탭 = 장소·층·도착/출발·체류시간 상세.
- 선 탭 = 이동수단·시간·거리·추정 신호·신뢰도 상세.
- 좌우 스와이프 = 하루의 더 긴 이동 경로 탐색.

이 표현은 실제 좌표와 도로 모양을 드러내지 않으면서 `위치 → 이동 → 위치` 순서를 바로 읽게 한다. 일간 화면에서는 하단 탭 바 바로 위에 한 줄 스트립으로 놓고, 필요할 때 위로 끌어 상세를 펼친다. 주·월·년 화면은 경로 선 대신 위치별 체류시간과 이동수단별 시간·거리 집계를 유지한다.

---

### 최신 결정 — 경로와 위치를 일간 시간축에 동기화

> 사용자 요청 원문  
> 위에 움직인 경로랑 위치를 시간에 맞춰서 이동하게 해

상단의 위치·이동 간트와 하단 노선형 경로는 동일한 `하루 시작시각 · 확대 배율 · 가로 오프셋 · 현재시각` 상태를 공유한다.

- 노드와 선을 균등 간격으로 배치하지 않고 실제 시작·종료시각을 하루 시간축의 X좌표로 변환한다.
- 머문 시간은 위치색 구간으로, 이동시간은 이동수단색 구간으로 길이를 표시한다.
- 위치나 이동이 새로 감지되면 해당 시각 위치에 노드와 선을 추가한다.
- 빨간 현재선과 하단의 빨간 현재점은 같은 시각으로 매초 또는 시스템 타이머가 갱신하는 주기마다 함께 이동한다.
- 현재 이동 중이면 빨간 점이 이동 선 위를 진행하고, 머무는 중이면 위치 구간 위에서 현재시각까지 늘어난다.
- 위 간트를 스와이프·줌하면 하단 경로도 같은 구간으로 이동·확대되고, 하단을 밀어도 위 간트가 같은 시간으로 이동한다.
- 실시간 자동 따라가기는 사용자의 수동 탐색 중 일시 정지하며 `지금으로`를 누르면 다시 켜진다.

예시:

```text
06        09        12        15        18        21
집━━━━━━○─걷기─○━━지하철━━○─걷기─○ 회사9F━━━━○↑1F○ 회사10F━━━━●
                                                        14:15 현재
```

이렇게 하면 노선도는 단순한 방문 순서가 아니라, 위 간트와 정확히 대응하는 또 하나의 시간 표현이 된다.

---

### 최신 결정 — 달리는 고양이 7종 선택

> 사용자 요청 원문  
> 고양이를 흰색고양이 삼색고양이 고등어 고양이 검정 고영이 회색 고양이 치즈 고양이 젖소무늬 고양이 이렇게 선택 가능

위젯에서 달리는 고양이는 아래 7종 중 하나를 선택할 수 있게 한다.

1. 흰색 고양이
2. 삼색 고양이
3. 고등어 고양이
4. 검정 고양이
5. 회색 고양이
6. 치즈 고양이
7. 젖소무늬 고양이

- 고양이 선택은 키보드 없이 미리보기 카드를 터치하는 방식으로 한다.
- 카드를 누르면 선택 화면 상단에서 달리는 모습을 바로 미리 본 뒤 적용한다.
- 선택한 털색과 무늬는 앱 안 미리보기, 홈 위젯, 잠금 화면·Live Activity에 동일하게 반영한다.
- 시간축 위 위치, 달리기 속도, 현재 일정에 따른 동작은 모든 고양이가 동일하며 외형만 바뀐다.
- 밝은 털은 밝은 배경에서, 검은 털은 어두운 배경에서 사라지지 않도록 외곽선·눈 대비를 별도로 둔다.
- `동작 줄이기`가 켜져 있으면 선택한 고양이를 달리지 않는 정지 자세로 표시한다.
- iPhone UI 초안 v18에서는 삼색 고양이를 선택한 예시로 보여준다.

---

### 최신 결정 — 사진 앱 연동과 사진 대분류

> 사용자 요청 원문  
> 사진첩에 연동되서 사진이 있으면 타임라인에 사진을 표시 해줘. 대분류에 사진도 추가하자

사진 앱 접근을 허용한 경우, 촬영된 사진을 일간 타임라인의 실제 촬영 시각에 맞춰 자동으로 보여준다.

- 기본 대분류에 `사진`을 추가한다.
- 일간 간트에 `사진` 행을 두고 촬영 시각을 하루 시간축의 X좌표로 변환해 썸네일을 배치한다.
- 같은 시간대에 사진이 여러 장이면 대표사진 한 장과 `+N` 배지로 묶는다.
- 썸네일을 터치하면 해당 시간대의 사진 묶음과 연결된 일정·위치 맥락을 펼친다.
- 제한된 사진 접근이면 사용자가 허용한 사진만 표시한다.
- 사진이 없거나 권한이 꺼져 있으면 사진 행을 숨겨 화면을 넓게 쓴다.
- 자동 사진은 `사진` 대분류의 촬영 순간 기록이며 시간 길이를 가진 계획 막대로 취급하지 않는다.
- 사용자가 고른 사진은 액션 아이템의 메모나 대표사진으로 연결할 수 있다.
- 주·월·년에서는 날짜별 대표사진과 사진 수로 요약한다.
- 사진은 홈 위젯·잠금 화면·Live Activity에 기본 노출하지 않으며 민감한 사진을 숨길 수 있게 한다.
- iPhone UI 초안 v19의 오늘 화면에는 `11:42`, `13:56 +2`, `18:22` 촬영 예시를 표시한다.

---

### 최신 결정 — 오늘·타임라인 통합과 설정 하단 메뉴

> 사용자 요청 원문  
> 설정을 아래 메뉴에 추가하고 하단 메뉴 두개를 하나로 합쳐서 구성해봐라

기존 하단 메뉴의 `오늘`과 `타임라인`을 `시간표` 하나로 합치고, 빈자리에 `설정`을 추가한다.

```text
시간표 | 목표 | ＋ | 회고 | 설정
```

- `시간표` 안에서 상단 세그먼트로 `일 · 주 · 월 · 년`을 전환한다.
- 시간표의 기본 시작 화면은 일간이며 설정에서 마지막 배율 또는 원하는 시작 배율로 바꿀 수 있다.
- `＋`는 빠른 항목 추가를 위해 계속 하단 중앙에 둔다.
- 목표와 회고는 미래 계획을 내리는 기능과 지난 기록을 합치는 기능이 달라 각각 유지한다.
- 설정은 하단 오른쪽의 고정 메뉴로 둔다.
- 설정 첫 화면은 `화면과 동작 · 앱 연동 · 데이터와 개인정보` 세 묶음으로만 구성한다.
- 화면과 동작: 시간표 시작 화면, 달리는 고양이.
- 앱 연동: 사진, 캘린더, 건강·Apple Watch, 위치·이동.
- 데이터와 개인정보: 위젯 노출, 권한, 내보내기, 연결 해제, 데이터 삭제.
- 그룹 간트에는 하단 메뉴를 유지한다. 항목 입력·편집 시트, 키보드, 온보딩, 완료·취소가 필요한 선택·검토 작업에서는 하단 메뉴를 숨긴다. 홈 위젯·Live Activity에는 앱 메뉴를 표시하지 않는다.
- iPhone UI 초안 v20에는 전체 하단 메뉴 교체와 ㉒ 설정 화면을 반영한다.

---

### 최신 결정 — 그룹 간트에도 하단 메뉴 유지

> 사용자 요청 원문  
> ⑤ 그룹 간트 — '신제품 기획' 탭하면  
> 여기 하단에 메뉴 있어야 하는거 아니가?

그룹 간트는 입력 시트가 아니라 프로젝트 내부를 탐색하는 일반 화면이므로 하단 5개 메뉴를 유지한다.

- 시간표에서 그룹 막대를 눌러 들어오면 하단 `시간표`를 선택 상태로 표시한다.
- 목표 카드에서 같은 그룹으로 들어오면 하단 `목표`를 선택 상태로 표시한다.
- 상단 `◀`는 이전 화면으로 돌아가고, 하단 메뉴는 시간표·목표·회고·설정 등 다른 루트로 즉시 이동한다.
- 하단 메뉴 때문에 마지막 행이 가려지지 않도록 간트 캔버스 아래쪽에 안전 여백을 둔다.
- 항목 입력·편집 시트, 키보드, 온보딩, 완료·취소가 필요한 선택·검토 작업에서만 하단 메뉴를 숨긴다.
- iPhone UI 초안 v21의 ⑤ 화면에는 `시간표`가 선택된 하단 메뉴를 표시한다.

---

### 최신 결정 — 하단 메뉴 표시·숨김 최종 구분

> 사용자 요청 원문  
> 이제 초안은 완성된 것 같은데 하단 메뉴가 사라지는 케이스 다시 구분해줘

하단 메뉴는 화면이 `상세`인지 아닌지로 결정하지 않는다. 사용자가 자유롭게 다른 탭으로 이동해도 되는 `탐색 화면`인지, 완료·취소 전까지 한 작업에 집중해야 하는 `작업 화면`인지로 구분한다.

#### 하단 메뉴 유지

| 화면 | 이유 | 선택 탭 |
|---|---|---|
| ①~④ 시간표 일·주·월·년 | 시간표의 루트 탐색 | 시간표 |
| ⑤ 그룹 간트 | 프로젝트 내부를 계속 탐색하는 화면 | 진입한 루트에 따라 시간표 또는 목표 |
| ⑥ 목표 | 목표 루트 탐색 | 목표 |
| ⑨ 계획과 실제 | 시간표의 실제 기록 보기 | 시간표 |
| ⑬ 회고 | 회고 루트 탐색 | 회고 |
| ⑭ 위치·이동 자동 기록 | 시간표 안의 자동 기록 보기 | 시간표 |
| ㉒ 설정 | 설정 루트 탐색 | 설정 |

#### 하단 메뉴 일시 숨김

| 화면 | 숨기는 이유 | 돌아가는 시점 |
|---|---|---|
| ⑦ 계획 추가 | 키보드와 입력 시트 | 추가 또는 취소 |
| ⑧ 시간 선택 | 시간 조절 작업 | 완료 또는 취소 |
| ⑩ 빠른 처리 | 현재 계획 실행·완료 시트 | 행동 선택 또는 시트 닫기 |
| ⑪ 첫 실행 | 온보딩 진행 | 설정 완료 |
| ⑮ 액션 아이템 메모 | 메모 작성과 키보드 집중 | 저장 또는 뒤로 |
| ⑯ 대분류 선택 | 계획 추가 과정의 단일 선택 | 분류 선택 또는 취소 |
| ⑰ 사용자 대분류 추가 | 이름·아이콘·색상 생성 작업 | 추가 또는 취소 |
| ⑱ 직접 조합 | 온보딩 조합 작업 | 다음 또는 취소 |
| ⑲ 대표 조합 확인 | 온보딩 최종 확인 | 시작 또는 뒤로 |
| ⑳ 이동·층 추정 상세 | 추정 결과 확인·수정 작업 | 완료 |
| ㉑ 달리는 고양이 선택 | 외형 선택·적용 작업 | 적용 또는 완료 |

#### 앱 하단 메뉴 대상 아님

| 화면 | 이유 |
|---|---|
| ⑫ 홈 위젯·Live Activity | 앱 내부 화면이 아니라 iOS 홈·잠금 화면 시스템 UI |

추가 규칙:

- 숨김 화면을 완료·취소하면 사용자가 출발했던 탭과 스크롤 위치로 돌아가며 하단 메뉴가 다시 나타난다.
- 저장되지 않은 입력이 있으면 하단 메뉴 대신 상단의 `취소·완료` 또는 스와이프 닫기를 사용한다.
- 단순 조회 상세는 하단 메뉴를 유지한다. ⑤ 그룹 간트가 이에 해당한다.
- 확인·수정·선택처럼 완료 개념이 있는 상세는 하단 메뉴를 숨긴다. ⑳·㉑이 이에 해당한다.

---
