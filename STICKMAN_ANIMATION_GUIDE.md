# Taption Plan 졸라맨 애니메이션 제작 규격

## 기준

- 논리 좌표는 `64×56`이며 지도는 12프레임·0.08초, Dynamic Island는 8프레임·0.12초를 사용한다.
- 사람은 `#D94772` 진분홍 단색, 지도 선 굵기는 1pt다. 머리와 몸 색을 나누지 않는다.
- 소품은 검정 선과 흰색 면을 기본으로 한다. 풍경만 저채도 파스텔을 쓴다.
- 레이어 순서는 `풍경 → 바닥 그림자 → 동작선 → 소품 → 사람`이다. 사람은 항상 최전면이다.
- 프레임마다 임의 값을 만들지 않는다. `phase`만 입력받는 결정론적 좌표를 사용하고 Reduce Motion에서는 phase 0을 표시한다.

## 공용 구현 위치

- 공용 위상·프레임 값: `TaptionPlan/Core/WidgetShared.swift`
- 지도 Canvas: `TaptionPlan/UI/MapHomeLocationPresentation.swift`
- Dynamic Island Canvas: `TaptionPlanWidget/TaptionPlanWidget.swift`
- 프레임 규칙 테스트: `TaptionPlanTests/FeatureEngineTests.swift`

지도와 Dynamic Island가 같은 동작을 새로 만들 때 먼저 공용 프레임 값을 정의하고 두 Canvas는 그 값만 그린다. 화면별로 별도 난수·타이머·좌표 공식을 만들지 않는다.

## 걷기 풍경 기준

- 나무 2개는 진행 반대 방향으로 한 주기 동안 88단위 이동한다.
- 구름 2개 반복 패턴은 같은 방향으로 44단위 이동해 나무보다 느리고 주기 끝이 이어지는 패럴랙스를 만든다.
- 지면 눈금은 8단위 간격으로 순환한다.
- 지도 색상은 잎 `#B9DEC5`, 구름 `#D9E8FA`, 지면 `#B7D8C1`, 줄기 `#A98D7A`를 기준으로 한다.
- Dynamic Island는 같은 위상과 형태를 쓰되 검정 배경에서 보이도록 불투명도만 낮춘다.

## 동작 확장 템플릿

1. 동작을 알아보게 하는 핵심 소품을 최대 2개 고른다.
2. 사람의 머리·몸통 기준점을 먼저 고정하고 팔·다리 끝점만 phase로 움직인다.
3. 소품은 사람보다 뒤, 상호작용하는 손만 소품 위에 보이게 그린다.
4. 지도 12프레임과 Dynamic Island 8프레임에서 시작·중간·끝 자세가 같은 의미가 되도록 정규화 위상을 사용한다.
5. 작은 compact leading 크기에서도 한눈에 구분되지 않으면 선을 더하지 말고 소품과 자세를 단순화한다.

### 시안 생성 프롬프트

> Taption Plan용 미니멀 졸라맨 애니메이션 시안. 64×56 논리 좌표, 흰 배경, 진분홍(#D94772) 사람, 1pt 둥근 선, 검정·흰색 소품, 저채도 파스텔 풍경. [행동]을 [핵심 소품]으로 명확히 표현한다. 사람은 모든 요소보다 앞에 있고 얼굴은 단순한 두 눈과 짧은 입만 사용한다. 같은 동작의 시작·중간·끝 키포즈 3개를 가로 카드로 제시한다. 글자, 그라데이션, 입체 렌더, 두꺼운 외곽선, 사실적 인체는 제외한다.

시안은 좌표 참고용이며 래스터 이미지를 앱에 넣지 않는다. 확정한 키포즈를 공용 phase 수식과 SwiftUI Canvas 도형으로 옮긴다.

## 검증 카드

- 지도: 12프레임을 0부터 순서대로 한 장에 배열한다.
- Dynamic Island: 8프레임을 compact leading 크기와 expanded 크기로 각각 배열한다.
- 사람 최전면, 시작·끝 점프 없음, 나무가 구름보다 빠름, Reduce Motion 정지, 검정·흰색 소품 규칙을 확인한다.
- 공용 프레임 테스트와 Debug 빌드가 통과한 뒤 시뮬레이터와 iPhone에서 compact·expanded 렌더를 별도로 확인한다.
