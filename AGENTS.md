# Taption Plan 개발 규칙

- 사용자에게 보이는 문구와 코드는 필요한 만큼만 작성한다. 중복 추상화와 장황한 주석은 피하고, 이름과 작은 함수로 의도를 드러낸다.
- SwiftUI 상태 변경은 실제 값이 바뀔 때만 수행한다. 고빈도 제스처는 240Hz 입력을 직접 레이아웃에 전달하지 말고 화면 갱신 예산(최대 60Hz)으로 제한한다.
- 월·년 시간표처럼 데이터가 많은 화면은 캐시와 지연 게이트를 우선 사용하고, 드래그 중 전체 데이터 재계산을 하지 않는다. 제스처 종료 시 최종값은 반드시 즉시 반영한다.
- 자동 센서·HealthKit 기록은 원본을 보존하고 사용자 편집을 허용하지 않는다. 수동 계획과 자동 기록을 구분한다.
- 기능을 바꾼 뒤 관련 단위 테스트와 Debug 빌드를 실행한다. 설치·실기기 동작은 별도 검증으로 보고한다.
- TestFlight 빌드는 업로드 성공만 완료로 보지 않는다. 처리 상태가 `제출 준비 완료`가 되면 `TP Taption Plan 내부 테스트` 그룹에 자동 연결하고, 그룹 화면에서 해당 빌드가 노출되는 것까지 확인한다. 자동 연결 경로를 사용할 수 없으면 연결 대기 상태를 남기고 수동 연결·노출 확인을 완료 게이트로 둔다.
