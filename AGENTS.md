# Taption Plan 개발 규칙

- 사용자에게 보이는 문구와 코드는 필요한 만큼만 작성한다. 중복 추상화와 장황한 주석은 피하고, 이름과 작은 함수로 의도를 드러낸다.
- SwiftUI 상태 변경은 실제 값이 바뀔 때만 수행한다. 고빈도 제스처는 240Hz 입력을 직접 레이아웃에 전달하지 말고 화면 갱신 예산(최대 60Hz)으로 제한한다.
- 월·년 시간표처럼 데이터가 많은 화면은 캐시와 지연 게이트를 우선 사용하고, 드래그 중 전체 데이터 재계산을 하지 않는다. 제스처 종료 시 최종값은 반드시 즉시 반영한다.
- 자동 센서·HealthKit 기록은 원본을 보존하고 사용자 편집을 허용하지 않는다. 수동 계획과 자동 기록을 구분한다.
- 기능을 바꾼 뒤 관련 단위 테스트와 Debug 빌드를 실행한다. 설치·실기기 동작은 별도 검증으로 보고한다.
- TestFlight 빌드는 업로드 성공만 완료로 보지 않는다. 처리 상태가 `제출 준비 완료`가 되면 `TP Taption Plan 내부 테스트` 그룹에 자동 연결하고, 그룹 화면에서 해당 빌드가 노출되는 것까지 확인한다. 자동 연결 경로를 사용할 수 없으면 연결 대기 상태를 남기고 수동 연결·노출 확인을 완료 게이트로 둔다.

## 통합 엔진·성능·저장소 규칙

- 엔진 공개 경계는 Swift Package로 유지하고 앱 UI·ActivityKit·MapKit 코드는 패키지에 넣지 않는다. 공개 facade는 원본 센서와 자동 판정 근거를 보존한다.
- SQLite를 정본으로 사용하며 Codable 바이너리 payload는 LZFSE 압축·체크섬·바이트 수를 함께 기록한다. 현재 포맷만 지원하고 동일 버전 변환은 한 번만 수행한다.
- 지도 캐시는 경로·주석·날씨·임시 위치·중심/줌 파생값만 저장한다. Apple 지도 타일은 저장하지 않으며 캐시는 bounded LRU와 지연 로딩·메모리 압력 축출을 따른다.
- NLE 스타일 파생값은 generation이 붙은 불변 projection으로 만들고 오래된 계산은 폐기한다. 입력 콜백에서 전체 재계산하지 않으며 화면 갱신은 최대 60Hz로 합친다.
- 백그라운드·재부팅·외부 센서 동작은 iOS가 실제 허용한 이벤트만 표시한다. 실행되지 않은 수집이나 다른 앱 센서 사용을 추정해 표시하지 않는다.
- 실시간 샘플은 원본 저장과 provenance를 먼저 확정한 뒤 지도·Live Activity를 증분 갱신한다. 외부 수신 센서는 지원되는 소스에 한해 빨간 심박 상태를 표시한다.
